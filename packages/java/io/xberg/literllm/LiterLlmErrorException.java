// DO NOT EDIT - auto-generated by alef
// alef:hash:35c0a6b54da5cbc405307e01f63e75e3600288f71cad50dbb5ce6270e232e8b5
package io.xberg.literllm;

/**
 * All errors that can occur when using `liter-llm`.
 */
public class LiterLlmErrorException extends Exception {
  private static final long serialVersionUID = 1L;
  /** statusCode. */
  private final int statusCode;
  /** isTransientFlag. */
  private final boolean isTransientFlag;
  /** errorType. */
  private final String errorType;

  /** Creates a new LiterLlmErrorException with the given message. */
  public LiterLlmErrorException(final String message) {
    super(message);
    this.statusCode = 0;
    this.isTransientFlag = false;
    this.errorType = "";
  }

  /** Creates a new LiterLlmErrorException with the given message and cause. */
  public LiterLlmErrorException(final String message, final Throwable cause) {
    super(message, cause);
    this.statusCode = 0;
    this.isTransientFlag = false;
    this.errorType = "";
  }

  /** Creates a new LiterLlmErrorException with the given message and all introspection fields. */
  public LiterLlmErrorException(final String message, final int statusCode, final boolean isTransientFlag, final String errorType) {
    super(message);
    this.statusCode = statusCode;
    this.isTransientFlag = isTransientFlag;
    this.errorType = errorType;
  }

  /** Returns the canonical HTTP status code associated with this error.

Maps error variants to their originating HTTP status code as set by
the private `from_status` constructor. Used by e2e assertions that check
`error.status_code` against the expected HTTP status. */
  public int getStatusCode() { return this.statusCode; }

  /** Returns `true` for errors that are worth retrying on a different service
or deployment (transient failures).

Used by `FallbackService` and
`Router` to decide whether to route to an
alternative endpoint. */
  public boolean isTransient() { return this.isTransientFlag; }

  /** Return the OpenTelemetry `error.type` string for this error variant.

Used by the tracing middleware to record the `error.type` span attribute
on failed requests per the GenAI semantic conventions. */
  public String getErrorType() { return this.errorType; }
}
