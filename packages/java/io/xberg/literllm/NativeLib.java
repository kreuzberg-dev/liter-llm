// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a0f9aabe4bc71029cba7dc5e6895847a98fc6ec05d853e01988f8e942a7fb097
// alef:handle-abi:1
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.literllm;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.Linker;
import java.lang.foreign.SymbolLookup;
import java.lang.foreign.ValueLayout;
import java.lang.invoke.MethodHandle;
import java.io.File;
import java.net.URL;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.StandardCopyOption;
import java.util.ArrayList;
import java.util.Enumeration;
import java.util.List;
import java.util.jar.JarEntry;
import java.util.jar.JarFile;

@SuppressWarnings({"PMD.AvoidCatchingGenericException", "PMD"})
final class NativeLib {
    private static final System.Logger LOGGER = System.getLogger(NativeLib.class.getName());
    private static final Linker LINKER = Linker.nativeLinker();
    private static SymbolLookup LIB;
    private static final String NATIVES_RESOURCE_ROOT = "/natives";
    private static final String LIBRARY_PATH_PROPERTY = "liter_llm_ffi.library.path";
    private static final String LIBRARY_DIRECTORY_PROPERTY = "liter_llm_ffi.library.dir";
    private static final String SKIP_BUNDLED_PROPERTY = "liter_llm_ffi.skipBundled";
    private static final String LIBRARY_PATH_ENV = "LITER_LLM_FFI_LIB_PATH";
    private static final String LIBRARY_DIRECTORY_ENV = "LITER_LLM_FFI_LIB_DIR";
    private static final String SKIP_BUNDLED_ENV = "LITER_LLM_FFI_SKIP_BUNDLED";
    private static final String[] REQUIRED_SYMBOLS = {
        "literllm_all_providers",
        "literllm_assistant_message_free",
        "literllm_assistant_message_from_json",
        "literllm_audio_content_free",
        "literllm_audio_content_from_json",
        "literllm_auth_config_free",
        "literllm_auth_config_from_json",
        "literllm_batch_list_query_free",
        "literllm_batch_list_query_from_json",
        "literllm_batch_list_response_free",
        "literllm_batch_list_response_from_json",
        "literllm_batch_list_response_to_json",
        "literllm_batch_object_free",
        "literllm_batch_object_from_json",
        "literllm_batch_object_to_json",
        "literllm_batch_request_counts_free",
        "literllm_batch_request_counts_from_json",
        "literllm_bedrock_config_free",
        "literllm_bedrock_config_from_json",
        "literllm_budget_config_free",
        "literllm_budget_config_from_json",
        "literllm_cache_config_free",
        "literllm_cache_config_from_json",
        "literllm_capabilities",
        "literllm_catalog_refresh_config_free",
        "literllm_catalog_refresh_config_from_json",
        "literllm_chat_completion_chunk_free",
        "literllm_chat_completion_chunk_from_json",
        "literllm_chat_completion_chunk_to_json",
        "literllm_chat_completion_request_free",
        "literllm_chat_completion_request_from_json",
        "literllm_chat_completion_response_free",
        "literllm_chat_completion_response_from_json",
        "literllm_chat_completion_response_to_json",
        "literllm_chat_completion_tool_free",
        "literllm_chat_completion_tool_from_json",
        "literllm_check_bound",
        "literllm_choice_free",
        "literllm_choice_from_json",
        "literllm_clear",
        "literllm_clear_catalog_overlay",
        "literllm_completion_cost",
        "literllm_completion_cost_has_result",
        "literllm_completion_cost_with_cache",
        "literllm_completion_cost_with_cache_has_result",
        "literllm_complex_provider_names",
        "literllm_count_request_tokens",
        "literllm_count_tokens",
        "literllm_create_batch_request_free",
        "literllm_create_batch_request_from_json",
        "literllm_create_client",
        "literllm_create_client_from_json",
        "literllm_create_file_request_free",
        "literllm_create_file_request_from_json",
        "literllm_create_image_request_free",
        "literllm_create_image_request_from_json",
        "literllm_create_response_request_free",
        "literllm_create_response_request_from_json",
        "literllm_create_speech_request_free",
        "literllm_create_speech_request_from_json",
        "literllm_create_transcription_request_free",
        "literllm_create_transcription_request_from_json",
        "literllm_custom_provider_config_free",
        "literllm_custom_provider_config_from_json",
        "literllm_decode_data_url",
        "literllm_decoded_data_url_free",
        "literllm_decoded_data_url_from_json",
        "literllm_decoded_data_url_to_json",
        "literllm_default_client_cancel_batch",
        "literllm_default_client_cancel_response",
        "literllm_default_client_chat",
        "literllm_default_client_chat_stream_free",
        "literllm_default_client_chat_stream_next",
        "literllm_default_client_chat_stream_start",
        "literllm_default_client_create_batch",
        "literllm_default_client_create_file",
        "literllm_default_client_create_response",
        "literllm_default_client_delete_file",
        "literllm_default_client_embed",
        "literllm_default_client_fetch_batch_for_polling",
        "literllm_default_client_file_content",
        "literllm_default_client_free",
        "literllm_default_client_image_generate",
        "literllm_default_client_list_batches",
        "literllm_default_client_list_files",
        "literllm_default_client_list_models",
        "literllm_default_client_moderate",
        "literllm_default_client_ocr",
        "literllm_default_client_rerank",
        "literllm_default_client_retrieve_batch",
        "literllm_default_client_retrieve_file",
        "literllm_default_client_retrieve_response",
        "literllm_default_client_search",
        "literllm_default_client_speech",
        "literllm_default_client_transcribe",
        "literllm_default_client_wait_for_batch",
        "literllm_delete_response_free",
        "literllm_delete_response_from_json",
        "literllm_delete_response_to_json",
        "literllm_developer_message_free",
        "literllm_developer_message_from_json",
        "literllm_document_content_free",
        "literllm_document_content_from_json",
        "literllm_embedding_object_free",
        "literllm_embedding_object_from_json",
        "literllm_embedding_request_free",
        "literllm_embedding_request_from_json",
        "literllm_embedding_response_free",
        "literllm_embedding_response_from_json",
        "literllm_embedding_response_to_json",
        "literllm_encode_data_url",
        "literllm_encode_data_url_len",
        "literllm_ensure_crypto_provider",
        "literllm_file_list_query_free",
        "literllm_file_list_query_from_json",
        "literllm_file_list_response_free",
        "literllm_file_list_response_from_json",
        "literllm_file_list_response_to_json",
        "literllm_file_object_free",
        "literllm_file_object_from_json",
        "literllm_file_object_to_json",
        "literllm_free_bytes",
        "literllm_free_string",
        "literllm_function_call_free",
        "literllm_function_call_from_json",
        "literllm_function_definition_free",
        "literllm_function_definition_from_json",
        "literllm_function_message_free",
        "literllm_function_message_from_json",
        "literllm_image_free",
        "literllm_image_from_json",
        "literllm_image_url_free",
        "literllm_image_url_from_json",
        "literllm_images_response_free",
        "literllm_images_response_from_json",
        "literllm_images_response_to_json",
        "literllm_in_flight_limit_config_free",
        "literllm_in_flight_limit_config_from_json",
        "literllm_install_catalog_overlay_from_str",
        "literllm_intent_prototype_free",
        "literllm_json_schema_format_free",
        "literllm_json_schema_format_from_json",
        "literllm_last_error_code",
        "literllm_last_error_context",
        "literllm_llm_budget_config_free",
        "literllm_llm_budget_config_from_json",
        "literllm_llm_cache_config_free",
        "literllm_llm_cache_config_from_json",
        "literllm_llm_config_free",
        "literllm_llm_config_from_json",
        "literllm_llm_in_flight_limit_config_free",
        "literllm_llm_in_flight_limit_config_from_json",
        "literllm_llm_provider_config_free",
        "literllm_llm_provider_config_from_json",
        "literllm_llm_rate_limit_config_free",
        "literllm_llm_rate_limit_config_from_json",
        "literllm_model_info",
        "literllm_model_info_free",
        "literllm_model_info_from_json",
        "literllm_model_info_to_json",
        "literllm_model_object_free",
        "literllm_model_object_from_json",
        "literllm_model_tier_free",
        "literllm_model_tier_from_json",
        "literllm_models_list_response_free",
        "literllm_models_list_response_from_json",
        "literllm_models_list_response_to_json",
        "literllm_moderation_categories_free",
        "literllm_moderation_categories_from_json",
        "literllm_moderation_category_scores_free",
        "literllm_moderation_category_scores_from_json",
        "literllm_moderation_request_free",
        "literllm_moderation_request_from_json",
        "literllm_moderation_response_free",
        "literllm_moderation_response_from_json",
        "literllm_moderation_response_to_json",
        "literllm_moderation_result_free",
        "literllm_moderation_result_from_json",
        "literllm_ocr_image_free",
        "literllm_ocr_image_from_json",
        "literllm_ocr_page_free",
        "literllm_ocr_page_from_json",
        "literllm_ocr_request_free",
        "literllm_ocr_request_from_json",
        "literllm_ocr_response_free",
        "literllm_ocr_response_from_json",
        "literllm_ocr_response_to_json",
        "literllm_page_dimensions_free",
        "literllm_page_dimensions_from_json",
        "literllm_prompt_tokens_details_free",
        "literllm_prompt_tokens_details_from_json",
        "literllm_provider_capabilities_free",
        "literllm_provider_capabilities_from_json",
        "literllm_provider_capabilities_to_json",
        "literllm_provider_config_free",
        "literllm_provider_config_from_json",
        "literllm_rate_limit_config_free",
        "literllm_rate_limit_config_from_json",
        "literllm_record_cost_usd",
        "literllm_refresh_catalog",
        "literllm_refresh_outcome_free",
        "literllm_refresh_outcome_to_json",
        "literllm_register_custom_provider",
        "literllm_rerank_request_free",
        "literllm_rerank_request_from_json",
        "literllm_rerank_response_free",
        "literllm_rerank_response_from_json",
        "literllm_rerank_response_to_json",
        "literllm_rerank_result_document_free",
        "literllm_rerank_result_document_from_json",
        "literllm_rerank_result_free",
        "literllm_rerank_result_from_json",
        "literllm_response_object_free",
        "literllm_response_object_from_json",
        "literllm_response_object_to_json",
        "literllm_response_output_item_free",
        "literllm_response_output_item_from_json",
        "literllm_response_tool_free",
        "literllm_response_tool_from_json",
        "literllm_response_usage_free",
        "literllm_response_usage_from_json",
        "literllm_search_request_free",
        "literllm_search_request_from_json",
        "literllm_search_response_free",
        "literllm_search_response_from_json",
        "literllm_search_response_to_json",
        "literllm_search_result_free",
        "literllm_search_result_from_json",
        "literllm_singleflight_result_free",
        "literllm_specific_function_free",
        "literllm_specific_function_from_json",
        "literllm_specific_tool_choice_free",
        "literllm_specific_tool_choice_from_json",
        "literllm_stream_choice_free",
        "literllm_stream_choice_from_json",
        "literllm_stream_delta_free",
        "literllm_stream_delta_from_json",
        "literllm_stream_function_call_free",
        "literllm_stream_function_call_from_json",
        "literllm_stream_options_free",
        "literllm_stream_options_from_json",
        "literllm_stream_tool_call_free",
        "literllm_stream_tool_call_from_json",
        "literllm_system_message_free",
        "literllm_system_message_from_json",
        "literllm_tool_call_free",
        "literllm_tool_call_from_json",
        "literllm_tool_message_free",
        "literllm_tool_message_from_json",
        "literllm_transcription_response_free",
        "literllm_transcription_response_from_json",
        "literllm_transcription_response_to_json",
        "literllm_transcription_segment_free",
        "literllm_transcription_segment_from_json",
        "literllm_unregister_custom_provider",
        "literllm_usage_free",
        "literllm_usage_from_json",
        "literllm_user_message_free",
        "literllm_user_message_from_json",
        "literllm_wait_for_batch_config_free",
        "literllm_wait_for_batch_config_from_json"
    };
    private static final Object NATIVE_EXTRACT_LOCK = new Object();
    private static String cachedExtractKey;
    private static Path cachedExtractDir;

    static {
        Path loadedLibraryPath = loadNativeLibrary();
        try {
            Arena arena = Arena.ofShared();
            // Try the loaded library path (for System.load() with absolute path)
            if (loadedLibraryPath != null) {
                try {
                    LIB = SymbolLookup.libraryLookup(loadedLibraryPath, arena);
                } catch (RuntimeException | Error inner) {
                    // If Path variant fails, fallback to defaultLookup
                    LIB = LINKER.defaultLookup();
                }
            } else {
                LIB = LINKER.defaultLookup();
            }
            validateRequiredSymbols(loadedLibraryPath);
        } catch (RuntimeException | Error e) {
            ExceptionInInitializerError error = new ExceptionInInitializerError(
                "Failed to initialize library symbols: " + e.getMessage());
            error.initCause(e);
            throw error;
        }
    }

    private static Path loadNativeLibrary() {
        Path loadedLibraryPath = null;
        String osName = System.getProperty("os.name", "").toLowerCase(java.util.Locale.ROOT);
        String osArch = System.getProperty("os.arch", "").toLowerCase(java.util.Locale.ROOT);

        String libName;
        String libExt;
        if (osName.contains("mac") || osName.contains("darwin")) {
            libName = "libliter_llm_ffi";
            libExt = ".dylib";
        } else if (osName.contains("win")) {
            libName = "liter_llm_ffi";
            libExt = ".dll";
        } else {
            libName = "libliter_llm_ffi";
            libExt = ".so";
        }

        String nativesRid = resolveNativesRid(osName, osArch);
        String nativesDir = NATIVES_RESOURCE_ROOT + "/" + nativesRid;

        String explicitPath = firstNonBlank(System.getProperty(LIBRARY_PATH_PROPERTY), System.getenv(LIBRARY_PATH_ENV));
        if (explicitPath != null) {
            return loadAbsolutePath(Paths.get(explicitPath));
        }

        String explicitDirectory = firstNonBlank(
                System.getProperty(LIBRARY_DIRECTORY_PROPERTY),
                System.getenv(LIBRARY_DIRECTORY_ENV));
        if (explicitDirectory != null) {
            return loadAbsolutePath(Paths.get(explicitDirectory, libName + libExt));
        }

        String skipBundledValue = firstNonBlank(
                System.getProperty(SKIP_BUNDLED_PROPERTY),
                System.getenv(SKIP_BUNDLED_ENV));
        if (!Boolean.parseBoolean(skipBundledValue)) {
            Path extracted = tryExtractAndLoadFromResources(nativesDir, libName, libExt);
            if (extracted != null) {
                return extracted;
            }
        }

        try {
            System.loadLibrary("liter_llm_ffi");
            // Find the full path by searching java.library.path
            Path libPath = findLoadedLibraryPath(libName, libExt);
            if (libPath != null) {
                loadedLibraryPath = libPath;
            }
        } catch (UnsatisfiedLinkError e) {
            String msg = "Failed to load liter_llm_ffi native library. Expected resource: " + nativesDir + "/" + libName
                    + libExt + " (RID: " + nativesRid + "). "
                    + "Ensure the library is bundled in the JAR under natives/{os-arch}/, "
                    + "place it on java.library.path, set -D" + LIBRARY_PATH_PROPERTY
                    + "=/absolute/path, or set " + LIBRARY_PATH_ENV + ".";
            UnsatisfiedLinkError out = new UnsatisfiedLinkError(msg + " Original error: " + e.getMessage());
            out.initCause(e);
            throw out;
        }
        return loadedLibraryPath;
    }

    private static String firstNonBlank(String preferred, String fallback) {
        if (preferred != null && !preferred.isBlank()) {
            return preferred;
        }
        if (fallback != null && !fallback.isBlank()) {
            return fallback;
        }
        return null;
    }

    private static Path loadAbsolutePath(Path configuredPath) {
        Path absolutePath = configuredPath.toAbsolutePath().normalize();
        if (!Files.isRegularFile(absolutePath)) {
            throw new UnsatisfiedLinkError("Configured native library does not exist: " + absolutePath);
        }
        System.load(absolutePath.toString());
        return absolutePath;
    }

    private static void validateRequiredSymbols(Path loadedLibraryPath) {
        List<String> missing = new ArrayList<>();
        for (String symbol : REQUIRED_SYMBOLS) {
            if (LIB.find(symbol).isEmpty() && LIB.find("_" + symbol).isEmpty()) {
                missing.add(symbol);
            }
        }
        if (missing.isEmpty()) {
            return;
        }

        int exportedCount = REQUIRED_SYMBOLS.length - missing.size();
        String loadedFrom = loadedLibraryPath == null
                ? "System.loadLibrary(liter_llm_ffi) / java.library.path"
                : loadedLibraryPath.toString();
        throw new UnsatisfiedLinkError(
                "The loaded liter_llm_ffi native library is stale relative to this binding: it exports "
                        + exportedCount + " of " + REQUIRED_SYMBOLS.length + " required symbols. Missing: "
                        + String.join(", ", missing) + ". Loaded from: " + loadedFrom
                        + ". Rebuild the native library, point this binding at a current one with -D"
                        + LIBRARY_PATH_PROPERTY + "=/absolute/path, or bypass bundled resources with -D"
                        + SKIP_BUNDLED_PROPERTY + "=true.");
    }

    private static Path tryExtractAndLoadFromResources(String nativesDir, String libName, String libExt) {
        String resourcePath = nativesDir + "/" + libName + libExt;
        URL resource = NativeLib.class.getResource(resourcePath);
        if (resource == null) {
            return null;
        }

        try {
            Path tempDir = extractOrReuseNativeDirectory(nativesDir);
            Path libPath = tempDir.resolve(libName + libExt);
            if (!Files.exists(libPath)) {
                throw new UnsatisfiedLinkError("Missing extracted native library: " + libPath);
            }
            Path absPath = libPath.toAbsolutePath();
            System.load(absPath.toString());
            return absPath;
        } catch (Exception | Error e) {
            LOGGER.log(System.Logger.Level.WARNING, "Failed to extract and load native library from resources", e);
            return null;
        }
    }

    private static Path extractOrReuseNativeDirectory(String nativesDir) throws Exception {
        URL location = NativeLib.class.getProtectionDomain().getCodeSource().getLocation();
        if (location == null) {
            throw new IllegalStateException("Missing code source location for liter_llm_ffi JAR");
        }

        Path codePath = Path.of(location.toURI());
        String key = codePath.toAbsolutePath() + "::" + nativesDir;

        synchronized (NATIVE_EXTRACT_LOCK) {
            if (cachedExtractDir != null && key.equals(cachedExtractKey)) {
                return cachedExtractDir;
            }
            Path tempDir = Files.createTempDirectory("liter_llm_ffi_native");
            tempDir.toFile().deleteOnExit();
            List<Path> extracted = extractNativeDirectory(codePath, nativesDir, tempDir);
            if (extracted.isEmpty()) {
                throw new IllegalStateException("No native files extracted from resources dir: " + nativesDir);
            }
            cachedExtractKey = key;
            cachedExtractDir = tempDir;
            return tempDir;
        }
    }

    private static List<Path> extractNativeDirectory(Path codePath, String nativesDir, Path destDir) throws Exception {
        if (!Files.exists(destDir) || !Files.isDirectory(destDir)) {
            throw new IllegalArgumentException("Destination directory does not exist: " + destDir);
        }

        String prefix = nativesDir.startsWith("/") ? nativesDir.substring(1) : nativesDir;
        if (!prefix.endsWith("/")) {
            prefix = prefix + "/";
        }

        if (Files.isDirectory(codePath)) {
            Path nativesPath = codePath.resolve(prefix);
            if (!Files.exists(nativesPath) || !Files.isDirectory(nativesPath)) {
                return List.of();
            }
            return copyDirectory(nativesPath, destDir);
        }

        List<Path> extracted = new ArrayList<>();
        try (JarFile jar = new JarFile(codePath.toFile())) {
            Enumeration<JarEntry> entries = jar.entries();
            while (entries.hasMoreElements()) {
                JarEntry entry = entries.nextElement();
                String name = entry.getName();
                if (!name.startsWith(prefix) || entry.isDirectory()) {
                    continue;
                }
                String relative = name.substring(prefix.length());
                Path out = safeResolve(destDir, relative);
                Files.createDirectories(out.getParent());
                try (var in = jar.getInputStream(entry)) {
                    Files.copy(in, out, StandardCopyOption.REPLACE_EXISTING);
                }
                out.toFile().deleteOnExit();
                extracted.add(out);
            }
        }
        return extracted;
    }

    private static List<Path> copyDirectory(Path srcDir, Path destDir) throws Exception {
        List<Path> copied = new ArrayList<>();
        try (var paths = Files.walk(srcDir)) {
            for (Path src : (Iterable<Path>) paths::iterator) {
                if (Files.isDirectory(src)) {
                    continue;
                }
                Path relative = srcDir.relativize(src);
                Path out = safeResolve(destDir, relative.toString());
                Files.createDirectories(out.getParent());
                Files.copy(src, out, StandardCopyOption.REPLACE_EXISTING);
                out.toFile().deleteOnExit();
                copied.add(out);
            }
        }
        return copied;
    }

    private static Path safeResolve(Path destDir, String relative) throws Exception {
        Path normalizedDest = destDir.toAbsolutePath().normalize();
        Path out = normalizedDest.resolve(relative).normalize();
        if (!out.startsWith(normalizedDest)) {
            throw new SecurityException("Blocked extracting native file outside destination directory: " + relative);
        }
        return out;
    }

    private static String resolveNativesRid(String osName, String osArch) {
        // Classifier names match go_java_platform(): `linux-aarch64`, `macos-arm64`,
        // `windows-x86_64`, etc. macOS distinguishes arm64 (ARM-based), while other
        // platforms use aarch64. Align with src/publish/platform.rs::go_java_platform().
        boolean isMac = osName.contains("mac") || osName.contains("darwin");
        boolean isWindows = osName.contains("win");

        String arch;
        if (osArch.contains("aarch64") || osArch.contains("arm64")) {
            // macOS uses "arm64" for aarch64; Linux uses "aarch64"
            arch = isMac ? "arm64" : "aarch64";
        } else if (osArch.contains("x86_64") || osArch.contains("amd64")) {
            arch = "x86_64";
        } else {
            arch = osArch.replaceAll("[^a-z0-9_]+", "");
        }

        String os;
        if (isMac) {
            os = "macos";
        } else if (isWindows) {
            os = "windows";
        } else {
            os = "linux";
        }

        return os + "-" + arch;
    }

    private static Path findLoadedLibraryPath(String fullLibName, String libExt) {
        // Search java.library.path for the library file
        String javaLibPath = System.getProperty("java.library.path");
        if (javaLibPath != null) {
            for (String path : javaLibPath.split(File.pathSeparator)) {
                Path libPath = Paths.get(path, fullLibName + libExt);
                if (Files.exists(libPath)) {
                    try {
                        return libPath.toRealPath();
                    } catch (java.io.IOException e) {
                        return libPath.toAbsolutePath();
                    }
                }
            }
        }
        // Library not found in java.library.path
        return null;
    }


    static final MethodHandle LITERLLM_ALL_PROVIDERS = LINKER.downcallHandle(
        LIB.find("literllm_all_providers")
            .or(() -> LIB.find("_literllm_all_providers"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_all_providers"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_all_providers"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_all_providers" +
                " (tried: literllm_all_providers, _literllm_all_providers). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CAPABILITIES = LINKER.downcallHandle(
        LIB.find("literllm_capabilities")
            .or(() -> LIB.find("_literllm_capabilities"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_capabilities"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_capabilities"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_capabilities" +
                " (tried: literllm_capabilities, _literllm_capabilities). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHECK_BOUND = LINKER.downcallHandle(
        LIB.find("literllm_check_bound")
            .or(() -> LIB.find("_literllm_check_bound"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_check_bound"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_check_bound"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_check_bound" +
                " (tried: literllm_check_bound, _literllm_check_bound). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle LITERLLM_CLEAR = LINKER.downcallHandle(
        LIB.find("literllm_clear")
            .or(() -> LIB.find("_literllm_clear"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_clear"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_clear"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_clear" +
                " (tried: literllm_clear, _literllm_clear). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid()
    );


    static final MethodHandle LITERLLM_CLEAR_CATALOG_OVERLAY = LINKER.downcallHandle(
        LIB.find("literllm_clear_catalog_overlay")
            .or(() -> LIB.find("_literllm_clear_catalog_overlay"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_clear_catalog_overlay"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_clear_catalog_overlay"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_clear_catalog_overlay" +
                " (tried: literllm_clear_catalog_overlay, _literllm_clear_catalog_overlay). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid()
    );


    static final MethodHandle LITERLLM_COMPLETION_COST = LINKER.downcallHandle(
        LIB.find("literllm_completion_cost")
            .or(() -> LIB.find("_literllm_completion_cost"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_completion_cost"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_completion_cost"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_completion_cost" +
                " (tried: literllm_completion_cost, _literllm_completion_cost). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_DOUBLE, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle LITERLLM_COMPLETION_COST_HAS_RESULT = LINKER.downcallHandle(
        LIB.find("literllm_completion_cost_has_result")
            .or(() -> LIB.find("_literllm_completion_cost_has_result"))
            .or(() -> LINKER.defaultLookup().find("literllm_completion_cost_has_result"))
            .or(() -> LINKER.defaultLookup().find("_literllm_completion_cost_has_result"))
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_completion_cost_has_result" +
                " (tried: literllm_completion_cost_has_result, _literllm_completion_cost_has_result). " +
                "This is the presence companion for an Option-returning export; " +
                "regenerate the FFI crate so it is exported alongside its primary function.")),
        FunctionDescriptor.of(ValueLayout.JAVA_INT, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle LITERLLM_COMPLETION_COST_WITH_CACHE = LINKER.downcallHandle(
        LIB.find("literllm_completion_cost_with_cache")
            .or(() -> LIB.find("_literllm_completion_cost_with_cache"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_completion_cost_with_cache"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_completion_cost_with_cache"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_completion_cost_with_cache" +
                " (tried: literllm_completion_cost_with_cache, _literllm_completion_cost_with_cache). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.JAVA_DOUBLE,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG
        )
    );


    static final MethodHandle LITERLLM_COMPLETION_COST_WITH_CACHE_HAS_RESULT = LINKER.downcallHandle(
        LIB.find("literllm_completion_cost_with_cache_has_result")
            .or(() -> LIB.find("_literllm_completion_cost_with_cache_has_result"))
            .or(() -> LINKER.defaultLookup().find("literllm_completion_cost_with_cache_has_result"))
            .or(() -> LINKER.defaultLookup().find("_literllm_completion_cost_with_cache_has_result"))
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_completion_cost_with_cache_has_result" +
                " (tried: literllm_completion_cost_with_cache_has_result, _literllm_completion_cost_with_cache_has_result). " +
                "This is the presence companion for an Option-returning export; " +
                "regenerate the FFI crate so it is exported alongside its primary function.")),
        FunctionDescriptor.of(
            ValueLayout.JAVA_INT,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG
        )
    );


    static final MethodHandle LITERLLM_COMPLEX_PROVIDER_NAMES = LINKER.downcallHandle(
        LIB.find("literllm_complex_provider_names")
            .or(() -> LIB.find("_literllm_complex_provider_names"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_complex_provider_names"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_complex_provider_names"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_complex_provider_names" +
                " (tried: literllm_complex_provider_names, _literllm_complex_provider_names). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_COUNT_REQUEST_TOKENS = LINKER.downcallHandle(
        LIB.find("literllm_count_request_tokens")
            .or(() -> LIB.find("_literllm_count_request_tokens"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_count_request_tokens"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_count_request_tokens"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_count_request_tokens" +
                " (tried: literllm_count_request_tokens, _literllm_count_request_tokens). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_COUNT_TOKENS = LINKER.downcallHandle(
        LIB.find("literllm_count_tokens")
            .or(() -> LIB.find("_literllm_count_tokens"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_count_tokens"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_count_tokens"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_count_tokens" +
                " (tried: literllm_count_tokens, _literllm_count_tokens). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_CLIENT = LINKER.downcallHandle(
        LIB.find("literllm_create_client")
            .or(() -> LIB.find("_literllm_create_client"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_client"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_client"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_create_client" +
                " (tried: literllm_create_client, _literllm_create_client). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS
        )
    );


    static final MethodHandle LITERLLM_CREATE_CLIENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_create_client_from_json")
            .or(() -> LIB.find("_literllm_create_client_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_client_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_client_from_json"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_create_client_from_json" +
                " (tried: literllm_create_client_from_json, _literllm_create_client_from_json). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DECODE_DATA_URL = LINKER.downcallHandle(
        LIB.find("literllm_decode_data_url")
            .or(() -> LIB.find("_literllm_decode_data_url"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_decode_data_url"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_decode_data_url"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_decode_data_url" +
                " (tried: literllm_decode_data_url, _literllm_decode_data_url). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_ENCODE_DATA_URL = LINKER.downcallHandle(
        LIB.find("literllm_encode_data_url")
            .or(() -> LIB.find("_literllm_encode_data_url"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_encode_data_url"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_encode_data_url"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_encode_data_url" +
                " (tried: literllm_encode_data_url, _literllm_encode_data_url). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );

    static final MethodHandle LITERLLM_ENCODE_DATA_URL_LEN = LINKER.downcallHandle(
        LIB.find("literllm_encode_data_url_len")
            .or(() -> LIB.find("_literllm_encode_data_url_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_encode_data_url_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_encode_data_url_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_ENSURE_CRYPTO_PROVIDER = LINKER.downcallHandle(
        LIB.find("literllm_ensure_crypto_provider")
            .or(() -> LIB.find("_literllm_ensure_crypto_provider"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ensure_crypto_provider"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ensure_crypto_provider"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_ensure_crypto_provider" +
                " (tried: literllm_ensure_crypto_provider, _literllm_ensure_crypto_provider). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid()
    );


    static final MethodHandle LITERLLM_INSTALL_CATALOG_OVERLAY_FROM_STR = LINKER.downcallHandle(
        LIB.find("literllm_install_catalog_overlay_from_str")
            .or(() -> LIB.find("_literllm_install_catalog_overlay_from_str"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_install_catalog_overlay_from_str"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_install_catalog_overlay_from_str"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_install_catalog_overlay_from_str" +
                " (tried: literllm_install_catalog_overlay_from_str, _literllm_install_catalog_overlay_from_str). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODEL_INFO = LINKER.downcallHandle(
        LIB.find("literllm_model_info")
            .or(() -> LIB.find("_literllm_model_info"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_model_info"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_model_info"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_model_info" +
                " (tried: literllm_model_info, _literllm_model_info). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RECORD_COST_USD = LINKER.downcallHandle(
        LIB.find("literllm_record_cost_usd")
            .or(() -> LIB.find("_literllm_record_cost_usd"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_record_cost_usd"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_record_cost_usd"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_record_cost_usd" +
                " (tried: literllm_record_cost_usd, _literllm_record_cost_usd). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_DOUBLE)
    );


    static final MethodHandle LITERLLM_REFRESH_CATALOG = LINKER.downcallHandle(
        LIB.find("literllm_refresh_catalog")
            .or(() -> LIB.find("_literllm_refresh_catalog"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_refresh_catalog"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_refresh_catalog"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_refresh_catalog" +
                " (tried: literllm_refresh_catalog, _literllm_refresh_catalog). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_REGISTER_CUSTOM_PROVIDER = LINKER.downcallHandle(
        LIB.find("literllm_register_custom_provider")
            .or(() -> LIB.find("_literllm_register_custom_provider"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_register_custom_provider"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_register_custom_provider"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_register_custom_provider" +
                " (tried: literllm_register_custom_provider, _literllm_register_custom_provider). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_UNREGISTER_CUSTOM_PROVIDER = LINKER.downcallHandle(
        LIB.find("literllm_unregister_custom_provider")
            .or(() -> LIB.find("_literllm_unregister_custom_provider"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_unregister_custom_provider"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_unregister_custom_provider"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_unregister_custom_provider" +
                " (tried: literllm_unregister_custom_provider, _literllm_unregister_custom_provider). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FREE_STRING = LINKER.downcallHandle(
        LIB.find("literllm_free_string")
            .or(() -> LIB.find("_literllm_free_string"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_free_string"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_free_string"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FREE_BYTES = LINKER.downcallHandle(
        LIB.find("literllm_free_bytes")
            .or(() -> LIB.find("_literllm_free_bytes"))  // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_free_bytes"))  // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_free_bytes"))  // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CHAT = LINKER.downcallHandle(
        LIB.find("literllm_default_client_chat")
            .or(() -> LIB.find("_literllm_default_client_chat"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_chat"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_chat"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_chat" +
                " (tried: literllm_default_client_chat, _literllm_default_client_chat). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_EMBED = LINKER.downcallHandle(
        LIB.find("literllm_default_client_embed")
            .or(() -> LIB.find("_literllm_default_client_embed"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_embed"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_embed"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_embed" +
                " (tried: literllm_default_client_embed, _literllm_default_client_embed). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_LIST_MODELS = LINKER.downcallHandle(
        LIB.find("literllm_default_client_list_models")
            .or(() -> LIB.find("_literllm_default_client_list_models"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_list_models"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_list_models"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_list_models" +
                " (tried: literllm_default_client_list_models, _literllm_default_client_list_models). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_IMAGE_GENERATE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_image_generate")
            .or(() -> LIB.find("_literllm_default_client_image_generate"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_image_generate"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_image_generate"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_image_generate" +
                " (tried: literllm_default_client_image_generate, _literllm_default_client_image_generate). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_SPEECH = LINKER.downcallHandle(
        LIB.find("literllm_default_client_speech")
            .or(() -> LIB.find("_literllm_default_client_speech"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_speech"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_speech"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_speech" +
                " (tried: literllm_default_client_speech, _literllm_default_client_speech). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_TRANSCRIBE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_transcribe")
            .or(() -> LIB.find("_literllm_default_client_transcribe"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_transcribe"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_transcribe"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_transcribe" +
                " (tried: literllm_default_client_transcribe, _literllm_default_client_transcribe). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_MODERATE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_moderate")
            .or(() -> LIB.find("_literllm_default_client_moderate"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_moderate"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_moderate"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_moderate" +
                " (tried: literllm_default_client_moderate, _literllm_default_client_moderate). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_RERANK = LINKER.downcallHandle(
        LIB.find("literllm_default_client_rerank")
            .or(() -> LIB.find("_literllm_default_client_rerank"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_rerank"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_rerank"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_rerank" +
                " (tried: literllm_default_client_rerank, _literllm_default_client_rerank). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_SEARCH = LINKER.downcallHandle(
        LIB.find("literllm_default_client_search")
            .or(() -> LIB.find("_literllm_default_client_search"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_search"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_search"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_search" +
                " (tried: literllm_default_client_search, _literllm_default_client_search). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_OCR = LINKER.downcallHandle(
        LIB.find("literllm_default_client_ocr")
            .or(() -> LIB.find("_literllm_default_client_ocr"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_ocr"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_ocr"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_ocr" +
                " (tried: literllm_default_client_ocr, _literllm_default_client_ocr). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CREATE_FILE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_create_file")
            .or(() -> LIB.find("_literllm_default_client_create_file"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_create_file"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_create_file"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_create_file" +
                " (tried: literllm_default_client_create_file, _literllm_default_client_create_file). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_RETRIEVE_FILE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_retrieve_file")
            .or(() -> LIB.find("_literllm_default_client_retrieve_file"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_retrieve_file"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_retrieve_file"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_retrieve_file" +
                " (tried: literllm_default_client_retrieve_file, _literllm_default_client_retrieve_file). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_DELETE_FILE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_delete_file")
            .or(() -> LIB.find("_literllm_default_client_delete_file"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_delete_file"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_delete_file"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_delete_file" +
                " (tried: literllm_default_client_delete_file, _literllm_default_client_delete_file). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_LIST_FILES = LINKER.downcallHandle(
        LIB.find("literllm_default_client_list_files")
            .or(() -> LIB.find("_literllm_default_client_list_files"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_list_files"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_list_files"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_list_files" +
                " (tried: literllm_default_client_list_files, _literllm_default_client_list_files). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_FILE_CONTENT = LINKER.downcallHandle(
        LIB.find("literllm_default_client_file_content")
            .or(() -> LIB.find("_literllm_default_client_file_content"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_file_content"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_file_content"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_file_content" +
                " (tried: literllm_default_client_file_content, _literllm_default_client_file_content). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CREATE_BATCH = LINKER.downcallHandle(
        LIB.find("literllm_default_client_create_batch")
            .or(() -> LIB.find("_literllm_default_client_create_batch"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_create_batch"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_create_batch"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_create_batch" +
                " (tried: literllm_default_client_create_batch, _literllm_default_client_create_batch). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_RETRIEVE_BATCH = LINKER.downcallHandle(
        LIB.find("literllm_default_client_retrieve_batch")
            .or(() -> LIB.find("_literllm_default_client_retrieve_batch"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_retrieve_batch"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_retrieve_batch"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_retrieve_batch" +
                " (tried: literllm_default_client_retrieve_batch, _literllm_default_client_retrieve_batch). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_LIST_BATCHES = LINKER.downcallHandle(
        LIB.find("literllm_default_client_list_batches")
            .or(() -> LIB.find("_literllm_default_client_list_batches"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_list_batches"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_list_batches"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_list_batches" +
                " (tried: literllm_default_client_list_batches, _literllm_default_client_list_batches). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CANCEL_BATCH = LINKER.downcallHandle(
        LIB.find("literllm_default_client_cancel_batch")
            .or(() -> LIB.find("_literllm_default_client_cancel_batch"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_cancel_batch"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_cancel_batch"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_cancel_batch" +
                " (tried: literllm_default_client_cancel_batch, _literllm_default_client_cancel_batch). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_FETCH_BATCH_FOR_POLLING = LINKER.downcallHandle(
        LIB.find("literllm_default_client_fetch_batch_for_polling")
            .or(() -> LIB.find("_literllm_default_client_fetch_batch_for_polling"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_fetch_batch_for_polling"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_fetch_batch_for_polling"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_fetch_batch_for_polling" +
                " (tried: literllm_default_client_fetch_batch_for_polling, _literllm_default_client_fetch_batch_for_polling). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_WAIT_FOR_BATCH = LINKER.downcallHandle(
        LIB.find("literllm_default_client_wait_for_batch")
            .or(() -> LIB.find("_literllm_default_client_wait_for_batch"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_wait_for_batch"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_wait_for_batch"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_wait_for_batch" +
                " (tried: literllm_default_client_wait_for_batch, _literllm_default_client_wait_for_batch). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CREATE_RESPONSE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_create_response")
            .or(() -> LIB.find("_literllm_default_client_create_response"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_create_response"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_create_response"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_create_response" +
                " (tried: literllm_default_client_create_response, _literllm_default_client_create_response). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_RETRIEVE_RESPONSE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_retrieve_response")
            .or(() -> LIB.find("_literllm_default_client_retrieve_response"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_retrieve_response"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_retrieve_response"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_retrieve_response" +
                " (tried: literllm_default_client_retrieve_response, _literllm_default_client_retrieve_response). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CANCEL_RESPONSE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_cancel_response")
            .or(() -> LIB.find("_literllm_default_client_cancel_response"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_cancel_response"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_cancel_response"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_cancel_response" +
                " (tried: literllm_default_client_cancel_response, _literllm_default_client_cancel_response). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LAST_ERROR_CODE = LINKER.downcallHandle(
        LIB.find("literllm_last_error_code").orElse(null),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );

    static final MethodHandle LITERLLM_LAST_ERROR_CONTEXT = LINKER.downcallHandle(
        LIB.find("literllm_last_error_context").orElse(null),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_ASSISTANT_MESSAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_assistant_message_from_json")
            .or(() -> LIB.find("_literllm_assistant_message_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_assistant_message_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_assistant_message_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_ASSISTANT_MESSAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_assistant_message_free")
            .or(() -> LIB.find("_literllm_assistant_message_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_assistant_message_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_assistant_message_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_AUDIO_CONTENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_audio_content_from_json")
            .or(() -> LIB.find("_literllm_audio_content_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_audio_content_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_audio_content_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_AUDIO_CONTENT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_audio_content_free")
            .or(() -> LIB.find("_literllm_audio_content_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_audio_content_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_audio_content_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_AUTH_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_auth_config_from_json")
            .or(() -> LIB.find("_literllm_auth_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_auth_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_auth_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_AUTH_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_auth_config_free")
            .or(() -> LIB.find("_literllm_auth_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_auth_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_auth_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BATCH_LIST_QUERY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_batch_list_query_from_json")
            .or(() -> LIB.find("_literllm_batch_list_query_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_batch_list_query_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_batch_list_query_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BATCH_LIST_QUERY_FREE = LINKER.downcallHandle(
        LIB.find("literllm_batch_list_query_free")
            .or(() -> LIB.find("_literllm_batch_list_query_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_batch_list_query_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_batch_list_query_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BATCH_LIST_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_batch_list_response_from_json")
            .or(() -> LIB.find("_literllm_batch_list_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_batch_list_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_batch_list_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BATCH_LIST_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_batch_list_response_free")
            .or(() -> LIB.find("_literllm_batch_list_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_batch_list_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_batch_list_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BATCH_OBJECT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_batch_object_from_json")
            .or(() -> LIB.find("_literllm_batch_object_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_batch_object_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_batch_object_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BATCH_OBJECT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_batch_object_free")
            .or(() -> LIB.find("_literllm_batch_object_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_batch_object_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_batch_object_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BATCH_REQUEST_COUNTS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_batch_request_counts_from_json")
            .or(() -> LIB.find("_literllm_batch_request_counts_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_batch_request_counts_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_batch_request_counts_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BATCH_REQUEST_COUNTS_FREE = LINKER.downcallHandle(
        LIB.find("literllm_batch_request_counts_free")
            .or(() -> LIB.find("_literllm_batch_request_counts_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_batch_request_counts_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_batch_request_counts_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BEDROCK_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_bedrock_config_from_json")
            .or(() -> LIB.find("_literllm_bedrock_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_bedrock_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_bedrock_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BEDROCK_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_bedrock_config_free")
            .or(() -> LIB.find("_literllm_bedrock_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_bedrock_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_bedrock_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BUDGET_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_budget_config_from_json")
            .or(() -> LIB.find("_literllm_budget_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_budget_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_budget_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_BUDGET_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_budget_config_free")
            .or(() -> LIB.find("_literllm_budget_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_budget_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_budget_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CACHE_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_cache_config_from_json")
            .or(() -> LIB.find("_literllm_cache_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_cache_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_cache_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CACHE_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_cache_config_free")
            .or(() -> LIB.find("_literllm_cache_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_cache_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_cache_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CATALOG_REFRESH_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_catalog_refresh_config_from_json")
            .or(() -> LIB.find("_literllm_catalog_refresh_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_catalog_refresh_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_catalog_refresh_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CATALOG_REFRESH_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_catalog_refresh_config_free")
            .or(() -> LIB.find("_literllm_catalog_refresh_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_catalog_refresh_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_catalog_refresh_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_CHUNK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_chat_completion_chunk_from_json")
            .or(() -> LIB.find("_literllm_chat_completion_chunk_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_chat_completion_chunk_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_chat_completion_chunk_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_CHUNK_FREE = LINKER.downcallHandle(
        LIB.find("literllm_chat_completion_chunk_free")
            .or(() -> LIB.find("_literllm_chat_completion_chunk_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_chat_completion_chunk_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_chat_completion_chunk_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_chat_completion_request_from_json")
            .or(() -> LIB.find("_literllm_chat_completion_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_chat_completion_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_chat_completion_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_chat_completion_request_free")
            .or(() -> LIB.find("_literllm_chat_completion_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_chat_completion_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_chat_completion_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_chat_completion_response_from_json")
            .or(() -> LIB.find("_literllm_chat_completion_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_chat_completion_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_chat_completion_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_chat_completion_response_free")
            .or(() -> LIB.find("_literllm_chat_completion_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_chat_completion_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_chat_completion_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_TOOL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_chat_completion_tool_from_json")
            .or(() -> LIB.find("_literllm_chat_completion_tool_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_chat_completion_tool_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_chat_completion_tool_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_TOOL_FREE = LINKER.downcallHandle(
        LIB.find("literllm_chat_completion_tool_free")
            .or(() -> LIB.find("_literllm_chat_completion_tool_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_chat_completion_tool_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_chat_completion_tool_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHOICE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_choice_from_json")
            .or(() -> LIB.find("_literllm_choice_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_choice_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_choice_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHOICE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_choice_free")
            .or(() -> LIB.find("_literllm_choice_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_choice_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_choice_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_BATCH_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_create_batch_request_from_json")
            .or(() -> LIB.find("_literllm_create_batch_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_batch_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_batch_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_BATCH_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_create_batch_request_free")
            .or(() -> LIB.find("_literllm_create_batch_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_batch_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_batch_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_FILE_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_create_file_request_from_json")
            .or(() -> LIB.find("_literllm_create_file_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_file_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_file_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_FILE_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_create_file_request_free")
            .or(() -> LIB.find("_literllm_create_file_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_file_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_file_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_IMAGE_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_create_image_request_from_json")
            .or(() -> LIB.find("_literllm_create_image_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_image_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_image_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_IMAGE_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_create_image_request_free")
            .or(() -> LIB.find("_literllm_create_image_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_image_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_image_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_RESPONSE_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_create_response_request_from_json")
            .or(() -> LIB.find("_literllm_create_response_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_response_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_response_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_RESPONSE_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_create_response_request_free")
            .or(() -> LIB.find("_literllm_create_response_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_response_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_response_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_SPEECH_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_create_speech_request_from_json")
            .or(() -> LIB.find("_literllm_create_speech_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_speech_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_speech_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_SPEECH_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_create_speech_request_free")
            .or(() -> LIB.find("_literllm_create_speech_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_speech_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_speech_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_TRANSCRIPTION_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_create_transcription_request_from_json")
            .or(() -> LIB.find("_literllm_create_transcription_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_transcription_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_transcription_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CREATE_TRANSCRIPTION_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_create_transcription_request_free")
            .or(() -> LIB.find("_literllm_create_transcription_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_create_transcription_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_create_transcription_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CUSTOM_PROVIDER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_custom_provider_config_from_json")
            .or(() -> LIB.find("_literllm_custom_provider_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_custom_provider_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_custom_provider_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CUSTOM_PROVIDER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_custom_provider_config_free")
            .or(() -> LIB.find("_literllm_custom_provider_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_custom_provider_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_custom_provider_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DECODED_DATA_URL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_decoded_data_url_from_json")
            .or(() -> LIB.find("_literllm_decoded_data_url_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_decoded_data_url_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_decoded_data_url_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DECODED_DATA_URL_FREE = LINKER.downcallHandle(
        LIB.find("literllm_decoded_data_url_free")
            .or(() -> LIB.find("_literllm_decoded_data_url_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_decoded_data_url_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_decoded_data_url_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_free")
            .or(() -> LIB.find("_literllm_default_client_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DELETE_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_delete_response_from_json")
            .or(() -> LIB.find("_literllm_delete_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_delete_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_delete_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DELETE_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_delete_response_free")
            .or(() -> LIB.find("_literllm_delete_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_delete_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_delete_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEVELOPER_MESSAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_developer_message_from_json")
            .or(() -> LIB.find("_literllm_developer_message_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_developer_message_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_developer_message_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEVELOPER_MESSAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_developer_message_free")
            .or(() -> LIB.find("_literllm_developer_message_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_developer_message_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_developer_message_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DOCUMENT_CONTENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_document_content_from_json")
            .or(() -> LIB.find("_literllm_document_content_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_document_content_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_document_content_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DOCUMENT_CONTENT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_document_content_free")
            .or(() -> LIB.find("_literllm_document_content_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_document_content_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_document_content_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_EMBEDDING_OBJECT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_embedding_object_from_json")
            .or(() -> LIB.find("_literllm_embedding_object_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_embedding_object_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_embedding_object_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_EMBEDDING_OBJECT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_embedding_object_free")
            .or(() -> LIB.find("_literllm_embedding_object_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_embedding_object_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_embedding_object_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_EMBEDDING_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_embedding_request_from_json")
            .or(() -> LIB.find("_literllm_embedding_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_embedding_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_embedding_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_EMBEDDING_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_embedding_request_free")
            .or(() -> LIB.find("_literllm_embedding_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_embedding_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_embedding_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_EMBEDDING_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_embedding_response_from_json")
            .or(() -> LIB.find("_literllm_embedding_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_embedding_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_embedding_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_EMBEDDING_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_embedding_response_free")
            .or(() -> LIB.find("_literllm_embedding_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_embedding_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_embedding_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FILE_LIST_QUERY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_file_list_query_from_json")
            .or(() -> LIB.find("_literllm_file_list_query_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_file_list_query_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_file_list_query_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FILE_LIST_QUERY_FREE = LINKER.downcallHandle(
        LIB.find("literllm_file_list_query_free")
            .or(() -> LIB.find("_literllm_file_list_query_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_file_list_query_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_file_list_query_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FILE_LIST_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_file_list_response_from_json")
            .or(() -> LIB.find("_literllm_file_list_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_file_list_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_file_list_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FILE_LIST_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_file_list_response_free")
            .or(() -> LIB.find("_literllm_file_list_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_file_list_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_file_list_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FILE_OBJECT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_file_object_from_json")
            .or(() -> LIB.find("_literllm_file_object_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_file_object_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_file_object_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FILE_OBJECT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_file_object_free")
            .or(() -> LIB.find("_literllm_file_object_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_file_object_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_file_object_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FUNCTION_CALL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_function_call_from_json")
            .or(() -> LIB.find("_literllm_function_call_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_function_call_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_function_call_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FUNCTION_CALL_FREE = LINKER.downcallHandle(
        LIB.find("literllm_function_call_free")
            .or(() -> LIB.find("_literllm_function_call_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_function_call_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_function_call_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FUNCTION_DEFINITION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_function_definition_from_json")
            .or(() -> LIB.find("_literllm_function_definition_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_function_definition_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_function_definition_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FUNCTION_DEFINITION_FREE = LINKER.downcallHandle(
        LIB.find("literllm_function_definition_free")
            .or(() -> LIB.find("_literllm_function_definition_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_function_definition_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_function_definition_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FUNCTION_MESSAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_function_message_from_json")
            .or(() -> LIB.find("_literllm_function_message_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_function_message_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_function_message_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_FUNCTION_MESSAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_function_message_free")
            .or(() -> LIB.find("_literllm_function_message_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_function_message_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_function_message_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_IMAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_image_from_json")
            .or(() -> LIB.find("_literllm_image_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_image_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_image_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_IMAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_image_free")
            .or(() -> LIB.find("_literllm_image_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_image_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_image_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_IMAGE_URL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_image_url_from_json")
            .or(() -> LIB.find("_literllm_image_url_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_image_url_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_image_url_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_IMAGE_URL_FREE = LINKER.downcallHandle(
        LIB.find("literllm_image_url_free")
            .or(() -> LIB.find("_literllm_image_url_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_image_url_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_image_url_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_IMAGES_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_images_response_from_json")
            .or(() -> LIB.find("_literllm_images_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_images_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_images_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_IMAGES_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_images_response_free")
            .or(() -> LIB.find("_literllm_images_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_images_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_images_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_IN_FLIGHT_LIMIT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_in_flight_limit_config_from_json")
            .or(() -> LIB.find("_literllm_in_flight_limit_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_in_flight_limit_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_in_flight_limit_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_IN_FLIGHT_LIMIT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_in_flight_limit_config_free")
            .or(() -> LIB.find("_literllm_in_flight_limit_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_in_flight_limit_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_in_flight_limit_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_INTENT_PROTOTYPE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_intent_prototype_free")
            .or(() -> LIB.find("_literllm_intent_prototype_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_intent_prototype_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_intent_prototype_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_JSON_SCHEMA_FORMAT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_json_schema_format_from_json")
            .or(() -> LIB.find("_literllm_json_schema_format_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_json_schema_format_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_json_schema_format_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_JSON_SCHEMA_FORMAT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_json_schema_format_free")
            .or(() -> LIB.find("_literllm_json_schema_format_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_json_schema_format_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_json_schema_format_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_BUDGET_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_llm_budget_config_from_json")
            .or(() -> LIB.find("_literllm_llm_budget_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_budget_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_budget_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_BUDGET_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_llm_budget_config_free")
            .or(() -> LIB.find("_literllm_llm_budget_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_budget_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_budget_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_CACHE_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_llm_cache_config_from_json")
            .or(() -> LIB.find("_literllm_llm_cache_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_cache_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_cache_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_CACHE_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_llm_cache_config_free")
            .or(() -> LIB.find("_literllm_llm_cache_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_cache_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_cache_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_llm_config_from_json")
            .or(() -> LIB.find("_literllm_llm_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_llm_config_free")
            .or(() -> LIB.find("_literllm_llm_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_IN_FLIGHT_LIMIT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_llm_in_flight_limit_config_from_json")
            .or(() -> LIB.find("_literllm_llm_in_flight_limit_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_in_flight_limit_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_in_flight_limit_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_IN_FLIGHT_LIMIT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_llm_in_flight_limit_config_free")
            .or(() -> LIB.find("_literllm_llm_in_flight_limit_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_in_flight_limit_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_in_flight_limit_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_PROVIDER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_llm_provider_config_from_json")
            .or(() -> LIB.find("_literllm_llm_provider_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_provider_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_provider_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_PROVIDER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_llm_provider_config_free")
            .or(() -> LIB.find("_literllm_llm_provider_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_provider_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_provider_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_RATE_LIMIT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_llm_rate_limit_config_from_json")
            .or(() -> LIB.find("_literllm_llm_rate_limit_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_rate_limit_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_rate_limit_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_LLM_RATE_LIMIT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_llm_rate_limit_config_free")
            .or(() -> LIB.find("_literllm_llm_rate_limit_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_llm_rate_limit_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_llm_rate_limit_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODEL_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_model_info_from_json")
            .or(() -> LIB.find("_literllm_model_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_model_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_model_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODEL_INFO_FREE = LINKER.downcallHandle(
        LIB.find("literllm_model_info_free")
            .or(() -> LIB.find("_literllm_model_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_model_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_model_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODEL_OBJECT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_model_object_from_json")
            .or(() -> LIB.find("_literllm_model_object_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_model_object_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_model_object_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODEL_OBJECT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_model_object_free")
            .or(() -> LIB.find("_literllm_model_object_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_model_object_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_model_object_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODEL_TIER_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_model_tier_from_json")
            .or(() -> LIB.find("_literllm_model_tier_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_model_tier_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_model_tier_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODEL_TIER_FREE = LINKER.downcallHandle(
        LIB.find("literllm_model_tier_free")
            .or(() -> LIB.find("_literllm_model_tier_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_model_tier_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_model_tier_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODELS_LIST_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_models_list_response_from_json")
            .or(() -> LIB.find("_literllm_models_list_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_models_list_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_models_list_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODELS_LIST_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_models_list_response_free")
            .or(() -> LIB.find("_literllm_models_list_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_models_list_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_models_list_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_CATEGORIES_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_moderation_categories_from_json")
            .or(() -> LIB.find("_literllm_moderation_categories_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_categories_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_categories_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_CATEGORIES_FREE = LINKER.downcallHandle(
        LIB.find("literllm_moderation_categories_free")
            .or(() -> LIB.find("_literllm_moderation_categories_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_categories_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_categories_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_CATEGORY_SCORES_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_moderation_category_scores_from_json")
            .or(() -> LIB.find("_literllm_moderation_category_scores_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_category_scores_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_category_scores_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_CATEGORY_SCORES_FREE = LINKER.downcallHandle(
        LIB.find("literllm_moderation_category_scores_free")
            .or(() -> LIB.find("_literllm_moderation_category_scores_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_category_scores_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_category_scores_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_moderation_request_from_json")
            .or(() -> LIB.find("_literllm_moderation_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_moderation_request_free")
            .or(() -> LIB.find("_literllm_moderation_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_moderation_response_from_json")
            .or(() -> LIB.find("_literllm_moderation_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_moderation_response_free")
            .or(() -> LIB.find("_literllm_moderation_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_moderation_result_from_json")
            .or(() -> LIB.find("_literllm_moderation_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_MODERATION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_moderation_result_free")
            .or(() -> LIB.find("_literllm_moderation_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_moderation_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_moderation_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_OCR_IMAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_ocr_image_from_json")
            .or(() -> LIB.find("_literllm_ocr_image_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ocr_image_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ocr_image_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_OCR_IMAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_ocr_image_free")
            .or(() -> LIB.find("_literllm_ocr_image_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ocr_image_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ocr_image_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_OCR_PAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_ocr_page_from_json")
            .or(() -> LIB.find("_literllm_ocr_page_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ocr_page_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ocr_page_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_OCR_PAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_ocr_page_free")
            .or(() -> LIB.find("_literllm_ocr_page_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ocr_page_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ocr_page_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_OCR_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_ocr_request_from_json")
            .or(() -> LIB.find("_literllm_ocr_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ocr_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ocr_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_OCR_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_ocr_request_free")
            .or(() -> LIB.find("_literllm_ocr_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ocr_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ocr_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_OCR_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_ocr_response_from_json")
            .or(() -> LIB.find("_literllm_ocr_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ocr_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ocr_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_OCR_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_ocr_response_free")
            .or(() -> LIB.find("_literllm_ocr_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_ocr_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_ocr_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PAGE_DIMENSIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_page_dimensions_from_json")
            .or(() -> LIB.find("_literllm_page_dimensions_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_page_dimensions_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_page_dimensions_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PAGE_DIMENSIONS_FREE = LINKER.downcallHandle(
        LIB.find("literllm_page_dimensions_free")
            .or(() -> LIB.find("_literllm_page_dimensions_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_page_dimensions_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_page_dimensions_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PROMPT_TOKENS_DETAILS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_prompt_tokens_details_from_json")
            .or(() -> LIB.find("_literllm_prompt_tokens_details_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_prompt_tokens_details_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_prompt_tokens_details_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PROMPT_TOKENS_DETAILS_FREE = LINKER.downcallHandle(
        LIB.find("literllm_prompt_tokens_details_free")
            .or(() -> LIB.find("_literllm_prompt_tokens_details_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_prompt_tokens_details_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_prompt_tokens_details_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PROVIDER_CAPABILITIES_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_provider_capabilities_from_json")
            .or(() -> LIB.find("_literllm_provider_capabilities_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_provider_capabilities_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_provider_capabilities_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PROVIDER_CAPABILITIES_FREE = LINKER.downcallHandle(
        LIB.find("literllm_provider_capabilities_free")
            .or(() -> LIB.find("_literllm_provider_capabilities_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_provider_capabilities_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_provider_capabilities_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PROVIDER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_provider_config_from_json")
            .or(() -> LIB.find("_literllm_provider_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_provider_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_provider_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PROVIDER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_provider_config_free")
            .or(() -> LIB.find("_literllm_provider_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_provider_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_provider_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RATE_LIMIT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_rate_limit_config_from_json")
            .or(() -> LIB.find("_literllm_rate_limit_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rate_limit_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rate_limit_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RATE_LIMIT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_rate_limit_config_free")
            .or(() -> LIB.find("_literllm_rate_limit_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rate_limit_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rate_limit_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RERANK_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_rerank_request_from_json")
            .or(() -> LIB.find("_literllm_rerank_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rerank_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rerank_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RERANK_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_rerank_request_free")
            .or(() -> LIB.find("_literllm_rerank_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rerank_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rerank_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RERANK_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_rerank_response_from_json")
            .or(() -> LIB.find("_literllm_rerank_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rerank_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rerank_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RERANK_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_rerank_response_free")
            .or(() -> LIB.find("_literllm_rerank_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rerank_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rerank_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RERANK_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_rerank_result_from_json")
            .or(() -> LIB.find("_literllm_rerank_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rerank_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rerank_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RERANK_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_rerank_result_free")
            .or(() -> LIB.find("_literllm_rerank_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rerank_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rerank_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RERANK_RESULT_DOCUMENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_rerank_result_document_from_json")
            .or(() -> LIB.find("_literllm_rerank_result_document_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rerank_result_document_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rerank_result_document_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RERANK_RESULT_DOCUMENT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_rerank_result_document_free")
            .or(() -> LIB.find("_literllm_rerank_result_document_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_rerank_result_document_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_rerank_result_document_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RESPONSE_OBJECT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_response_object_from_json")
            .or(() -> LIB.find("_literllm_response_object_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_response_object_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_response_object_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RESPONSE_OBJECT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_response_object_free")
            .or(() -> LIB.find("_literllm_response_object_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_response_object_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_response_object_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RESPONSE_OUTPUT_ITEM_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_response_output_item_from_json")
            .or(() -> LIB.find("_literllm_response_output_item_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_response_output_item_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_response_output_item_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RESPONSE_OUTPUT_ITEM_FREE = LINKER.downcallHandle(
        LIB.find("literllm_response_output_item_free")
            .or(() -> LIB.find("_literllm_response_output_item_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_response_output_item_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_response_output_item_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RESPONSE_TOOL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_response_tool_from_json")
            .or(() -> LIB.find("_literllm_response_tool_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_response_tool_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_response_tool_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RESPONSE_TOOL_FREE = LINKER.downcallHandle(
        LIB.find("literllm_response_tool_free")
            .or(() -> LIB.find("_literllm_response_tool_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_response_tool_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_response_tool_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RESPONSE_USAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_response_usage_from_json")
            .or(() -> LIB.find("_literllm_response_usage_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_response_usage_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_response_usage_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_RESPONSE_USAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_response_usage_free")
            .or(() -> LIB.find("_literllm_response_usage_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_response_usage_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_response_usage_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SEARCH_REQUEST_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_search_request_from_json")
            .or(() -> LIB.find("_literllm_search_request_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_search_request_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_search_request_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SEARCH_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("literllm_search_request_free")
            .or(() -> LIB.find("_literllm_search_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_search_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_search_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SEARCH_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_search_response_from_json")
            .or(() -> LIB.find("_literllm_search_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_search_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_search_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SEARCH_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_search_response_free")
            .or(() -> LIB.find("_literllm_search_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_search_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_search_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SEARCH_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_search_result_from_json")
            .or(() -> LIB.find("_literllm_search_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_search_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_search_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SEARCH_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_search_result_free")
            .or(() -> LIB.find("_literllm_search_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_search_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_search_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SINGLEFLIGHT_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_singleflight_result_free")
            .or(() -> LIB.find("_literllm_singleflight_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_singleflight_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_singleflight_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SPECIFIC_FUNCTION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_specific_function_from_json")
            .or(() -> LIB.find("_literllm_specific_function_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_specific_function_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_specific_function_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SPECIFIC_FUNCTION_FREE = LINKER.downcallHandle(
        LIB.find("literllm_specific_function_free")
            .or(() -> LIB.find("_literllm_specific_function_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_specific_function_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_specific_function_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SPECIFIC_TOOL_CHOICE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_specific_tool_choice_from_json")
            .or(() -> LIB.find("_literllm_specific_tool_choice_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_specific_tool_choice_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_specific_tool_choice_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SPECIFIC_TOOL_CHOICE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_specific_tool_choice_free")
            .or(() -> LIB.find("_literllm_specific_tool_choice_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_specific_tool_choice_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_specific_tool_choice_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_CHOICE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_stream_choice_from_json")
            .or(() -> LIB.find("_literllm_stream_choice_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_choice_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_choice_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_CHOICE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_stream_choice_free")
            .or(() -> LIB.find("_literllm_stream_choice_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_choice_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_choice_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_DELTA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_stream_delta_from_json")
            .or(() -> LIB.find("_literllm_stream_delta_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_delta_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_delta_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_DELTA_FREE = LINKER.downcallHandle(
        LIB.find("literllm_stream_delta_free")
            .or(() -> LIB.find("_literllm_stream_delta_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_delta_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_delta_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_FUNCTION_CALL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_stream_function_call_from_json")
            .or(() -> LIB.find("_literllm_stream_function_call_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_function_call_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_function_call_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_FUNCTION_CALL_FREE = LINKER.downcallHandle(
        LIB.find("literllm_stream_function_call_free")
            .or(() -> LIB.find("_literllm_stream_function_call_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_function_call_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_function_call_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_stream_options_from_json")
            .or(() -> LIB.find("_literllm_stream_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("literllm_stream_options_free")
            .or(() -> LIB.find("_literllm_stream_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_TOOL_CALL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_stream_tool_call_from_json")
            .or(() -> LIB.find("_literllm_stream_tool_call_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_tool_call_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_tool_call_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_STREAM_TOOL_CALL_FREE = LINKER.downcallHandle(
        LIB.find("literllm_stream_tool_call_free")
            .or(() -> LIB.find("_literllm_stream_tool_call_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_stream_tool_call_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_stream_tool_call_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SYSTEM_MESSAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_system_message_from_json")
            .or(() -> LIB.find("_literllm_system_message_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_system_message_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_system_message_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_SYSTEM_MESSAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_system_message_free")
            .or(() -> LIB.find("_literllm_system_message_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_system_message_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_system_message_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_TOOL_CALL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_tool_call_from_json")
            .or(() -> LIB.find("_literllm_tool_call_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_tool_call_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_tool_call_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_TOOL_CALL_FREE = LINKER.downcallHandle(
        LIB.find("literllm_tool_call_free")
            .or(() -> LIB.find("_literllm_tool_call_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_tool_call_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_tool_call_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_TOOL_MESSAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_tool_message_from_json")
            .or(() -> LIB.find("_literllm_tool_message_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_tool_message_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_tool_message_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_TOOL_MESSAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_tool_message_free")
            .or(() -> LIB.find("_literllm_tool_message_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_tool_message_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_tool_message_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_TRANSCRIPTION_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_transcription_response_from_json")
            .or(() -> LIB.find("_literllm_transcription_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_transcription_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_transcription_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_TRANSCRIPTION_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_transcription_response_free")
            .or(() -> LIB.find("_literllm_transcription_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_transcription_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_transcription_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_TRANSCRIPTION_SEGMENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_transcription_segment_from_json")
            .or(() -> LIB.find("_literllm_transcription_segment_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_transcription_segment_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_transcription_segment_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_TRANSCRIPTION_SEGMENT_FREE = LINKER.downcallHandle(
        LIB.find("literllm_transcription_segment_free")
            .or(() -> LIB.find("_literllm_transcription_segment_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_transcription_segment_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_transcription_segment_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_USAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_usage_from_json")
            .or(() -> LIB.find("_literllm_usage_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_usage_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_usage_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_USAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_usage_free")
            .or(() -> LIB.find("_literllm_usage_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_usage_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_usage_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_USER_MESSAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_user_message_from_json")
            .or(() -> LIB.find("_literllm_user_message_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_user_message_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_user_message_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_USER_MESSAGE_FREE = LINKER.downcallHandle(
        LIB.find("literllm_user_message_free")
            .or(() -> LIB.find("_literllm_user_message_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_user_message_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_user_message_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_WAIT_FOR_BATCH_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("literllm_wait_for_batch_config_from_json")
            .or(() -> LIB.find("_literllm_wait_for_batch_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_wait_for_batch_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_wait_for_batch_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_WAIT_FOR_BATCH_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("literllm_wait_for_batch_config_free")
            .or(() -> LIB.find("_literllm_wait_for_batch_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_wait_for_batch_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_wait_for_batch_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_PROVIDER_CAPABILITIES_TO_JSON = LIB.find("literllm_provider_capabilities_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_DECODED_DATA_URL_TO_JSON = LIB.find("literllm_decoded_data_url_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_MODEL_INFO_TO_JSON = LIB.find("literllm_model_info_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_REFRESH_OUTCOME_TO_JSON = LIB.find("literllm_refresh_outcome_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_REFRESH_OUTCOME_FREE = LINKER.downcallHandle(
        LIB.find("literllm_refresh_outcome_free")
            .or(() -> LIB.find("_literllm_refresh_outcome_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_refresh_outcome_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_refresh_outcome_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CHAT_STREAM_START = LINKER.downcallHandle(
        LIB.find("literllm_default_client_chat_stream_start")
            .or(() -> LIB.find("_literllm_default_client_chat_stream_start"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_chat_stream_start"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_chat_stream_start"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_chat_stream_start" +
                " (tried: literllm_default_client_chat_stream_start, _literllm_default_client_chat_stream_start). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CHAT_STREAM_NEXT = LINKER.downcallHandle(
        LIB.find("literllm_default_client_chat_stream_next")
            .or(() -> LIB.find("_literllm_default_client_chat_stream_next"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_chat_stream_next"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_chat_stream_next"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "literllm_default_client_chat_stream_next" +
                " (tried: literllm_default_client_chat_stream_next, _literllm_default_client_chat_stream_next). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_DEFAULT_CLIENT_CHAT_STREAM_FREE = LINKER.downcallHandle(
        LIB.find("literllm_default_client_chat_stream_free")
            .or(() -> LIB.find("_literllm_default_client_chat_stream_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("literllm_default_client_chat_stream_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_literllm_default_client_chat_stream_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle LITERLLM_CHAT_COMPLETION_CHUNK_TO_JSON = LIB.find("literllm_chat_completion_chunk_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_CHAT_COMPLETION_RESPONSE_TO_JSON = LIB.find("literllm_chat_completion_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_EMBEDDING_RESPONSE_TO_JSON = LIB.find("literllm_embedding_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_MODELS_LIST_RESPONSE_TO_JSON = LIB.find("literllm_models_list_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_IMAGES_RESPONSE_TO_JSON = LIB.find("literllm_images_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_TRANSCRIPTION_RESPONSE_TO_JSON = LIB.find("literllm_transcription_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_MODERATION_RESPONSE_TO_JSON = LIB.find("literllm_moderation_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_RERANK_RESPONSE_TO_JSON = LIB.find("literllm_rerank_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_SEARCH_RESPONSE_TO_JSON = LIB.find("literllm_search_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_OCR_RESPONSE_TO_JSON = LIB.find("literllm_ocr_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_FILE_OBJECT_TO_JSON = LIB.find("literllm_file_object_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_DELETE_RESPONSE_TO_JSON = LIB.find("literllm_delete_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_FILE_LIST_RESPONSE_TO_JSON = LIB.find("literllm_file_list_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_BATCH_OBJECT_TO_JSON = LIB.find("literllm_batch_object_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_BATCH_LIST_RESPONSE_TO_JSON = LIB.find("literllm_batch_list_response_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle LITERLLM_RESPONSE_OBJECT_TO_JSON = LIB.find("literllm_response_object_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


}
