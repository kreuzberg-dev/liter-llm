// Generated by alef. Do not edit by hand.
// alef:hash:cdd20dc09918b66151519c26339cbf1a5fd183a2e0ef22423c9767a15d2213ab
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg.literllm.android

/**
 * Canonical configuration for an LLM client.
 *
 * All fields except `model` are optional so that partially-specified
 * configs (e.g. from environment-driven defaults) round-trip cleanly.
 * Convert to a runtime client configuration via
 * `LlmConfig.into_client_builder`.
 *
 * `temperature` and `max_tokens` are request-time parameters rather than
 * client-level settings; they are carried on this struct for callers to
 * read when building individual requests, and are intentionally **not**
 * mapped by `LlmConfig.into_client_builder`.
 *
 * Implements `Debug` manually (see below) so `api_key` and header values are
 * redacted rather than printed in full.
 */
data class LlmConfig(
    /** Model identifier (e.g. `"gpt-4o"`, `"bedrock/anthropic.claude-3-sonnet-20240229-v1:0"`). */
    val model: String = "",
    /** API key for authentication. */
    val apiKey: String? = null,
    /**
     * Override base URL. When set, all requests go here and provider
     * auto-detection is skipped.
     */
    val baseUrl: String? = null,
    /** Request timeout, in seconds. */
    val timeoutSecs: Long? = null,
    /** Maximum number of retries on 429 / 5xx responses. */
    val maxRetries: Int? = null,
    /** Sampling temperature for requests built from this config. */
    val temperature: Double? = null,
    /** Maximum number of tokens to generate for requests built from this config. */
    val maxTokens: Long? = null,
    /**
     * Automatically load the API key from the provider's environment variable
     * when no explicit key is provided (default: `true`).
     */
    val loadEnv: Boolean? = null,
    /** Extra headers sent on every request. */
    val headers: Map<String, String>? = null,
    /** Custom provider configurations, in addition to the built-in providers. */
    val providers: List<LlmProviderConfig>? = null,
    /** Response cache configuration. */
    val cache: LlmCacheConfig? = null,
    /** Budget enforcement configuration. */
    val budget: LlmBudgetConfig? = null,
    /** Per-model rate limiting configuration. */
    val rateLimit: LlmRateLimitConfig? = null,
    /** Global per-client in-flight provider request limit. */
    val inFlightLimit: LlmInFlightLimitConfig? = null,
    /** Enable per-request cost tracking. */
    val costTracking: Boolean? = null,
    /** Enable OpenTelemetry-compatible tracing spans. */
    val tracing: Boolean? = null,
    /** Cooldown duration after transient errors, in seconds. */
    val cooldownSecs: Long? = null,
    /** Background health check interval, in seconds. */
    val healthCheckSecs: Long? = null,
    /** AWS Bedrock configuration (region, credentials, cross-region routing). */
    val bedrock: BedrockConfig? = null,
) {

    /** Get the custom provider configurations from this config. */
    fun providers(): List<LlmProviderConfig> {
        val selfJson = VALUE_METHOD_MAPPER.writeValueAsString(this)
        val resultJson = LiterLlmBridge.nativeLlmConfigProviders(selfJson)
        return VALUE_METHOD_MAPPER.readValue(
            resultJson,
            object : com.fasterxml.jackson.core.type.TypeReference<List<LlmProviderConfig>>() {},
        )
    }
}

// Marshals the receiver and the parameter object across the value-method JNI
// boundary. The naming strategy, null handling, and unknown-property tolerance
// must match the module facade's mapper — both feed the same Rust serde types.
private val VALUE_METHOD_MAPPER = com.fasterxml.jackson.module.kotlin.jacksonObjectMapper()
    .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
    .registerModule(
        com.fasterxml.jackson.module.kotlin.KotlinModule.Builder()
            .configure(com.fasterxml.jackson.module.kotlin.KotlinFeature.NullIsSameAsDefault, true)
            .configure(com.fasterxml.jackson.module.kotlin.KotlinFeature.NullToEmptyCollection, true)
            .configure(com.fasterxml.jackson.module.kotlin.KotlinFeature.NullToEmptyMap, true)
            .build(),
    )
    .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    .setDefaultPropertyInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
    .configure(com.fasterxml.jackson.databind.DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false)
