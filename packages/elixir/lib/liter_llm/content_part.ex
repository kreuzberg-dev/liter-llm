# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f804165312b558294439d69c26a62e268b320001b21e9d674ab765a184735ce6
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule LiterLlm.ContentPart do
  @moduledoc "A single content part in a user message — text, image, document, or audio."

  @typedoc "A single content part in a user message — text, image, document, or audio."
  @type t :: term()

  @typedoc "Plain text."
  @type text :: %{type: :text, text: String.t()}
  @typedoc "Image identified by URL (with optional detail level)."
  @type image_url :: %{type: :image_url, image_url: LiterLlm.ImageUrl.t()}
  @typedoc "Document file (PDF, CSV, etc.) as base64 or URL."
  @type document :: %{type: :document, document: LiterLlm.DocumentContent.t()}
  @typedoc "Audio input as base64."
  @type input_audio :: %{type: :input_audio, input_audio: LiterLlm.AudioContent.t()}

  def text(text), do: {:text, %{text: text}}
  def image_url(image_url), do: {:image_url, %{image_url: image_url}}
  def document(document), do: {:document, %{document: document}}
  def input_audio(input_audio), do: {:input_audio, %{input_audio: input_audio}}

  @doc "Returns the serde wire value this variant carries on the JSON boundary (see `wire_variant_value` in alef's naming module) -- distinct from the atom's Elixir spelling."
  @spec wire_value(t()) :: String.t()
  def wire_value(:text), do: "text"
  def wire_value(:image_url), do: "image_url"
  def wire_value(:document), do: "document"
  def wire_value(:input_audio), do: "input_audio"
  def wire_value(value) when is_tuple(value), do: wire_value(elem(value, 0))
end
