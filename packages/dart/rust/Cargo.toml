# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:cb23fdaf4c9ae450ad05b11cbb915d185d81d1a87540126838c8c4798a2a78ac
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "liter-llm-dart"
version.workspace = true
edition = "2024"
license = "MIT"

[package.metadata.cargo-machete]
# Umbrella + sibling crates are pulled in so flutter_rust_bridge can resolve
# every referenced type, but the generated Rust wrapper only `use`s a subset.
ignored = ["liter_llm"]

[lib]
crate-type = ["cdylib", "staticlib"]
[features]
default = ["native-http", "opendal-cache", "tokenizer", "tower", "wasm-http"]
native-http = ["liter_llm/native-http"]
opendal-cache = ["liter_llm/opendal-cache"]
tokenizer = ["liter_llm/tokenizer"]
tower = ["liter_llm/tower"]
wasm-http = ["liter_llm/wasm-http"]

[dependencies]
flutter_rust_bridge = "=2.13.0"
futures-util = "0.3"
liter_llm = { path = "../../../crates/liter-llm", package = "liter-llm", features = ["native-http", "full"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "sync"] }

[lints.rust]
# flutter_rust_bridge uses #[cfg(frb_expand)] internally during macro expansion.
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }

[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
