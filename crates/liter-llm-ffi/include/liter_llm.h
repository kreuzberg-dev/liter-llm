#ifndef LITER_LLM_H
#define LITER_LLM_H

#pragma once

/* This file is auto-generated by skif. DO NOT EDIT. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>

/**
 * Return the last error code (0 means no error).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_last_error_code(void);

/**
 * Return the last error message. The pointer is valid until the next FFI call on this thread.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *liter_llm_last_error_context(void);

/**
 * Free a string previously returned by this library.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_free_string(char *ptr);

/**
 * Return the library version string. The pointer is static and must NOT be freed.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *liter_llm_version(void);

/**
 * Create a `CreateSpeechRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_create_speech_request_free`.
 */
LITER_LLMCreateSpeechRequest *liter_llm_create_speech_request_from_json(const char *json);

/**
 * Free a `CreateSpeechRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_create_speech_request_free(LITER_LLMCreateSpeechRequest *ptr);

/**
 * Get the `model` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_speech_request_model(const LITER_LLMCreateSpeechRequest *ptr);

/**
 * Get the `input` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_speech_request_input(const LITER_LLMCreateSpeechRequest *ptr);

/**
 * Get the `voice` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_speech_request_voice(const LITER_LLMCreateSpeechRequest *ptr);

/**
 * Get the `response_format` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_speech_request_response_format(const LITER_LLMCreateSpeechRequest *ptr);

/**
 * Get the `speed` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_create_speech_request_speed(const LITER_LLMCreateSpeechRequest *ptr);

/**
 * Create a `TranscriptionResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_transcription_response_free`.
 */
LITER_LLMTranscriptionResponse *liter_llm_transcription_response_from_json(const char *json);

/**
 * Free a `TranscriptionResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_transcription_response_free(LITER_LLMTranscriptionResponse *ptr);

/**
 * Get the `text` field from a `TranscriptionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_transcription_response_text(const LITER_LLMTranscriptionResponse *ptr);

/**
 * Get the `language` field from a `TranscriptionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_transcription_response_language(const LITER_LLMTranscriptionResponse *ptr);

/**
 * Get the `duration` field from a `TranscriptionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_transcription_response_duration(const LITER_LLMTranscriptionResponse *ptr);

/**
 * Get the `segments` field from a `TranscriptionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_transcription_response_segments(const LITER_LLMTranscriptionResponse *ptr);

/**
 * Create a `TranscriptionSegment` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_transcription_segment_free`.
 */
LITER_LLMTranscriptionSegment *liter_llm_transcription_segment_from_json(const char *json);

/**
 * Free a `TranscriptionSegment` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_transcription_segment_free(LITER_LLMTranscriptionSegment *ptr);

/**
 * Get the `id` field from a `TranscriptionSegment`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_transcription_segment_id(const LITER_LLMTranscriptionSegment *ptr);

/**
 * Get the `start` field from a `TranscriptionSegment`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_transcription_segment_start(const LITER_LLMTranscriptionSegment *ptr);

/**
 * Get the `end` field from a `TranscriptionSegment`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_transcription_segment_end(const LITER_LLMTranscriptionSegment *ptr);

/**
 * Get the `text` field from a `TranscriptionSegment`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_transcription_segment_text(const LITER_LLMTranscriptionSegment *ptr);

/**
 * Create a `ChatCompletionRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_chat_completion_request_free`.
 */
LITER_LLMChatCompletionRequest *liter_llm_chat_completion_request_from_json(const char *json);

/**
 * Free a `ChatCompletionRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_chat_completion_request_free(LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `model` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_request_model(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `messages` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_request_messages(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `temperature` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_chat_completion_request_temperature(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `top_p` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_chat_completion_request_top_p(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `n` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_chat_completion_request_n(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `stop` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMStopSequence *liter_llm_chat_completion_request_stop(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `max_tokens` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t liter_llm_chat_completion_request_max_tokens(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `presence_penalty` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_chat_completion_request_presence_penalty(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `frequency_penalty` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_chat_completion_request_frequency_penalty(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `logit_bias` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_request_logit_bias(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `user` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_request_user(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `tools` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_request_tools(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `tool_choice` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMToolChoice *liter_llm_chat_completion_request_tool_choice(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `parallel_tool_calls` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_chat_completion_request_parallel_tool_calls(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `response_format` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMResponseFormat *liter_llm_chat_completion_request_response_format(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `seed` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int64_t liter_llm_chat_completion_request_seed(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `reasoning_effort` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMReasoningEffort *liter_llm_chat_completion_request_reasoning_effort(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Get the `extra_body` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_request_extra_body(const LITER_LLMChatCompletionRequest *ptr);

/**
 * Create a `ChatCompletionResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_chat_completion_response_free`.
 */
LITER_LLMChatCompletionResponse *liter_llm_chat_completion_response_from_json(const char *json);

/**
 * Free a `ChatCompletionResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_chat_completion_response_free(LITER_LLMChatCompletionResponse *ptr);

/**
 * Get the `id` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_response_id(const LITER_LLMChatCompletionResponse *ptr);

/**
 * Get the `object` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_response_object(const LITER_LLMChatCompletionResponse *ptr);

/**
 * Get the `created` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t liter_llm_chat_completion_response_created(const LITER_LLMChatCompletionResponse *ptr);

/**
 * Get the `model` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_response_model(const LITER_LLMChatCompletionResponse *ptr);

/**
 * Get the `choices` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_response_choices(const LITER_LLMChatCompletionResponse *ptr);

/**
 * Get the `usage` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMUsage *liter_llm_chat_completion_response_usage(const LITER_LLMChatCompletionResponse *ptr);

/**
 * Get the `system_fingerprint` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_response_system_fingerprint(const LITER_LLMChatCompletionResponse *ptr);

/**
 * Get the `service_tier` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_response_service_tier(const LITER_LLMChatCompletionResponse *ptr);

/**
 * Estimate the cost of this response based on embedded pricing data.
 *
 * Returns `None` if:
 * - the `model` field is not present in the embedded pricing registry, or
 * - the `usage` field is absent from the response.
 *
 * # Example
 *
 * ```rust,ignore
 * let cost = response.estimated_cost();
 * if let Some(usd) = cost {
 *     println!("Request cost: ${usd:.6}");
 * }
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
double liter_llm_chat_completion_response_estimated_cost(const LITER_LLMChatCompletionResponse *this_);

/**
 * Create a `Choice` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_choice_free`.
 */
LITER_LLMChoice *liter_llm_choice_from_json(const char *json);

/**
 * Free a `Choice` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_choice_free(LITER_LLMChoice *ptr);

/**
 * Get the `index` field from a `Choice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_choice_index(const LITER_LLMChoice *ptr);

/**
 * Get the `message` field from a `Choice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMAssistantMessage *liter_llm_choice_message(const LITER_LLMChoice *ptr);

/**
 * Get the `finish_reason` field from a `Choice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMFinishReason *liter_llm_choice_finish_reason(const LITER_LLMChoice *ptr);

/**
 * Create a `ChatCompletionChunk` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_chat_completion_chunk_free`.
 */
LITER_LLMChatCompletionChunk *liter_llm_chat_completion_chunk_from_json(const char *json);

/**
 * Free a `ChatCompletionChunk` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_chat_completion_chunk_free(LITER_LLMChatCompletionChunk *ptr);

/**
 * Get the `id` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_chunk_id(const LITER_LLMChatCompletionChunk *ptr);

/**
 * Get the `object` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_chunk_object(const LITER_LLMChatCompletionChunk *ptr);

/**
 * Get the `created` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t liter_llm_chat_completion_chunk_created(const LITER_LLMChatCompletionChunk *ptr);

/**
 * Get the `model` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_chunk_model(const LITER_LLMChatCompletionChunk *ptr);

/**
 * Get the `choices` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_chunk_choices(const LITER_LLMChatCompletionChunk *ptr);

/**
 * Get the `usage` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMUsage *liter_llm_chat_completion_chunk_usage(const LITER_LLMChatCompletionChunk *ptr);

/**
 * Get the `system_fingerprint` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_chunk_system_fingerprint(const LITER_LLMChatCompletionChunk *ptr);

/**
 * Get the `service_tier` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_chat_completion_chunk_service_tier(const LITER_LLMChatCompletionChunk *ptr);

/**
 * Create a `StreamChoice` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_stream_choice_free`.
 */
LITER_LLMStreamChoice *liter_llm_stream_choice_from_json(const char *json);

/**
 * Free a `StreamChoice` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_stream_choice_free(LITER_LLMStreamChoice *ptr);

/**
 * Get the `index` field from a `StreamChoice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_stream_choice_index(const LITER_LLMStreamChoice *ptr);

/**
 * Get the `delta` field from a `StreamChoice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMStreamDelta *liter_llm_stream_choice_delta(const LITER_LLMStreamChoice *ptr);

/**
 * Get the `finish_reason` field from a `StreamChoice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMFinishReason *liter_llm_stream_choice_finish_reason(const LITER_LLMStreamChoice *ptr);

/**
 * Create a `StreamDelta` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_stream_delta_free`.
 */
LITER_LLMStreamDelta *liter_llm_stream_delta_from_json(const char *json);

/**
 * Free a `StreamDelta` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_stream_delta_free(LITER_LLMStreamDelta *ptr);

/**
 * Get the `role` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_stream_delta_role(const LITER_LLMStreamDelta *ptr);

/**
 * Get the `content` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_stream_delta_content(const LITER_LLMStreamDelta *ptr);

/**
 * Get the `tool_calls` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_stream_delta_tool_calls(const LITER_LLMStreamDelta *ptr);

/**
 * Get the `function_call` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMStreamFunctionCall *liter_llm_stream_delta_function_call(const LITER_LLMStreamDelta *ptr);

/**
 * Get the `refusal` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_stream_delta_refusal(const LITER_LLMStreamDelta *ptr);

/**
 * Create a `StreamToolCall` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_stream_tool_call_free`.
 */
LITER_LLMStreamToolCall *liter_llm_stream_tool_call_from_json(const char *json);

/**
 * Free a `StreamToolCall` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_stream_tool_call_free(LITER_LLMStreamToolCall *ptr);

/**
 * Get the `index` field from a `StreamToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_stream_tool_call_index(const LITER_LLMStreamToolCall *ptr);

/**
 * Get the `id` field from a `StreamToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_stream_tool_call_id(const LITER_LLMStreamToolCall *ptr);

/**
 * Get the `call_type` field from a `StreamToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMToolType *liter_llm_stream_tool_call_call_type(const LITER_LLMStreamToolCall *ptr);

/**
 * Get the `function` field from a `StreamToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMStreamFunctionCall *liter_llm_stream_tool_call_function(const LITER_LLMStreamToolCall *ptr);

/**
 * Create a `StreamFunctionCall` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_stream_function_call_free`.
 */
LITER_LLMStreamFunctionCall *liter_llm_stream_function_call_from_json(const char *json);

/**
 * Free a `StreamFunctionCall` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_stream_function_call_free(LITER_LLMStreamFunctionCall *ptr);

/**
 * Get the `name` field from a `StreamFunctionCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_stream_function_call_name(const LITER_LLMStreamFunctionCall *ptr);

/**
 * Get the `arguments` field from a `StreamFunctionCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_stream_function_call_arguments(const LITER_LLMStreamFunctionCall *ptr);

/**
 * Create a `AssistantMessage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_assistant_message_free`.
 */
LITER_LLMAssistantMessage *liter_llm_assistant_message_from_json(const char *json);

/**
 * Free a `AssistantMessage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_assistant_message_free(LITER_LLMAssistantMessage *ptr);

/**
 * Get the `content` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_assistant_message_content(const LITER_LLMAssistantMessage *ptr);

/**
 * Get the `name` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_assistant_message_name(const LITER_LLMAssistantMessage *ptr);

/**
 * Get the `tool_calls` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_assistant_message_tool_calls(const LITER_LLMAssistantMessage *ptr);

/**
 * Get the `refusal` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_assistant_message_refusal(const LITER_LLMAssistantMessage *ptr);

/**
 * Get the `function_call` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMFunctionCall *liter_llm_assistant_message_function_call(const LITER_LLMAssistantMessage *ptr);

/**
 * Create a `ChatCompletionTool` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_chat_completion_tool_free`.
 */
LITER_LLMChatCompletionTool *liter_llm_chat_completion_tool_from_json(const char *json);

/**
 * Free a `ChatCompletionTool` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_chat_completion_tool_free(LITER_LLMChatCompletionTool *ptr);

/**
 * Get the `tool_type` field from a `ChatCompletionTool`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMToolType *liter_llm_chat_completion_tool_tool_type(const LITER_LLMChatCompletionTool *ptr);

/**
 * Get the `function` field from a `ChatCompletionTool`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMFunctionDefinition *liter_llm_chat_completion_tool_function(const LITER_LLMChatCompletionTool *ptr);

/**
 * Create a `FunctionDefinition` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_function_definition_free`.
 */
LITER_LLMFunctionDefinition *liter_llm_function_definition_from_json(const char *json);

/**
 * Free a `FunctionDefinition` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_function_definition_free(LITER_LLMFunctionDefinition *ptr);

/**
 * Get the `name` field from a `FunctionDefinition`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_function_definition_name(const LITER_LLMFunctionDefinition *ptr);

/**
 * Get the `description` field from a `FunctionDefinition`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_function_definition_description(const LITER_LLMFunctionDefinition *ptr);

/**
 * Get the `parameters` field from a `FunctionDefinition`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_function_definition_parameters(const LITER_LLMFunctionDefinition *ptr);

/**
 * Get the `strict` field from a `FunctionDefinition`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_function_definition_strict(const LITER_LLMFunctionDefinition *ptr);

/**
 * Create a `ToolCall` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_tool_call_free`.
 */
LITER_LLMToolCall *liter_llm_tool_call_from_json(const char *json);

/**
 * Free a `ToolCall` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_tool_call_free(LITER_LLMToolCall *ptr);

/**
 * Get the `id` field from a `ToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_tool_call_id(const LITER_LLMToolCall *ptr);

/**
 * Get the `call_type` field from a `ToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMToolType *liter_llm_tool_call_call_type(const LITER_LLMToolCall *ptr);

/**
 * Get the `function` field from a `ToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMFunctionCall *liter_llm_tool_call_function(const LITER_LLMToolCall *ptr);

/**
 * Create a `FunctionCall` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_function_call_free`.
 */
LITER_LLMFunctionCall *liter_llm_function_call_from_json(const char *json);

/**
 * Free a `FunctionCall` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_function_call_free(LITER_LLMFunctionCall *ptr);

/**
 * Get the `name` field from a `FunctionCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_function_call_name(const LITER_LLMFunctionCall *ptr);

/**
 * Get the `arguments` field from a `FunctionCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_function_call_arguments(const LITER_LLMFunctionCall *ptr);

/**
 * Create a `Usage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_usage_free`.
 */
LITER_LLMUsage *liter_llm_usage_from_json(const char *json);

/**
 * Free a `Usage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_usage_free(LITER_LLMUsage *ptr);

/**
 * Get the `prompt_tokens` field from a `Usage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t liter_llm_usage_prompt_tokens(const LITER_LLMUsage *ptr);

/**
 * Get the `completion_tokens` field from a `Usage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t liter_llm_usage_completion_tokens(const LITER_LLMUsage *ptr);

/**
 * Get the `total_tokens` field from a `Usage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t liter_llm_usage_total_tokens(const LITER_LLMUsage *ptr);

/**
 * Create a `EmbeddingRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_embedding_request_free`.
 */
LITER_LLMEmbeddingRequest *liter_llm_embedding_request_from_json(const char *json);

/**
 * Free a `EmbeddingRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_embedding_request_free(LITER_LLMEmbeddingRequest *ptr);

/**
 * Get the `model` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_embedding_request_model(const LITER_LLMEmbeddingRequest *ptr);

/**
 * Get the `input` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMEmbeddingInput *liter_llm_embedding_request_input(const LITER_LLMEmbeddingRequest *ptr);

/**
 * Get the `encoding_format` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMEmbeddingFormat *liter_llm_embedding_request_encoding_format(const LITER_LLMEmbeddingRequest *ptr);

/**
 * Get the `dimensions` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_embedding_request_dimensions(const LITER_LLMEmbeddingRequest *ptr);

/**
 * Get the `user` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_embedding_request_user(const LITER_LLMEmbeddingRequest *ptr);

/**
 * Create a `EmbeddingResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_embedding_response_free`.
 */
LITER_LLMEmbeddingResponse *liter_llm_embedding_response_from_json(const char *json);

/**
 * Free a `EmbeddingResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_embedding_response_free(LITER_LLMEmbeddingResponse *ptr);

/**
 * Get the `object` field from a `EmbeddingResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_embedding_response_object(const LITER_LLMEmbeddingResponse *ptr);

/**
 * Get the `data` field from a `EmbeddingResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_embedding_response_data(const LITER_LLMEmbeddingResponse *ptr);

/**
 * Get the `model` field from a `EmbeddingResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_embedding_response_model(const LITER_LLMEmbeddingResponse *ptr);

/**
 * Get the `usage` field from a `EmbeddingResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMUsage *liter_llm_embedding_response_usage(const LITER_LLMEmbeddingResponse *ptr);

/**
 * Estimate the cost of this embedding request based on embedded pricing data.
 *
 * Returns `None` if:
 * - the `model` field is not present in the embedded pricing registry, or
 * - the `usage` field is absent from the response.
 *
 * Embedding models only charge for input tokens; output cost is zero.
 *
 * # Example
 *
 * ```rust,ignore
 * let cost = response.estimated_cost();
 * if let Some(usd) = cost {
 *     println!("Embedding cost: ${usd:.8}");
 * }
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
double liter_llm_embedding_response_estimated_cost(const LITER_LLMEmbeddingResponse *this_);

/**
 * Create a `EmbeddingObject` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_embedding_object_free`.
 */
LITER_LLMEmbeddingObject *liter_llm_embedding_object_from_json(const char *json);

/**
 * Free a `EmbeddingObject` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_embedding_object_free(LITER_LLMEmbeddingObject *ptr);

/**
 * Get the `object` field from a `EmbeddingObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_embedding_object_object(const LITER_LLMEmbeddingObject *ptr);

/**
 * Get the `embedding` field from a `EmbeddingObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_embedding_object_embedding(const LITER_LLMEmbeddingObject *ptr);

/**
 * Get the `index` field from a `EmbeddingObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_embedding_object_index(const LITER_LLMEmbeddingObject *ptr);

/**
 * Create a `CreateImageRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_create_image_request_free`.
 */
LITER_LLMCreateImageRequest *liter_llm_create_image_request_from_json(const char *json);

/**
 * Free a `CreateImageRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_create_image_request_free(LITER_LLMCreateImageRequest *ptr);

/**
 * Get the `prompt` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_image_request_prompt(const LITER_LLMCreateImageRequest *ptr);

/**
 * Get the `model` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_image_request_model(const LITER_LLMCreateImageRequest *ptr);

/**
 * Get the `n` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_create_image_request_n(const LITER_LLMCreateImageRequest *ptr);

/**
 * Get the `size` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_image_request_size(const LITER_LLMCreateImageRequest *ptr);

/**
 * Get the `quality` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_image_request_quality(const LITER_LLMCreateImageRequest *ptr);

/**
 * Get the `style` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_image_request_style(const LITER_LLMCreateImageRequest *ptr);

/**
 * Get the `response_format` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_image_request_response_format(const LITER_LLMCreateImageRequest *ptr);

/**
 * Get the `user` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_create_image_request_user(const LITER_LLMCreateImageRequest *ptr);

/**
 * Create a `ImagesResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_images_response_free`.
 */
LITER_LLMImagesResponse *liter_llm_images_response_from_json(const char *json);

/**
 * Free a `ImagesResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_images_response_free(LITER_LLMImagesResponse *ptr);

/**
 * Get the `created` field from a `ImagesResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t liter_llm_images_response_created(const LITER_LLMImagesResponse *ptr);

/**
 * Get the `data` field from a `ImagesResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_images_response_data(const LITER_LLMImagesResponse *ptr);

/**
 * Create a `Image` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_image_free`.
 */
LITER_LLMImage *liter_llm_image_from_json(const char *json);

/**
 * Free a `Image` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_image_free(LITER_LLMImage *ptr);

/**
 * Get the `url` field from a `Image`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_image_url(const LITER_LLMImage *ptr);

/**
 * Get the `b64_json` field from a `Image`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_image_b64_json(const LITER_LLMImage *ptr);

/**
 * Get the `revised_prompt` field from a `Image`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_image_revised_prompt(const LITER_LLMImage *ptr);

/**
 * Create a `ModelsListResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_models_list_response_free`.
 */
LITER_LLMModelsListResponse *liter_llm_models_list_response_from_json(const char *json);

/**
 * Free a `ModelsListResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_models_list_response_free(LITER_LLMModelsListResponse *ptr);

/**
 * Get the `object` field from a `ModelsListResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_models_list_response_object(const LITER_LLMModelsListResponse *ptr);

/**
 * Get the `data` field from a `ModelsListResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_models_list_response_data(const LITER_LLMModelsListResponse *ptr);

/**
 * Create a `ModelObject` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_model_object_free`.
 */
LITER_LLMModelObject *liter_llm_model_object_from_json(const char *json);

/**
 * Free a `ModelObject` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_model_object_free(LITER_LLMModelObject *ptr);

/**
 * Get the `id` field from a `ModelObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_model_object_id(const LITER_LLMModelObject *ptr);

/**
 * Get the `object` field from a `ModelObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_model_object_object(const LITER_LLMModelObject *ptr);

/**
 * Get the `created` field from a `ModelObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t liter_llm_model_object_created(const LITER_LLMModelObject *ptr);

/**
 * Get the `owned_by` field from a `ModelObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_model_object_owned_by(const LITER_LLMModelObject *ptr);

/**
 * Create a `ModerationRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_moderation_request_free`.
 */
LITER_LLMModerationRequest *liter_llm_moderation_request_from_json(const char *json);

/**
 * Free a `ModerationRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_moderation_request_free(LITER_LLMModerationRequest *ptr);

/**
 * Get the `input` field from a `ModerationRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMModerationInput *liter_llm_moderation_request_input(const LITER_LLMModerationRequest *ptr);

/**
 * Get the `model` field from a `ModerationRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_moderation_request_model(const LITER_LLMModerationRequest *ptr);

/**
 * Create a `ModerationResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_moderation_response_free`.
 */
LITER_LLMModerationResponse *liter_llm_moderation_response_from_json(const char *json);

/**
 * Free a `ModerationResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_moderation_response_free(LITER_LLMModerationResponse *ptr);

/**
 * Get the `id` field from a `ModerationResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_moderation_response_id(const LITER_LLMModerationResponse *ptr);

/**
 * Get the `model` field from a `ModerationResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_moderation_response_model(const LITER_LLMModerationResponse *ptr);

/**
 * Get the `results` field from a `ModerationResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_moderation_response_results(const LITER_LLMModerationResponse *ptr);

/**
 * Create a `ModerationResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_moderation_result_free`.
 */
LITER_LLMModerationResult *liter_llm_moderation_result_from_json(const char *json);

/**
 * Free a `ModerationResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_moderation_result_free(LITER_LLMModerationResult *ptr);

/**
 * Get the `flagged` field from a `ModerationResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_result_flagged(const LITER_LLMModerationResult *ptr);

/**
 * Get the `categories` field from a `ModerationResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMModerationCategories *liter_llm_moderation_result_categories(const LITER_LLMModerationResult *ptr);

/**
 * Get the `category_scores` field from a `ModerationResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMModerationCategoryScores *liter_llm_moderation_result_category_scores(const LITER_LLMModerationResult *ptr);

/**
 * Create a `ModerationCategories` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_moderation_categories_free`.
 */
LITER_LLMModerationCategories *liter_llm_moderation_categories_from_json(const char *json);

/**
 * Free a `ModerationCategories` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_moderation_categories_free(LITER_LLMModerationCategories *ptr);

/**
 * Get the `sexual` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_sexual(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `hate` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_hate(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `harassment` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_harassment(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `self_harm` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_self_harm(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `sexual_minors` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_sexual_minors(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `hate_threatening` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_hate_threatening(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `violence_graphic` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_violence_graphic(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `self_harm_intent` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_self_harm_intent(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `self_harm_instructions` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_self_harm_instructions(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `harassment_threatening` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_harassment_threatening(const LITER_LLMModerationCategories *ptr);

/**
 * Get the `violence` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_moderation_categories_violence(const LITER_LLMModerationCategories *ptr);

/**
 * Create a `ModerationCategoryScores` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_moderation_category_scores_free`.
 */
LITER_LLMModerationCategoryScores *liter_llm_moderation_category_scores_from_json(const char *json);

/**
 * Free a `ModerationCategoryScores` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_moderation_category_scores_free(LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `sexual` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_sexual(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `hate` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_hate(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `harassment` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_harassment(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `self_harm` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_self_harm(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `sexual_minors` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_sexual_minors(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `hate_threatening` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_hate_threatening(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `violence_graphic` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_violence_graphic(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `self_harm_intent` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_self_harm_intent(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `self_harm_instructions` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_self_harm_instructions(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `harassment_threatening` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_harassment_threatening(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Get the `violence` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_moderation_category_scores_violence(const LITER_LLMModerationCategoryScores *ptr);

/**
 * Create a `RerankRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_rerank_request_free`.
 */
LITER_LLMRerankRequest *liter_llm_rerank_request_from_json(const char *json);

/**
 * Free a `RerankRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_rerank_request_free(LITER_LLMRerankRequest *ptr);

/**
 * Get the `model` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_rerank_request_model(const LITER_LLMRerankRequest *ptr);

/**
 * Get the `query` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_rerank_request_query(const LITER_LLMRerankRequest *ptr);

/**
 * Get the `documents` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_rerank_request_documents(const LITER_LLMRerankRequest *ptr);

/**
 * Get the `top_n` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_rerank_request_top_n(const LITER_LLMRerankRequest *ptr);

/**
 * Get the `return_documents` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t liter_llm_rerank_request_return_documents(const LITER_LLMRerankRequest *ptr);

/**
 * Create a `RerankResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_rerank_response_free`.
 */
LITER_LLMRerankResponse *liter_llm_rerank_response_from_json(const char *json);

/**
 * Free a `RerankResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_rerank_response_free(LITER_LLMRerankResponse *ptr);

/**
 * Get the `id` field from a `RerankResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_rerank_response_id(const LITER_LLMRerankResponse *ptr);

/**
 * Get the `results` field from a `RerankResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_rerank_response_results(const LITER_LLMRerankResponse *ptr);

/**
 * Get the `meta` field from a `RerankResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_rerank_response_meta(const LITER_LLMRerankResponse *ptr);

/**
 * Create a `RerankResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_rerank_result_free`.
 */
LITER_LLMRerankResult *liter_llm_rerank_result_from_json(const char *json);

/**
 * Free a `RerankResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_rerank_result_free(LITER_LLMRerankResult *ptr);

/**
 * Get the `index` field from a `RerankResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t liter_llm_rerank_result_index(const LITER_LLMRerankResult *ptr);

/**
 * Get the `relevance_score` field from a `RerankResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double liter_llm_rerank_result_relevance_score(const LITER_LLMRerankResult *ptr);

/**
 * Get the `document` field from a `RerankResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITER_LLMRerankResultDocument *liter_llm_rerank_result_document(const LITER_LLMRerankResult *ptr);

/**
 * Create a `RerankResultDocument` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `liter_llm_rerank_result_document_free`.
 */
LITER_LLMRerankResultDocument *liter_llm_rerank_result_document_from_json(const char *json);

/**
 * Free a `RerankResultDocument` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void liter_llm_rerank_result_document_free(LITER_LLMRerankResultDocument *ptr);

/**
 * Get the `text` field from a `RerankResultDocument`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *liter_llm_rerank_result_document_text(const LITER_LLMRerankResultDocument *ptr);

/**
 * Convert an integer to a `FinishReason` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_finish_reason_from_i32(int32_t value);

/**
 * Convert a `FinishReason` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t liter_llm_finish_reason_from_str(const char *name);

/**
 * Convert an integer to a `ReasoningEffort` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_reasoning_effort_from_i32(int32_t value);

/**
 * Convert a `ReasoningEffort` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t liter_llm_reasoning_effort_from_str(const char *name);

/**
 * Convert an integer to a `ToolType` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_tool_type_from_i32(int32_t value);

/**
 * Convert a `ToolType` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t liter_llm_tool_type_from_str(const char *name);

/**
 * Convert an integer to a `StopSequence` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_stop_sequence_from_i32(int32_t value);

/**
 * Convert a `StopSequence` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t liter_llm_stop_sequence_from_str(const char *name);

/**
 * Convert an integer to a `EmbeddingFormat` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_embedding_format_from_i32(int32_t value);

/**
 * Convert a `EmbeddingFormat` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t liter_llm_embedding_format_from_str(const char *name);

/**
 * Convert an integer to a `EmbeddingInput` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_embedding_input_from_i32(int32_t value);

/**
 * Convert a `EmbeddingInput` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t liter_llm_embedding_input_from_str(const char *name);

/**
 * Convert an integer to a `ModerationInput` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_moderation_input_from_i32(int32_t value);

/**
 * Convert a `ModerationInput` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t liter_llm_moderation_input_from_str(const char *name);

/**
 * Convert an integer to a `RerankDocument` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t liter_llm_rerank_document_from_i32(int32_t value);

/**
 * Convert a `RerankDocument` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t liter_llm_rerank_document_from_str(const char *name);

#endif  /* LITER_LLM_H */
