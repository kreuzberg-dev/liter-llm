---
id: fixture_wasm_bedrock_chat
language: typescript
target: wasm
requires: []
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:a17a816537712c6f1a3fa658b091523e7005e4f0ffe1cd3b4fee2cd993d6ec32 -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape

```typescript title="WebAssembly"
import { WasmChatCompletionRequest, createClient } from "@xberg-io/liter-llm-wasm";
async function main() {
  const req: WasmChatCompletionRequest = (() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 16n; _u0.messages = [{ content: "Say hello in one word.", role: "user" }]; _u0.model = "bedrock/anthropic.claude-3-sonnet-20240229-v1:0"; _u0.temperature = 0; return _u0; })();
  const client = createClient("your-api-key");
  try {
    const result = await client.chat(req);
    console.log(result.choices);
    console.log(result.choices[0].message.content);
    console.log(result.choices[0].finishReason);
    console.log(result.model);
    console.log(result.usage?.totalTokens);
  } finally {
    client.free();
  }
}

void main();
```
