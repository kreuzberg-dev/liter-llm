---
id: fixture_c_error_speech_bad_model
language: c
target: c
requires: []
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:7915c79bf02a8775363f5f63e81cbba4fc588dfadc4e20172e3e2e15968df0ec -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

400 Bad Request for speech with unsupported model

```c title="C"
#include <assert.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "liter_llm.h"

#ifndef ALEF_TEST_PASS
#define ALEF_TEST_PASS() do { return EXIT_SUCCESS; } while (0)
#endif

int main(void) {
    const char* api_key = getenv("API_KEY");
    assert(api_key != NULL && "API_KEY must be set");
    LITERLLMAlefHandle create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello\",\"model\":\"tts-nonexistent\",\"voice\":\"alloy\"}");
    if (create_speech_request_handle == 0) { ALEF_TEST_PASS(); }
    LITERLLMAlefHandle client = literllm_create_client(api_key, NULL, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status != 0 && "expected call to fail");
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
    literllm_free_bytes(out_ptr, out_len, out_cap);
    return EXIT_SUCCESS;
}
```
