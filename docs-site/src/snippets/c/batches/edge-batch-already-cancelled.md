---
id: fixture_c_edge_batch_already_cancelled
language: c
target: c
requires: []
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:486ccd95131a903de94e12f1c9a70371a04fcae061ebf0960a2655c2b69d1f50 -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

Attempt to cancel an already-cancelled batch

```c title="C"
#include <assert.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "liter_llm.h"

int main(void) {
    const char* api_key = getenv("API_KEY");
    assert(api_key != NULL && "API_KEY must be set");
    LITERLLMAlefHandle client = literllm_create_client(api_key, NULL, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_cancel_batch(client, "batch-cancelled001");
    if (result != 0) { return EXIT_FAILURE; }
    literllm_default_client_free(client);
    return EXIT_SUCCESS;
}
```
