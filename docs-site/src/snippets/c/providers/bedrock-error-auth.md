---
id: fixture_c_bedrock_error_auth
language: c
target: c
requires: []
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:7ab6dc923ac66d554c06e0883f7fe2f7ae0a17d336d6850e79255fc96ea0572a -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication

```c title="C"
#include <assert.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "liter_llm.h"

#ifndef ALEF_TEST_PASS
#define ALEF_TEST_PASS() do { return EXIT_SUCCESS; } while (0)
#endif

int main(void) {
    const char* api_key = getenv("API_KEY");
    assert(api_key != NULL && "API_KEY must be set");
    LITERLLMAlefHandle chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\"}");
    if (chat_completion_request_handle == 0) { ALEF_TEST_PASS(); }
    LITERLLMAlefHandle client = literllm_create_client(api_key, NULL, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_chat(client, chat_completion_request_handle);
    if (result != 0) { return EXIT_FAILURE; }
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
    return EXIT_SUCCESS;
}
```
