---
id: fixture_c_error_rerank_auth_401
language: c
target: c
requires: []
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:a197f7816aaa89958019c6c2771e150b1fbe26ae64ac286fe8e86d6175d31dc9 -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

401 Unauthorized for reranking with invalid API key

```c title="C"
#include <assert.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "liter_llm.h"

#ifndef ALEF_TEST_PASS
#define ALEF_TEST_PASS() do { return EXIT_SUCCESS; } while (0)
#endif

int main(void) {
    const char* api_key = getenv("API_KEY");
    assert(api_key != NULL && "API_KEY must be set");
    LITERLLMAlefHandle rerank_request_handle = literllm_rerank_request_from_json("{\"documents\":[\"doc1\"],\"model\":\"rerank-v3.5\",\"query\":\"test\"}");
    if (rerank_request_handle == 0) { ALEF_TEST_PASS(); }
    LITERLLMAlefHandle client = literllm_create_client(api_key, NULL, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_rerank(client, rerank_request_handle);
    if (result != 0) { return EXIT_FAILURE; }
    literllm_rerank_request_free(rerank_request_handle);
    literllm_default_client_free(client);
    return EXIT_SUCCESS;
}
```
