// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:73b2346e9c7f0da5e75ce92559a770f4475ca8b42cba0d28bca5df41226589e1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: moderate
package e2e_test

import (
	"encoding/json"
	"os"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/kreuzberg-dev/liter-llm/packages/go"
)

func Test_EdgeModerateAllCategories(t *testing.T) {
	// Moderation response with multiple categories flagged
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_moderate_all_categories"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Moderate(`Extremely harmful content targeting multiple categories`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 1, "expected exactly 1 elements")
	if result.Results["0"].Flagged != true {
		t.Errorf("equals mismatch: got %v", result.Results["0"].Flagged)
	}
}

func Test_EdgeModerateEmptyInput(t *testing.T) {
	// Moderation with empty string input
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_moderate_empty_input"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Moderate(``)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 1, "expected exactly 1 elements")
	if result.Results["0"].Flagged != false {
		t.Errorf("equals mismatch: got %v", result.Results["0"].Flagged)
	}
}

func Test_ErrorModerateAuth401(t *testing.T) {
	// 401 Unauthorized for moderation with invalid API key
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_moderate_auth_401"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Moderate(`Hello`)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ErrorModerateBadRequest(t *testing.T) {
	// 400 Bad Request for moderation with invalid model
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_moderate_bad_request"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Moderate(`Hello`)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_SmokeModerateBatch(t *testing.T) {
	// Moderate multiple inputs in a single request
	var request []string
	if err := json.Unmarshal([]byte(`["Hello world","Nice weather today"]`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_batch"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Moderate(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 2, "expected exactly 2 elements")
	if result.Results["0"].Flagged != false {
		t.Errorf("equals mismatch: got %v", result.Results["0"].Flagged)
	}
}

func Test_SmokeModerateFlagged(t *testing.T) {
	// Moderation detects flagged content
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_flagged"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Moderate(`I want to hurt someone very badly`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 1, "expected exactly 1 elements")
	if result.Results["0"].Flagged != true {
		t.Errorf("equals mismatch: got %v", result.Results["0"].Flagged)
	}
}

func Test_SmokeModerateSingle(t *testing.T) {
	// Moderate a single non-flagged input
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_single"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Moderate(`The weather is nice today.`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 1, "expected exactly 1 elements")
	if result.Results["0"].Flagged != false {
		t.Errorf("equals mismatch: got %v", result.Results["0"].Flagged)
	}
}
