// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:46ba6ea8d10b608425fc7341b1a9bb40d026bca3bf59c9d8c2b0443d70670d6e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: batches
package e2e_test

import (
	"os"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/kreuzberg-dev/liter-llm/packages/go"
)

func Test_EdgeBatchAlreadyCancelled(t *testing.T) {
	// Attempt to cancel an already-cancelled batch
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_batch_already_cancelled"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.CancelBatch(`batch-cancelled001`)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_EdgeBatchEmptyList(t *testing.T) {
	// List batches when no batches exist
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_batch_empty_list"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ListBatches()
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 0, "expected exactly 0 elements")
}

func Test_ErrorBatchAuth401(t *testing.T) {
	// 401 Unauthorized when creating a batch with invalid API key
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_batch_auth_401"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.CreateBatch(nil)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ErrorBatchInvalidFile(t *testing.T) {
	// 400 Bad Request when creating a batch with invalid input file
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_batch_invalid_file"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.CreateBatch(nil)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ErrorBatchNotFound(t *testing.T) {
	// 404 Not Found when retrieving a nonexistent batch
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_batch_not_found"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.RetrieveBatch(`batch-nonexistent`)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_SmokeBatchCompleted(t *testing.T) {
	// Retrieve a completed batch job with output file
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_batch_completed"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.RetrieveBatch(`batch-ghi789`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.ID) == 0 {
		t.Errorf("expected non-empty value")
	}
	if strings.TrimSpace(result.Status) != `completed` {
		t.Errorf("equals mismatch: got %v", result.Status)
	}
}

func Test_SmokeCancelBatch(t *testing.T) {
	// Cancel a running batch job
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_cancel_batch"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.CancelBatch(`batch-def456`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.ID) == 0 {
		t.Errorf("expected non-empty value")
	}
	if strings.TrimSpace(result.Status) != `cancelling` {
		t.Errorf("equals mismatch: got %v", result.Status)
	}
}

func Test_SmokeCreateBatch(t *testing.T) {
	// Create a new batch processing job
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_create_batch"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.CreateBatch(nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.ID) == 0 {
		t.Errorf("expected non-empty value")
	}
	if strings.TrimSpace(result.Status) != `validating` {
		t.Errorf("equals mismatch: got %v", result.Status)
	}
}

func Test_SmokeListBatches(t *testing.T) {
	// List all batch jobs
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_list_batches"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ListBatches()
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 2, "expected exactly 2 elements")
}

func Test_SmokeRetrieveBatch(t *testing.T) {
	// Retrieve the status of a batch job
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_retrieve_batch"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.RetrieveBatch(`batch-abc123`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.ID) == 0 {
		t.Errorf("expected non-empty value")
	}
	if strings.TrimSpace(result.Status) != `in_progress` {
		t.Errorf("equals mismatch: got %v", result.Status)
	}
}
