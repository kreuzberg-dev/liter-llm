// Code generated by liter-llm-e2e-generator. DO NOT EDIT.

package literllm_e2e

import (
	"testing"
)

func TestTranscribe(t *testing.T) {
	t.Run("edge_transcribe_empty_audio", func(t *testing.T) {
		// Transcription of a silent or empty audio file returns empty text
		server := NewMockServer([]MockRoute{
			{
				Path:         "/audio/transcriptions",
				Method:       "POST",
				Status:       200,
				Body:         `{"text":""}`,
				StreamChunks: nil,
			},
		})
		defer server.Close()

		// TODO: unknown method "transcribe"
	})

	t.Run("error_transcribe_auth_401", func(t *testing.T) {
		// 401 Unauthorized for transcription with invalid API key
		server := NewMockServer([]MockRoute{
			{
				Path:         "/audio/transcriptions",
				Method:       "POST",
				Status:       401,
				Body:         `{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}`,
				StreamChunks: nil,
			},
		})
		defer server.Close()

		// TODO: unknown method "transcribe"
	})

	t.Run("error_transcribe_bad_format", func(t *testing.T) {
		// 400 Bad Request when audio format is unsupported
		server := NewMockServer([]MockRoute{
			{
				Path:         "/audio/transcriptions",
				Method:       "POST",
				Status:       400,
				Body:         `{"error":{"code":null,"message":"Invalid file format. Supported formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, webm.","param":"file","type":"invalid_request_error"}}`,
				StreamChunks: nil,
			},
		})
		defer server.Close()

		// TODO: unknown method "transcribe"
	})

	t.Run("smoke_transcribe_basic", func(t *testing.T) {
		// Basic audio transcription
		server := NewMockServer([]MockRoute{
			{
				Path:         "/audio/transcriptions",
				Method:       "POST",
				Status:       200,
				Body:         `{"text":"Hello, this is a test transcription."}`,
				StreamChunks: nil,
			},
		})
		defer server.Close()

		// TODO: unknown method "transcribe"
	})

	t.Run("smoke_transcribe_with_language", func(t *testing.T) {
		// Audio transcription with explicit language hint
		server := NewMockServer([]MockRoute{
			{
				Path:         "/audio/transcriptions",
				Method:       "POST",
				Status:       200,
				Body:         `{"text":"Hallo, dies ist ein Testtranskription."}`,
				StreamChunks: nil,
			},
		})
		defer server.Close()

		// TODO: unknown method "transcribe"
	})

}
