// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: files. */
class FilesTest {
    @Test
    fun testEdgeFileEmptyList() {
        // List files when no files have been uploaded
        val result = LiterLlm.listFiles()
        assertEquals(0, result.data.size, "expected exactly 0 elements")
    }

    @Test
    fun testEdgeFileLargeUpload() {
        // Upload a large file successfully
        val result = LiterLlm.createFile(null)
        assertFalse(result.id.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testErrorFileAuth401() {
        // 401 Unauthorized when listing files with invalid API key
        assertFailsWith<Exception> { LiterLlm.listFiles() }
    }

    @Test
    fun testErrorFileBadPurpose() {
        // 400 Bad Request when uploading a file with invalid purpose
        assertFailsWith<Exception> { LiterLlm.createFile(null) }
    }

    @Test
    fun testErrorFileNotFound() {
        // 404 Not Found when retrieving a nonexistent file
        assertFailsWith<Exception> { LiterLlm.retrieveFile("file-nonexistent") }
    }

    @Test
    fun testSmokeCreateFile() {
        // Upload a file for use with the API
        val result = LiterLlm.createFile(null)
        assertFalse(result.id.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testSmokeDeleteFile() {
        // Delete an uploaded file
        val result = LiterLlm.deleteFile("file-abc123")
        assertFalse(result.id.isEmpty(), "expected non-empty value")
        assertEquals(true, result.deleted)
    }

    @Test
    fun testSmokeFileContent() {
        // Retrieve the content of an uploaded file
        val result = LiterLlm.fileContent("file-abc123")
        assertFalse(result.content.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testSmokeListFiles() {
        // List all uploaded files
        val result = LiterLlm.listFiles()
        assertEquals(2, result.data.size, "expected exactly 2 elements")
    }

    @Test
    fun testSmokeRetrieveFile() {
        // Retrieve metadata for an uploaded file
        val result = LiterLlm.retrieveFile("file-abc123")
        assertFalse(result.id.isEmpty(), "expected non-empty value")
    }

}
