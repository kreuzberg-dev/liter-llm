// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: moderate. */
class ModerateTest {
    @Test
    fun testEdgeModerateAllCategories() {
        // Moderation response with multiple categories flagged
        val result = LiterLlm.moderate("Extremely harmful content targeting multiple categories")
        assertEquals(1, result.results.size, "expected exactly 1 elements")
        assertEquals(true, result.results.get("0").flagged)
    }

    @Test
    fun testEdgeModerateEmptyInput() {
        // Moderation with empty string input
        val result = LiterLlm.moderate("")
        assertEquals(1, result.results.size, "expected exactly 1 elements")
        assertEquals(false, result.results.get("0").flagged)
    }

    @Test
    fun testErrorModerateAuth401() {
        // 401 Unauthorized for moderation with invalid API key
        assertFailsWith<Exception> { LiterLlm.moderate("Hello") }
    }

    @Test
    fun testErrorModerateBadRequest() {
        // 400 Bad Request for moderation with invalid model
        assertFailsWith<Exception> { LiterLlm.moderate("Hello") }
    }

    @Test
    fun testSmokeModerateBatch() {
        // Moderate multiple inputs in a single request
        val result = LiterLlm.moderate(listOf("Hello world", "Nice weather today"))
        assertEquals(2, result.results.size, "expected exactly 2 elements")
        assertEquals(false, result.results.get("0").flagged)
    }

    @Test
    fun testSmokeModerateFlagged() {
        // Moderation detects flagged content
        val result = LiterLlm.moderate("I want to hurt someone very badly")
        assertEquals(1, result.results.size, "expected exactly 1 elements")
        assertEquals(true, result.results.get("0").flagged)
    }

    @Test
    fun testSmokeModerateSingle() {
        // Moderate a single non-flagged input
        val result = LiterLlm.moderate("The weather is nice today.")
        assertEquals(1, result.results.size, "expected exactly 1 elements")
        assertEquals(false, result.results.get("0").flagged)
    }

}
