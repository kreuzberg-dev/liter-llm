// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: proxy. */
class ProxyTest {
    @Test
    fun testProxyAuthInvalid() {
        // 401 Unauthorized when an invalid API key is provided through the proxy
        assertFailsWith<Exception> { LiterLlm.chat(null) }
    }

    @Test
    fun testProxyAuthMissing() {
        // 401 Unauthorized when no API key is provided through the proxy
        assertFailsWith<Exception> { LiterLlm.chat(null) }
    }

    @Test
    fun testProxyChatBasic() {
        // Basic chat completion request routed through the proxy
        val result = LiterLlm.chat(null)
        assertEquals(1, result.choices.size, "expected exactly 1 elements")
        assertEquals("Hello!", result.choices.get("0").message.content.trim())
        assertEquals("stop", result.choices.get("0").finish_reason.trim())
    }

    @Test
    fun testProxyChatStreaming() {
        // Streaming chat completion routed through the proxy
        val result = LiterLlm.chatStream(null)
        assertTrue(result.chunks.size >= 3, "expected at least 3 elements")
        assertEquals("1 2 3", result.stream_content.trim())
        assertTrue(result.stream_complete, "expected true")
    }

    @Test
    fun testProxyEmbeddings() {
        // Embedding request routed through the proxy
        val result = LiterLlm.embed("Hello world")
        assertEquals(1, result.data.size, "expected exactly 1 elements")
    }

    @Test
    fun testProxyHealth() {
        // Health check verifying proxy connectivity via list models
        val result = LiterLlm.listModels()
        assertTrue(result.data.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testProxyImageGenerate() {
        // Image generation request routed through the proxy
        val result = LiterLlm.imageGenerate(null)
        assertEquals(1, result.data.size, "expected exactly 1 elements")
        assertFalse(result.data.get("0").url.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testProxyModelsList() {
        // List models request routed through the proxy
        val result = LiterLlm.listModels()
        assertTrue(result.data.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testProxyModeration() {
        // Content moderation request routed through the proxy
        val result = LiterLlm.moderate("The weather is nice today.")
        assertEquals(1, result.results.size, "expected exactly 1 elements")
        assertEquals(false, result.results.get("0").flagged)
    }

    @Test
    fun testProxyRerank() {
        // Document reranking request routed through the proxy
        val result = LiterLlm.rerank(null)
        assertEquals(2, result.results.size, "expected exactly 2 elements")
        assertTrue(result.results.get("0").relevance_score > 0.9d, "expected > {kotlin_val}")
    }

    @Test
    fun testProxyUpstream429() {
        // 429 Too Many Requests from upstream provider through the proxy
        assertFailsWith<Exception> { LiterLlm.chat(null) }
    }

    @Test
    fun testProxyUpstream500() {
        // 500 Internal Server Error from upstream provider through the proxy
        assertFailsWith<Exception> { LiterLlm.chat(null) }
    }

}
