// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:01318a17759bb4ba35fa70be24dd1958db5d4020dadda2ab982f8a3b7445701f
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.literllm.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlin.test.assertNotNull
import kotlinx.coroutines.runBlocking
import io.xberg.literllm.android.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import io.xberg.literllm.android.ChatCompletionRequest

/** E2e tests for category: configuration. */
class ConfigurationTest {

    companion object {
        init {
            try {
                System.loadLibrary("literllm_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load literllm_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testCustomBaseUrl() = runBlocking {
        // Client configured with a custom base URL routes all requests to that endpoint
        val req = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"local-model\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.custom_base_url", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/custom_base_url"))
        val result = client.chat(req)
        // not_error: covered by the bare Optional's own assertion
        assertEquals(1, result.choices.size, "expected exactly 1 elements")
        assertEquals("Hi there!", result.choices.first().message.content?.toWire().orEmpty())
        assertEquals("stop", result.choices.first().finishReason?.toWire().orEmpty())
        assertEquals("local-model", result.model)
        client.close()
    }

    @Test
    fun testExtraHeaders() = runBlocking {
        // Client configured with extra custom headers successfully completes a chat request
        val req = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.extra_headers", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/extra_headers"))
        val result = client.chat(req)
        // not_error: covered by the bare Optional's own assertion
        assertEquals(1, result.choices.size, "expected exactly 1 elements")
        assertEquals("stop", result.choices.first().finishReason?.toWire().orEmpty())
        client.close()
    }

    @Test
    fun testLocalProviderLlamacpp() = runBlocking {
        // llamacpp local provider routes requests via llamacpp/ model prefix with no auth
        val req = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"llamacpp/my-model\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.local_provider_llamacpp", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/local_provider_llamacpp"))
        val result = client.chat(req)
        // not_error: covered by the bare Optional's own assertion
        assertEquals(1, result.choices.size, "expected exactly 1 elements")
        assertEquals("Hi there! I'm running locally.", result.choices.first().message.content?.toWire().orEmpty())
        assertEquals("stop", result.choices.first().finishReason?.toWire().orEmpty())
        assertEquals("my-model", result.model)
        client.close()
    }

    @Test
    fun testLocalProviderOllama() = runBlocking {
        // Ollama local provider routes requests via ollama/ model prefix with no auth
        val req = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"ollama/qwen2:0.5b\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.local_provider_ollama", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/local_provider_ollama"))
        val result = client.chat(req)
        // not_error: covered by the bare Optional's own assertion
        assertEquals(1, result.choices.size, "expected exactly 1 elements")
        assertEquals("Hello! How can I help you today?", result.choices.first().message.content?.toWire().orEmpty())
        assertEquals("stop", result.choices.first().finishReason?.toWire().orEmpty())
        assertEquals("qwen2:0.5b", result.model)
        client.close()
    }

    @Test
    fun testLocalProviderVllm() = runBlocking {
        // vLLM local provider routes requests via vllm/ model prefix with no auth
        val req = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vllm/meta-llama/Llama-3.2-1B\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.local_provider_vllm", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/local_provider_vllm"))
        val result = client.chat(req)
        // not_error: covered by the bare Optional's own assertion
        assertEquals(1, result.choices.size, "expected exactly 1 elements")
        assertEquals("Hello! How may I assist you?", result.choices.first().message.content?.toWire().orEmpty())
        assertEquals("stop", result.choices.first().finishReason?.toWire().orEmpty())
        assertEquals("meta-llama/Llama-3.2-1B", result.model)
        client.close()
    }

}
