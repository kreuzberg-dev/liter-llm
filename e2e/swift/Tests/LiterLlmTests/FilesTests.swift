// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import LiterLlm

/// E2e tests for category: files.
final class FilesTests: XCTestCase {
    func testEdgeFileEmptyList() async throws {
        // List files when no files have been uploaded
        let result = try await listFiles()
        XCTAssertEqual(result.data.count, 0)
    }

    func testEdgeFileLargeUpload() async throws {
        // Upload a large file successfully
        let result = try await createFile(nil)
        XCTAssertFalse(result.id.isEmpty, "expected non-empty value")
    }

    func testErrorFileAuth401() async throws {
        // 401 Unauthorized when listing files with invalid API key
        do {
            _ = try await listFiles()
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorFileBadPurpose() async throws {
        // 400 Bad Request when uploading a file with invalid purpose
        do {
            _ = try await createFile(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorFileNotFound() async throws {
        // 404 Not Found when retrieving a nonexistent file
        do {
            _ = try await retrieveFile("file-nonexistent")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeCreateFile() async throws {
        // Upload a file for use with the API
        let result = try await createFile(nil)
        XCTAssertFalse(result.id.isEmpty, "expected non-empty value")
    }

    func testSmokeDeleteFile() async throws {
        // Delete an uploaded file
        let result = try await deleteFile("file-abc123")
        XCTAssertFalse(result.id.isEmpty, "expected non-empty value")
        XCTAssertEqual(result.deleted, true)
    }

    func testSmokeFileContent() async throws {
        // Retrieve the content of an uploaded file
        let result = try await fileContent("file-abc123")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testSmokeListFiles() async throws {
        // List all uploaded files
        let result = try await listFiles()
        XCTAssertEqual(result.data.count, 2)
    }

    func testSmokeRetrieveFile() async throws {
        // Retrieve metadata for an uploaded file
        let result = try await retrieveFile("file-abc123")
        XCTAssertFalse(result.id.isEmpty, "expected non-empty value")
    }

}
