// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import LiterLlm

/// E2e tests for category: batches.
final class BatchesTests: XCTestCase {
    func testEdgeBatchAlreadyCancelled() async throws {
        // Attempt to cancel an already-cancelled batch
        do {
            _ = try await cancelBatch("batch-cancelled001")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testEdgeBatchEmptyList() async throws {
        // List batches when no batches exist
        let result = try await listBatches()
        XCTAssertEqual(result.data.count, 0)
    }

    func testErrorBatchAuth401() async throws {
        // 401 Unauthorized when creating a batch with invalid API key
        do {
            _ = try await createBatch(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorBatchInvalidFile() async throws {
        // 400 Bad Request when creating a batch with invalid input file
        do {
            _ = try await createBatch(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorBatchNotFound() async throws {
        // 404 Not Found when retrieving a nonexistent batch
        do {
            _ = try await retrieveBatch("batch-nonexistent")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeBatchCompleted() async throws {
        // Retrieve a completed batch job with output file
        let result = try await retrieveBatch("batch-ghi789")
        XCTAssertFalse(result.id.isEmpty, "expected non-empty value")
        XCTAssertEqual(result.status.trimmingCharacters(in: .whitespaces), "completed")
    }

    func testSmokeCancelBatch() async throws {
        // Cancel a running batch job
        let result = try await cancelBatch("batch-def456")
        XCTAssertFalse(result.id.isEmpty, "expected non-empty value")
        XCTAssertEqual(result.status.trimmingCharacters(in: .whitespaces), "cancelling")
    }

    func testSmokeCreateBatch() async throws {
        // Create a new batch processing job
        let result = try await createBatch(nil)
        XCTAssertFalse(result.id.isEmpty, "expected non-empty value")
        XCTAssertEqual(result.status.trimmingCharacters(in: .whitespaces), "validating")
    }

    func testSmokeListBatches() async throws {
        // List all batch jobs
        let result = try await listBatches()
        XCTAssertEqual(result.data.count, 2)
    }

    func testSmokeRetrieveBatch() async throws {
        // Retrieve the status of a batch job
        let result = try await retrieveBatch("batch-abc123")
        XCTAssertFalse(result.id.isEmpty, "expected non-empty value")
        XCTAssertEqual(result.status.trimmingCharacters(in: .whitespaces), "in_progress")
    }

}
