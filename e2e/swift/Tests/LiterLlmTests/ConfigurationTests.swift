// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import LiterLlm

/// E2e tests for category: configuration.
final class ConfigurationTests: XCTestCase {
    func testCustomBaseUrl() async throws {
        // Client configured with a custom base URL routes all requests to that endpoint
        let result = try await chat(nil)
        XCTAssertEqual(result.choices.count, 1)
        XCTAssertEqual(result.choices.get("0").message.content.trimmingCharacters(in: .whitespaces), "Hi there!")
        XCTAssertEqual(result.choices.get("0").finish_reason.trimmingCharacters(in: .whitespaces), "stop")
        XCTAssertEqual(result.model.trimmingCharacters(in: .whitespaces), "local-model")
    }

    func testExtraHeaders() async throws {
        // Client configured with extra custom headers successfully completes a chat request
        let result = try await chat(nil)
        XCTAssertEqual(result.choices.count, 1)
        XCTAssertEqual(result.choices.get("0").finish_reason.trimmingCharacters(in: .whitespaces), "stop")
    }

    func testLocalProviderLlamacpp() async throws {
        // llamacpp local provider routes requests via llamacpp/ model prefix with no auth
        let result = try await chat(nil)
        XCTAssertEqual(result.choices.count, 1)
        XCTAssertEqual(result.choices.get("0").message.content.trimmingCharacters(in: .whitespaces), "Hi there! I'm running locally.")
        XCTAssertEqual(result.choices.get("0").finish_reason.trimmingCharacters(in: .whitespaces), "stop")
        XCTAssertEqual(result.model.trimmingCharacters(in: .whitespaces), "my-model")
    }

    func testLocalProviderOllama() async throws {
        // Ollama local provider routes requests via ollama/ model prefix with no auth
        let result = try await chat(nil)
        XCTAssertEqual(result.choices.count, 1)
        XCTAssertEqual(result.choices.get("0").message.content.trimmingCharacters(in: .whitespaces), "Hello! How can I help you today?")
        XCTAssertEqual(result.choices.get("0").finish_reason.trimmingCharacters(in: .whitespaces), "stop")
        XCTAssertEqual(result.model.trimmingCharacters(in: .whitespaces), "qwen2:0.5b")
    }

    func testLocalProviderVllm() async throws {
        // vLLM local provider routes requests via vllm/ model prefix with no auth
        let result = try await chat(nil)
        XCTAssertEqual(result.choices.count, 1)
        XCTAssertEqual(result.choices.get("0").message.content.trimmingCharacters(in: .whitespaces), "Hello! How may I assist you?")
        XCTAssertEqual(result.choices.get("0").finish_reason.trimmingCharacters(in: .whitespaces), "stop")
        XCTAssertEqual(result.model.trimmingCharacters(in: .whitespaces), "meta-llama/Llama-3.2-1B")
    }

}
