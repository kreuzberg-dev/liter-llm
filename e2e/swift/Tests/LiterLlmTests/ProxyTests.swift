// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import LiterLlm

/// E2e tests for category: proxy.
final class ProxyTests: XCTestCase {
    func testProxyAuthInvalid() async throws {
        // 401 Unauthorized when an invalid API key is provided through the proxy
        do {
            _ = try await chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testProxyAuthMissing() async throws {
        // 401 Unauthorized when no API key is provided through the proxy
        do {
            _ = try await chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testProxyChatBasic() async throws {
        // Basic chat completion request routed through the proxy
        let result = try await chat(nil)
        XCTAssertEqual(result.choices.count, 1)
        XCTAssertEqual(result.choices.get("0").message.content.trimmingCharacters(in: .whitespaces), "Hello!")
        XCTAssertEqual(result.choices.get("0").finish_reason.trimmingCharacters(in: .whitespaces), "stop")
    }

    func testProxyChatStreaming() async throws {
        // Streaming chat completion routed through the proxy
        let result = try await chatStream(nil)
        XCTAssertGreaterThanOrEqual(result.chunks.count, 3)
        XCTAssertEqual(result.stream_content.trimmingCharacters(in: .whitespaces), "1 2 3")
        XCTAssertTrue(result.stream_complete)
    }

    func testProxyEmbeddings() async throws {
        // Embedding request routed through the proxy
        let result = try await embed("Hello world")
        XCTAssertEqual(result.data.count, 1)
    }

    func testProxyHealth() async throws {
        // Health check verifying proxy connectivity via list models
        let result = try await listModels()
        XCTAssertGreaterThanOrEqual(result.data.count, 1)
    }

    func testProxyImageGenerate() async throws {
        // Image generation request routed through the proxy
        let result = try await imageGenerate(nil)
        XCTAssertEqual(result.data.count, 1)
        XCTAssertFalse(result.data.get("0").url.isEmpty, "expected non-empty value")
    }

    func testProxyModelsList() async throws {
        // List models request routed through the proxy
        let result = try await listModels()
        XCTAssertGreaterThanOrEqual(result.data.count, 1)
    }

    func testProxyModeration() async throws {
        // Content moderation request routed through the proxy
        let result = try await moderate("The weather is nice today.")
        XCTAssertEqual(result.results.count, 1)
        XCTAssertEqual(result.results.get("0").flagged, false)
    }

    func testProxyRerank() async throws {
        // Document reranking request routed through the proxy
        let result = try await rerank(nil)
        XCTAssertEqual(result.results.count, 2)
        XCTAssertGreaterThan(result.results.get("0").relevance_score, 0.9)
    }

    func testProxyUpstream429() async throws {
        // 429 Too Many Requests from upstream provider through the proxy
        do {
            _ = try await chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testProxyUpstream500() async throws {
        // 500 Internal Server Error from upstream provider through the proxy
        do {
            _ = try await chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
