// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: embed

test "batch_embed" {
    // Embedding request with multiple input strings returns one embedding object per input
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.embed(&.{"Hello", "World"});
    try testing.expectEqual(2, result.data.len);
    try testing.expectEqual(5, result.data.get("0").embedding.len);
}

test "edge_embed_batch_input" {
    // Embedding request with multiple inputs returns multiple embedding objects
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.embed(&.{"Hello world", "Goodbye world"});
    try testing.expectEqual(2, result.data.len);
    try testing.expectEqual(0, result.data.get("0").index);
    try testing.expectEqual(1, result.data.get("1").index);
}

test "edge_embed_empty_input" {
    // Embedding request with empty string input returns empty data array
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.embed("");
    try testing.expectEqual(0, result.data.len);
}

test "embed_encoding_format" {
    // Embedding request with explicit encoding_format of float returns float array embeddings
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.embed("Test input");
    try testing.expectEqual(1, result.data.len);
    try testing.expectEqual(5, result.data.get("0").embedding.len);
}

test "embed_error_401" {
    // 401 Unauthorized error on embedding request when API key is invalid
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.embed("Hello world");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "embed_with_dimensions" {
    // Embedding request with explicit dimensions parameter returns embeddings of the requested size
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.embed("Hello world");
    try testing.expectEqual(1, result.data.len);
    try testing.expectEqual(8, result.data.get("0").embedding.len);
}

test "local_embed_ollama" {
    // Embedding request via Ollama local provider with all-minilm model
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.embed("The quick brown fox jumps over the lazy dog");
    try testing.expectEqual(1, result.data.len);
    try testing.expectEqual(32, result.data.get("0").embedding.len);
}

