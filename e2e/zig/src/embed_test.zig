// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4880efa6d86ad79ef808f1603411be1ec28225c2fb79645545bb73336e32f136
// To regenerate: alef generate
// To verify freshness: alef verify
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: embed

test "batch_embed" {
    // Embedding request with multiple input strings returns one embedding object per input
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/batch_embed", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.embed("{\"input\":[\"Hello\",\"World\"],\"model\":\"text-embedding-3-small\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 2), result.object.get("data").?.array.items.len);
    try testing.expectEqual(@as(usize, 5), result.object.get("data").?.array.items[0].object.get("embedding").?.array.items.len);
}

test "edge_embed_batch_input" {
    // Embedding request with multiple inputs returns multiple embedding objects
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/edge_embed_batch_input", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.embed("{\"input\":[\"Hello world\",\"Goodbye world\"],\"model\":\"text-embedding-3-small\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 2), result.object.get("data").?.array.items.len);
    try testing.expectEqual(@as(i64, 0), result.object.get("data").?.array.items[0].object.get("index").?.integer);
    try testing.expectEqual(@as(i64, 1), result.object.get("data").?.array.items[1].object.get("index").?.integer);
}

test "edge_embed_empty_input" {
    // Embedding request with empty string input returns empty data array
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/edge_embed_empty_input", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.embed("{\"input\":\"\",\"model\":\"text-embedding-3-small\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 0), result.object.get("data").?.array.items.len);
}

test "embed_base64" {
    // Embedding request with encoding_format base64 decodes to a float array embedding
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/embed_base64", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.embed("{\"encoding_format\":\"base64\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 1), result.object.get("data").?.array.items.len);
    try testing.expectEqual(@as(usize, 4), result.object.get("data").?.array.items[0].object.get("embedding").?.array.items.len);
}

test "embed_encoding_format" {
    // Embedding request with explicit encoding_format of float returns float array embeddings
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/embed_encoding_format", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.embed("{\"encoding_format\":\"float\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 1), result.object.get("data").?.array.items.len);
    try testing.expectEqual(@as(usize, 5), result.object.get("data").?.array.items[0].object.get("embedding").?.array.items.len);
}

test "embed_error_401" {
    // 401 Unauthorized error on embedding request when API key is invalid
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/embed_error_401", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.embed("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "embed_with_dimensions" {
    // Embedding request with explicit dimensions parameter returns embeddings of the requested size
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/embed_with_dimensions", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.embed("{\"dimensions\":256,\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 1), result.object.get("data").?.array.items.len);
    try testing.expectEqual(@as(usize, 8), result.object.get("data").?.array.items[0].object.get("embedding").?.array.items.len);
}

test "local_embed_ollama" {
    // Embedding request via Ollama local provider with all-minilm model
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/local_embed_ollama", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.embed("{\"input\":\"The quick brown fox jumps over the lazy dog\",\"model\":\"ollama/all-minilm\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 1), result.object.get("data").?.array.items.len);
    try testing.expectEqual(@as(usize, 32), result.object.get("data").?.array.items[0].object.get("embedding").?.array.items.len);
}
