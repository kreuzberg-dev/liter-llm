// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: image-generate

test "edge_image_b64_response" {
    // Image generation returning base64-encoded data instead of URL
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expectEqual(1, result.data.len);
    try testing.expect(result.data.get("0").b64_json.len > 0);
}

test "edge_image_empty_prompt" {
    // Image generation with an empty prompt returns 400
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "edge_image_multiple_n" {
    // Image generation requesting multiple images with n=3
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expectEqual(3, result.data.len);
    try testing.expect(result.data.get("0").url.len > 0);
    try testing.expect(result.data.get("1").url.len > 0);
    try testing.expect(result.data.get("2").url.len > 0);
}

test "error_image_auth_401" {
    // 401 Unauthorized when generating images with invalid API key
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_image_bad_request" {
    // 400 Bad Request when image generation parameters are invalid
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_image_rate_limit" {
    // 429 Rate limit exceeded for image generation
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "smoke_image_basic" {
    // Basic image generation with a text prompt
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expectEqual(1, result.data.len);
    try testing.expect(result.data.get("0").url.len > 0);
}

test "smoke_image_multiple" {
    // Image generation requesting multiple images
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expectEqual(3, result.data.len);
    try testing.expect(result.data.get("0").url.len > 0);
}

test "smoke_image_with_size" {
    // Image generation with explicit size parameter
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expectEqual(1, result.data.len);
    try testing.expect(result.data.get("0").url.len > 0);
}

