// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: moderate

test "edge_moderate_all_categories" {
    // Moderation response with multiple categories flagged
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.moderate("Extremely harmful content targeting multiple categories");
    try testing.expectEqual(1, result.results.len);
    try testing.expectEqual(true, result.results.get("0").flagged);
}

test "edge_moderate_empty_input" {
    // Moderation with empty string input
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.moderate("");
    try testing.expectEqual(1, result.results.len);
    try testing.expectEqual(false, result.results.get("0").flagged);
}

test "error_moderate_auth_401" {
    // 401 Unauthorized for moderation with invalid API key
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.moderate("Hello");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_moderate_bad_request" {
    // 400 Bad Request for moderation with invalid model
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.moderate("Hello");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "smoke_moderate_batch" {
    // Moderate multiple inputs in a single request
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.moderate(&.{"Hello world", "Nice weather today"});
    try testing.expectEqual(2, result.results.len);
    try testing.expectEqual(false, result.results.get("0").flagged);
}

test "smoke_moderate_flagged" {
    // Moderation detects flagged content
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.moderate("I want to hurt someone very badly");
    try testing.expectEqual(1, result.results.len);
    try testing.expectEqual(true, result.results.get("0").flagged);
}

test "smoke_moderate_single" {
    // Moderate a single non-flagged input
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.moderate("The weather is nice today.");
    try testing.expectEqual(1, result.results.len);
    try testing.expectEqual(false, result.results.get("0").flagged);
}

