// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: transcribe

test "edge_transcribe_empty_audio" {
    // Transcription of a silent or empty audio file returns empty text
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.transcribe(null);
    try testing.expect(std.mem.indexOf(u8, result.text, "") != null);
}

test "edge_transcribe_with_timestamps" {
    // Transcription with verbose JSON response format including timestamp segments
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.transcribe(null);
    try testing.expect(result.text.len > 0);
    try testing.expectEqual(3, result.segments.len);
    try testing.expectEqual(0, result.segments.get("0").id);
}

test "error_transcribe_auth_401" {
    // 401 Unauthorized for transcription with invalid API key
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.transcribe(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_transcribe_bad_format" {
    // 400 Bad Request when audio format is unsupported
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.transcribe(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "smoke_transcribe_basic" {
    // Basic audio transcription
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.transcribe(null);
    try testing.expect(std.mem.indexOf(u8, result.text, "Hello, this is a test transcription.") != null);
}

test "smoke_transcribe_with_language" {
    // Audio transcription with explicit language hint
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.transcribe(null);
    try testing.expect(std.mem.indexOf(u8, result.text, "Hallo, dies ist ein Testtranskription.") != null);
}

