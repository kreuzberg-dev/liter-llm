// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: files

test "edge_file_empty_list" {
    // List files when no files have been uploaded
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.list_files();
    try testing.expectEqual(0, result.data.len);
}

test "edge_file_large_upload" {
    // Upload a large file successfully
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.create_file(null);
    try testing.expect(result.id.len > 0);
}

test "error_file_auth_401" {
    // 401 Unauthorized when listing files with invalid API key
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.list_files();
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_file_bad_purpose" {
    // 400 Bad Request when uploading a file with invalid purpose
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.create_file(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_file_not_found" {
    // 404 Not Found when retrieving a nonexistent file
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.retrieve_file("file-nonexistent");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "smoke_create_file" {
    // Upload a file for use with the API
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.create_file(null);
    try testing.expect(result.id.len > 0);
}

test "smoke_delete_file" {
    // Delete an uploaded file
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.delete_file("file-abc123");
    try testing.expect(result.id.len > 0);
    try testing.expectEqual(true, result.deleted);
}

test "smoke_file_content" {
    // Retrieve the content of an uploaded file
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.file_content("file-abc123");
    try testing.expect(result.content.len > 0);
}

test "smoke_list_files" {
    // List all uploaded files
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.list_files();
    try testing.expectEqual(2, result.data.len);
}

test "smoke_retrieve_file" {
    // Retrieve metadata for an uploaded file
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.retrieve_file("file-abc123");
    try testing.expect(result.id.len > 0);
}

