// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:55df6c8c12de08d0520cc6697c9515ebc6ba87592ab124aa52d876c7176b69df
// To regenerate: alef generate
// To verify freshness: alef verify
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: batches

test "edge_batch_already_cancelled" {
    // Attempt to cancel an already-cancelled batch
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/edge_batch_already_cancelled", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.cancel_batch("batch-cancelled001")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "edge_batch_empty_list" {
    // List batches when no batches exist
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/edge_batch_empty_list", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.list_batches(null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 0), result.object.get("data").?.array.items.len);
}

test "error_batch_auth_401" {
    // 401 Unauthorized when creating a batch with invalid API key
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/error_batch_auth_401", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.create_batch("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "error_batch_invalid_file" {
    // 400 Bad Request when creating a batch with invalid input file
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/error_batch_invalid_file", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.create_batch("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-wrong-purpose\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "error_batch_not_found" {
    // 404 Not Found when retrieving a nonexistent batch
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/error_batch_not_found", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.retrieve_batch("batch-nonexistent")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'NotFound' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "smoke_batch_completed" {
    // Retrieve a completed batch job with output file
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/smoke_batch_completed", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.retrieve_batch("batch-ghi789");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _ne = (result.object.get("id") orelse std.json.Value{ .null = {} });
        try testing.expect(switch (_ne) {
            .null => false,
            .string => |_s| _s.len > 0,
            .array => |_a| _a.items.len > 0,
            .object => |_o| _o.count() > 0,
            else => true,
        });
    }
    try testing.expectEqualStrings("completed", (result.object.get("status") orelse std.json.Value{ .null = {} }).string);
}

test "smoke_cancel_batch" {
    // Cancel a running batch job
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/smoke_cancel_batch", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.cancel_batch("batch-def456");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _ne = (result.object.get("id") orelse std.json.Value{ .null = {} });
        try testing.expect(switch (_ne) {
            .null => false,
            .string => |_s| _s.len > 0,
            .array => |_a| _a.items.len > 0,
            .object => |_o| _o.count() > 0,
            else => true,
        });
    }
    try testing.expectEqualStrings("cancelling", (result.object.get("status") orelse std.json.Value{ .null = {} }).string);
}

test "smoke_create_batch" {
    // Create a new batch processing job
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/smoke_create_batch", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.create_batch("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _ne = (result.object.get("id") orelse std.json.Value{ .null = {} });
        try testing.expect(switch (_ne) {
            .null => false,
            .string => |_s| _s.len > 0,
            .array => |_a| _a.items.len > 0,
            .object => |_o| _o.count() > 0,
            else => true,
        });
    }
    try testing.expectEqualStrings("validating", (result.object.get("status") orelse std.json.Value{ .null = {} }).string);
}

test "smoke_list_batches" {
    // List all batch jobs
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/smoke_list_batches", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.list_batches(null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 2), result.object.get("data").?.array.items.len);
}

test "smoke_retrieve_batch" {
    // Retrieve the status of a batch job
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/smoke_retrieve_batch", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.retrieve_batch("batch-abc123");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _ne = (result.object.get("id") orelse std.json.Value{ .null = {} });
        try testing.expect(switch (_ne) {
            .null => false,
            .string => |_s| _s.len > 0,
            .array => |_a| _a.items.len > 0,
            .object => |_o| _o.count() > 0,
            else => true,
        });
    }
    try testing.expectEqualStrings("in_progress", (result.object.get("status") orelse std.json.Value{ .null = {} }).string);
}
