// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: batches

test "edge_batch_already_cancelled" {
    // Attempt to cancel an already-cancelled batch
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.cancel_batch("batch-cancelled001");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "edge_batch_empty_list" {
    // List batches when no batches exist
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.list_batches();
    try testing.expectEqual(0, result.data.len);
}

test "error_batch_auth_401" {
    // 401 Unauthorized when creating a batch with invalid API key
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.create_batch(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_batch_invalid_file" {
    // 400 Bad Request when creating a batch with invalid input file
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.create_batch(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_batch_not_found" {
    // 404 Not Found when retrieving a nonexistent batch
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.retrieve_batch("batch-nonexistent");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "smoke_batch_completed" {
    // Retrieve a completed batch job with output file
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.retrieve_batch("batch-ghi789");
    try testing.expect(result.id.len > 0);
    try testing.expectEqual("completed", result.status);
}

test "smoke_cancel_batch" {
    // Cancel a running batch job
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.cancel_batch("batch-def456");
    try testing.expect(result.id.len > 0);
    try testing.expectEqual("cancelling", result.status);
}

test "smoke_create_batch" {
    // Create a new batch processing job
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.create_batch(null);
    try testing.expect(result.id.len > 0);
    try testing.expectEqual("validating", result.status);
}

test "smoke_list_batches" {
    // List all batch jobs
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.list_batches();
    try testing.expectEqual(2, result.data.len);
}

test "smoke_retrieve_batch" {
    // Retrieve the status of a batch job
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.retrieve_batch("batch-abc123");
    try testing.expect(result.id.len > 0);
    try testing.expectEqual("in_progress", result.status);
}

