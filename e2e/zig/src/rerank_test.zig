// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: rerank

test "edge_rerank_empty_query" {
    // Reranking with an empty query string
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.rerank(null);
    try testing.expectEqual(2, result.results.len);
}

test "edge_rerank_single_doc" {
    // Reranking with only a single document
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.rerank(null);
    try testing.expectEqual(1, result.results.len);
    try testing.expect(result.results.get("0").relevance_score > 0.9);
}

test "error_rerank_auth_401" {
    // 401 Unauthorized for reranking with invalid API key
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.rerank(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_rerank_bad_request" {
    // 400 Bad Request for reranking with invalid model
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.rerank(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "smoke_rerank_basic" {
    // Basic reranking of documents against a query
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.rerank(null);
    try testing.expectEqual(3, result.results.len);
    try testing.expect(result.results.get("0").relevance_score > 0.9);
}

test "smoke_rerank_return_docs" {
    // Reranking with return_documents flag to include document text
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.rerank(null);
    try testing.expectEqual(2, result.results.len);
    try testing.expect(result.results.get("0").document.len > 0);
    try testing.expect(result.results.get("0").relevance_score > 0.9);
}

test "smoke_rerank_with_top_n" {
    // Reranking with top_n parameter to limit results
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.rerank(null);
    try testing.expectEqual(2, result.results.len);
    try testing.expect(result.results.get("0").relevance_score > 0.9);
}

