// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: configuration

test "custom_base_url" {
    // Client configured with a custom base URL routes all requests to that endpoint
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hi there!", result.choices.get("0").message.content);
    try testing.expectEqual("stop", result.choices.get("0").finish_reason);
    try testing.expectEqual("local-model", result.model);
}

test "extra_headers" {
    // Client configured with extra custom headers successfully completes a chat request
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("stop", result.choices.get("0").finish_reason);
}

test "local_provider_llamacpp" {
    // llamacpp local provider routes requests via llamacpp/ model prefix with no auth
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hi there! I'm running locally.", result.choices.get("0").message.content);
    try testing.expectEqual("stop", result.choices.get("0").finish_reason);
    try testing.expectEqual("my-model", result.model);
}

test "local_provider_ollama" {
    // Ollama local provider routes requests via ollama/ model prefix with no auth
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hello! How can I help you today?", result.choices.get("0").message.content);
    try testing.expectEqual("stop", result.choices.get("0").finish_reason);
    try testing.expectEqual("qwen2:0.5b", result.model);
}

test "local_provider_vllm" {
    // vLLM local provider routes requests via vllm/ model prefix with no auth
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hello! How may I assist you?", result.choices.get("0").message.content);
    try testing.expectEqual("stop", result.choices.get("0").finish_reason);
    try testing.expectEqual("meta-llama/Llama-3.2-1B", result.model);
}

