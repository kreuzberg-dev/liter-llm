// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c69f748fda0057d855b74886ca7f7af68a4b0b310bd095c03d53966c4c7f944a
// To regenerate: alef generate
// To verify freshness: alef verify
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: error-handling

test "anthropic_error_auth" {
    // 401 Authentication error returned by the Anthropic API when the API key is invalid
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/anthropic_error_auth", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "auth_401" {
    // 401 Unauthorized error when API key is invalid or missing
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/auth_401", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "azure_error_auth" {
    // Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/azure_error_auth", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "bad_request_400" {
    // 400 Bad Request error when a parameter value is invalid
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/bad_request_400", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":5.0}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "bedrock_error_auth" {
    // AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/bedrock_error_auth", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "content_policy_violation" {
    // 400 error when a request is rejected due to content policy
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/content_policy_violation", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Generate harmful content\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'ContentPolicy' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "context_window_exceeded" {
    // 400 error when the prompt exceeds the model's maximum context length
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/context_window_exceeded", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Very long prompt that exceeds the context window...\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'ContextWindowExceeded' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "empty_response_body" {
    // 200 OK response with an empty JSON object body, missing required fields
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/empty_response_body", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_| {}
}

test "forbidden_403" {
    // 403 Forbidden error when the API key does not have access to the requested resource
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/forbidden_403", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "gateway_timeout_504" {
    // 504 Gateway Timeout error when the upstream service times out
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/gateway_timeout_504", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'ServiceUnavailable' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "github_copilot_error_auth" {
    // 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/github_copilot_error_auth", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "not_found_404" {
    // 404 Not Found error when requesting a model that does not exist
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/not_found_404", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-99\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'NotFound' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "rate_limit_429" {
    // 429 Too Many Requests error when the rate limit is exceeded
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/rate_limit_429", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'RateLimited' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "server_error_500" {
    // 500 Internal Server Error from the upstream API
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/server_error_500", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'ServerError' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "service_unavailable_502" {
    // 502 Bad Gateway error when the upstream service is unavailable
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/service_unavailable_502", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'ServiceUnavailable' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "timeout_error" {
    // 408 Request Timeout error when the API request takes too long to complete
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/timeout_error", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Timeout' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "vertex_error_auth" {
    // Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/vertex_error_auth", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.chat("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}
