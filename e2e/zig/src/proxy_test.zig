// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: proxy

test "proxy_auth_invalid" {
    // 401 Unauthorized when an invalid API key is provided through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "proxy_auth_missing" {
    // 401 Unauthorized when no API key is provided through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "proxy_chat_basic" {
    // Basic chat completion request routed through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hello!", result.choices.get("0").message.content);
    try testing.expectEqual("stop", result.choices.get("0").finish_reason);
}

test "proxy_chat_streaming" {
    // Streaming chat completion routed through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat_stream(null);
    try testing.expect(result.chunks.len >= 3);
    try testing.expectEqual("1 2 3", result.stream_content);
    try testing.expect(result.stream_complete);
}

test "proxy_embeddings" {
    // Embedding request routed through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.embed("Hello world");
    try testing.expectEqual(1, result.data.len);
}

test "proxy_health" {
    // Health check verifying proxy connectivity via list models
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.list_models();
    try testing.expect(result.data.len >= 1);
}

test "proxy_image_generate" {
    // Image generation request routed through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.image_generate(null);
    try testing.expectEqual(1, result.data.len);
    try testing.expect(result.data.get("0").url.len > 0);
}

test "proxy_models_list" {
    // List models request routed through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.list_models();
    try testing.expect(result.data.len >= 1);
}

test "proxy_moderation" {
    // Content moderation request routed through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.moderate("The weather is nice today.");
    try testing.expectEqual(1, result.results.len);
    try testing.expectEqual(false, result.results.get("0").flagged);
}

test "proxy_rerank" {
    // Document reranking request routed through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.rerank(null);
    try testing.expectEqual(2, result.results.len);
    try testing.expect(result.results.get("0").relevance_score > 0.9);
}

test "proxy_upstream_429" {
    // 429 Too Many Requests from upstream provider through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "proxy_upstream_500" {
    // 500 Internal Server Error from upstream provider through the proxy
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = liter_llm.chat(null);
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

