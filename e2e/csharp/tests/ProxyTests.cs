// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2bff2b7365f62d840630785dd56124effc8577ad3d6140fa8d1613600dbb0125
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using LiterLlm;
using static LiterLlm.LiterLlmLib;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: proxy.</summary>
public class ProxyTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public async Task Test_ProxyAuthInvalid()
    {
        // 401 Unauthorized when an invalid API key is provided through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_auth_invalid";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.Chat(JsonSerializer.Deserialize<ChatCompletionRequest>("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}", ConfigOptions)!));
    }

    [Fact]
    public async Task Test_ProxyAuthMissing()
    {
        // 401 Unauthorized when no API key is provided through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_auth_missing";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.Chat(JsonSerializer.Deserialize<ChatCompletionRequest>("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}", ConfigOptions)!));
    }

    [Fact]
    public async Task Test_ProxyChatBasic()
    {
        // Basic chat completion request routed through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_chat_basic";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        var result = await client.Chat(JsonSerializer.Deserialize<ChatCompletionRequest>("{\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}", ConfigOptions)!);
        Assert.Equal(1, result.Choices.Count);
        Assert.Equal("Hello!", result.Choices[0].Message.Content!.Trim());
        Assert.Equal("stop", result.Choices[0].FinishReason!.Trim());
    }

    [Fact]
    public async Task Test_ProxyChatStreaming()
    {
        // Streaming chat completion routed through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_chat_streaming";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        var result = await client.ChatStream(JsonSerializer.Deserialize<ChatCompletionRequest>("{\"messages\":[{\"content\":\"Count to 3\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\",\"stream\":true}", ConfigOptions)!);
        // skipped: field 'chunks' not available on result type
        // skipped: field 'stream_content' not available on result type
        // skipped: field 'stream_complete' not available on result type
    }

    [Fact]
    public async Task Test_ProxyEmbeddings()
    {
        // Embedding request routed through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_embeddings";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        var result = await client.Embed(JsonSerializer.Deserialize<EmbeddingRequest>("{\"input\":\"Hello world\",\"model\":\"openai/text-embedding-3-small\"}", ConfigOptions)!);
        Assert.Equal(1, result.Data.Count);
    }

    [Fact]
    public async Task Test_ProxyHealth()
    {
        // Health check verifying proxy connectivity via list models
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_health";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        var result = await client.ListModels();
        Assert.True(result.Data.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_ProxyImageGenerate()
    {
        // Image generation request routed through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_image_generate";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        var result = await client.ImageGenerate(JsonSerializer.Deserialize<CreateImageRequest>("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over the ocean\",\"size\":\"1024x1024\"}", ConfigOptions)!);
        Assert.Equal(1, result.Data.Count);
        Assert.False(string.IsNullOrEmpty(result.Data[0].Url?.ToString()));
    }

    [Fact]
    public async Task Test_ProxyModelsList()
    {
        // List models request routed through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_models_list";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        var result = await client.ListModels();
        Assert.True(result.Data.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_ProxyModeration()
    {
        // Content moderation request routed through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_moderation";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        var result = await client.Moderate(JsonSerializer.Deserialize<ModerationRequest>("{\"input\":\"The weather is nice today.\",\"model\":\"omni-moderation-latest\"}", ConfigOptions)!);
        Assert.Equal(1, result.Results.Count);
        Assert.False(result.Results[0].Flagged);
    }

    [Fact]
    public async Task Test_ProxyRerank()
    {
        // Document reranking request routed through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_rerank";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        var result = await client.Rerank(JsonSerializer.Deserialize<RerankRequest>("{\"documents\":[\"Deep learning is a subset of machine learning using neural networks.\",\"The stock market closed higher today.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is deep learning?\"}", ConfigOptions)!);
        Assert.Equal(2, result.Results.Count);
        Assert.True(result.Results[0].RelevanceScore > 0.9d, "expected > 0.9d");
    }

    [Fact]
    public async Task Test_ProxyUpstream429()
    {
        // 429 Too Many Requests from upstream provider through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_upstream_429";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.Chat(JsonSerializer.Deserialize<ChatCompletionRequest>("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}", ConfigOptions)!));
    }

    [Fact]
    public async Task Test_ProxyUpstream500()
    {
        // 500 Internal Server Error from upstream provider through the proxy
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/proxy_upstream_500";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);
        await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.Chat(JsonSerializer.Deserialize<ChatCompletionRequest>("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}", ConfigOptions)!));
    }
}
