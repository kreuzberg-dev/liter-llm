// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cf581fe63ac4956294f547a41447c7e1337c5a76b75d451f7add058314714ac8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using LiterLlm;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: rerank.</summary>
public class RerankTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public async Task Test_EdgeRerankEmptyQuery()
    {
        // Reranking with an empty query string
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/edge_rerank_empty_query";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Rerank(null);
        Assert.Equal(2, result.Results.Count);
    }

    [Fact]
    public async Task Test_EdgeRerankSingleDoc()
    {
        // Reranking with only a single document
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/edge_rerank_single_doc";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Rerank(null);
        Assert.Equal(1, result.Results.Count);
        Assert.True(result.Results["0"].RelevanceScore > 0.9d, "expected > 0.9d");
    }

    [Fact]
    public async Task Test_ErrorRerankAuth401()
    {
        // 401 Unauthorized for reranking with invalid API key
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_rerank_auth_401";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        await Assert.ThrowsAsync<LiterLlmException>(() => client.Rerank(null));
    }

    [Fact]
    public async Task Test_ErrorRerankBadRequest()
    {
        // 400 Bad Request for reranking with invalid model
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_rerank_bad_request";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        await Assert.ThrowsAsync<LiterLlmException>(() => client.Rerank(null));
    }

    [Fact]
    public async Task Test_SmokeRerankBasic()
    {
        // Basic reranking of documents against a query
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_rerank_basic";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Rerank(null);
        Assert.Equal(3, result.Results.Count);
        Assert.True(result.Results["0"].RelevanceScore > 0.9d, "expected > 0.9d");
    }

    [Fact]
    public async Task Test_SmokeRerankReturnDocs()
    {
        // Reranking with return_documents flag to include document text
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_rerank_return_docs";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Rerank(null);
        Assert.Equal(2, result.Results.Count);
        Assert.False(string.IsNullOrEmpty(result.Results["0"].Document?.ToString()));
        Assert.True(result.Results["0"].RelevanceScore > 0.9d, "expected > 0.9d");
    }

    [Fact]
    public async Task Test_SmokeRerankWithTopN()
    {
        // Reranking with top_n parameter to limit results
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_rerank_with_top_n";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Rerank(null);
        Assert.Equal(2, result.Results.Count);
        Assert.True(result.Results["0"].RelevanceScore > 0.9d, "expected > 0.9d");
    }
}
