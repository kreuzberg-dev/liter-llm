// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d0eac07b4fad4f0a309c4dbe6417573591b266fd7fad621ff793c2113ed4bddd
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using LiterLlm;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: speech.</summary>
public class SpeechTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public async Task Test_EdgeSpeechAllVoices()
    {
        // Text-to-speech with specific voice selection
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/edge_speech_all_voices";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Speech("Hello world");
        Assert.False(string.IsNullOrEmpty(result.Audio?.ToString()));
    }

    [Fact]
    public async Task Test_EdgeSpeechLongInput()
    {
        // Speech generation with a very long input text
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/edge_speech_long_input";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Speech("This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.");
        Assert.False(string.IsNullOrEmpty(result.Audio?.ToString()));
    }

    [Fact]
    public async Task Test_ErrorSpeechAuth401()
    {
        // 401 Unauthorized for speech generation with invalid API key
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_speech_auth_401";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        await Assert.ThrowsAsync<LiterLlmException>(() => client.Speech("Hello"));
    }

    [Fact]
    public async Task Test_ErrorSpeechBadModel()
    {
        // 400 Bad Request for speech with unsupported model
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_speech_bad_model";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        await Assert.ThrowsAsync<LiterLlmException>(() => client.Speech("Hello"));
    }

    [Fact]
    public async Task Test_SmokeSpeechBasic()
    {
        // Basic text-to-speech generation
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_speech_basic";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Speech("Hello, world!");
        Assert.False(string.IsNullOrEmpty(result.Audio?.ToString()));
    }

    [Fact]
    public async Task Test_SmokeSpeechMp3Format()
    {
        // Text-to-speech with explicit MP3 response format
        var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_speech_mp3_format";
        var client = LiterLlmLib.CreateClient("test-key", baseUrl);
        var result = await client.Speech("The quick brown fox jumps over the lazy dog.");
        Assert.False(string.IsNullOrEmpty(result.Audio?.ToString()));
    }
}
