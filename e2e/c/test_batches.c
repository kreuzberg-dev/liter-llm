/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Attempt to cancel an already-cancelled batch */
static void test_edge_batch_already_cancelled(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":null,\"message\":\"Batch batch-cancelled001 is "
      "already in terminal state 'cancelled' and cannot be "
      "cancelled.\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches/batch-placeholder", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"batch_id\":\"batch-cancelled001\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 400L, "test_edge_batch_already_cancelled");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* List batches when no batches exist */
static void test_edge_batch_empty_list(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"data\":[],\"has_more\":false,\"object\":\"list\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches", base_url);

    LiterLlmResponse *resp = liter_llm_http_get(url);
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_edge_batch_empty_list");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* 401 Unauthorized when creating a batch with invalid API key */
static void test_error_batch_auth_401(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API "
      "key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/"
             "completions\",\"input_file_id\":\"file-abc123\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 401L, "test_error_batch_auth_401");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 400 Bad Request when creating a batch with invalid input file */
static void test_error_batch_invalid_file(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":null,\"message\":\"The file 'file-wrong-purpose' "
      "has purpose 'fine-tune', but batch requires "
      "'batch'.\",\"param\":\"input_file_id\",\"type\":\"invalid_request_"
      "error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/"
             "completions\",\"input_file_id\":\"file-wrong-purpose\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 400L, "test_error_batch_invalid_file");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 404 Not Found when retrieving a nonexistent batch */
static void test_error_batch_not_found(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":null,\"message\":\"No such Batch object: "
      "batch-nonexistent\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches/batch-placeholder", base_url);

    LiterLlmResponse *resp = liter_llm_http_get(url);
    assert(resp != NULL);
    liter_llm_assert_status(resp, 404L, "test_error_batch_not_found");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* Retrieve a completed batch job with output file */
static void test_smoke_batch_completed(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"completed_at\":1711003600,\"completion_window\":\"24h\",\"created_"
      "at\":1711000000,\"endpoint\":\"/v1/chat/"
      "completions\",\"id\":\"batch-ghi789\",\"in_progress_at\":1711000100,"
      "\"input_file_id\":\"file-input001\",\"object\":\"batch\",\"output_file_"
      "id\":\"file-output001\",\"request_counts\":{\"completed\":98,\"failed\":"
      "2,\"total\":100},\"status\":\"completed\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches/batch-placeholder", base_url);

    LiterLlmResponse *resp = liter_llm_http_get(url);
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_batch_completed");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Cancel a running batch job */
static void test_smoke_cancel_batch(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"cancelling_at\":1711000400,\"created_at\":1711000200,\"endpoint\":\"/"
      "v1/chat/"
      "completions\",\"id\":\"batch-def456\",\"object\":\"batch\",\"request_"
      "counts\":{\"completed\":30,\"failed\":0,\"total\":100},\"status\":"
      "\"cancelling\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches/batch-placeholder", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"batch_id\":\"batch-def456\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_cancel_batch");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Create a new batch processing job */
static void test_smoke_create_batch(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"completion_window\":\"24h\",\"created_at\":1711000000,\"endpoint\":"
      "\"/v1/chat/"
      "completions\",\"id\":\"batch-abc123\",\"input_file_id\":\"file-abc123\","
      "\"object\":\"batch\",\"request_counts\":{\"completed\":0,\"failed\":0,"
      "\"total\":0},\"status\":\"validating\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/"
             "completions\",\"input_file_id\":\"file-abc123\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_create_batch");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* List all batch jobs */
static void test_smoke_list_batches(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"data\":[{\"created_at\":1711000000,\"endpoint\":\"/v1/chat/"
      "completions\",\"id\":\"batch-abc123\",\"object\":\"batch\",\"status\":"
      "\"completed\"},{\"created_at\":1711000200,\"endpoint\":\"/v1/"
      "embeddings\",\"id\":\"batch-def456\",\"object\":\"batch\",\"status\":"
      "\"in_progress\"}],\"has_more\":false,\"object\":\"list\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches", base_url);

    LiterLlmResponse *resp = liter_llm_http_get(url);
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_list_batches");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Retrieve the status of a batch job */
static void test_smoke_retrieve_batch(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"completion_window\":\"24h\",\"created_at\":1711000000,\"endpoint\":"
      "\"/v1/chat/"
      "completions\",\"id\":\"batch-abc123\",\"in_progress_at\":1711000100,"
      "\"input_file_id\":\"file-abc123\",\"object\":\"batch\",\"request_"
      "counts\":{\"completed\":45,\"failed\":2,\"total\":100},\"status\":\"in_"
      "progress\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/batches/batch-placeholder", base_url);

    LiterLlmResponse *resp = liter_llm_http_get(url);
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_retrieve_batch");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

int main(void) {
  test_edge_batch_already_cancelled();
  printf("PASS: Attempt to cancel an already-cancelled batch\n");
  test_edge_batch_empty_list();
  printf("PASS: List batches when no batches exist\n");
  test_error_batch_auth_401();
  printf("PASS: 401 Unauthorized when creating a batch with invalid API key\n");
  test_error_batch_invalid_file();
  printf(
      "PASS: 400 Bad Request when creating a batch with invalid input file\n");
  test_error_batch_not_found();
  printf("PASS: 404 Not Found when retrieving a nonexistent batch\n");
  test_smoke_batch_completed();
  printf("PASS: Retrieve a completed batch job with output file\n");
  test_smoke_cancel_batch();
  printf("PASS: Cancel a running batch job\n");
  test_smoke_create_batch();
  printf("PASS: Create a new batch processing job\n");
  test_smoke_list_batches();
  printf("PASS: List all batch jobs\n");
  test_smoke_retrieve_batch();
  printf("PASS: Retrieve the status of a batch job\n");
  printf("All batches tests passed.\n");
  return 0;
}
