/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:cef2db22df8e4ef5d9035da9fb1b20922e4d11f5f20f2e54377d02d811a7c5f4
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: search */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_search_basic(void) {
    /* Basic web search request with a simple query */
    LITERLLMAlefHandle search_request_handle = literllm_search_request_from_json("{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}");
    assert(search_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/search_basic", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_search(client, search_request_handle);
    assert(result != 0 && "expected call to succeed");
    literllm_search_response_free(result);
    literllm_search_request_free(search_request_handle);
    literllm_default_client_free(client);
}

void test_search_empty_results(void) {
    /* Web search with a query that returns no results */
    LITERLLMAlefHandle search_request_handle = literllm_search_request_from_json("{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}");
    assert(search_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/search_empty_results", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_search(client, search_request_handle);
    assert(result != 0 && "expected call to succeed");
    literllm_search_response_free(result);
    literllm_search_request_free(search_request_handle);
    literllm_default_client_free(client);
}

void test_search_error_400(void) {
    /* 400 Bad Request error when search query is empty */
    LITERLLMAlefHandle search_request_handle = literllm_search_request_from_json("{\"model\":\"brave/web-search\",\"query\":\"\"}");
    if (search_request_handle == 0) { ALEF_TEST_PASS(); }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/search_error_400", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_search(client, search_request_handle);
    assert(result == 0 && "expected call to fail");
    // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    literllm_search_request_free(search_request_handle);
    literllm_default_client_free(client);
}

void test_search_error_401(void) {
    /* 401 Unauthorized error on web search due to invalid API credentials */
    LITERLLMAlefHandle search_request_handle = literllm_search_request_from_json("{\"model\":\"brave/web-search\",\"query\":\"test\"}");
    if (search_request_handle == 0) { ALEF_TEST_PASS(); }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/search_error_401", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_search(client, search_request_handle);
    assert(result == 0 && "expected call to fail");
    // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    literllm_search_request_free(search_request_handle);
    literllm_default_client_free(client);
}

void test_search_with_max_results(void) {
    /* Search request with max_results parameter limiting response count */
    LITERLLMAlefHandle search_request_handle = literllm_search_request_from_json("{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}");
    assert(search_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/search_with_max_results", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_search(client, search_request_handle);
    assert(result != 0 && "expected call to succeed");
    char* results = literllm_search_response_results(result);
    {
        /* count_equals: count elements in array */
        assert(results != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(results);
        assert(elem_count == 2 && "expected 2 elements");
    }
    literllm_free_string(results);
    literllm_search_response_free(result);
    literllm_search_request_free(search_request_handle);
    literllm_default_client_free(client);
}
