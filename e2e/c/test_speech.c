/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:d5d6906b534a893ba81d10787de163d645f009bc8d60e33b6427b276d1f4de3c
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: speech */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_edge_speech_all_voices(void) {
    /* Text-to-speech with specific voice selection */
    LITERLLMAlefHandle create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello world\",\"model\":\"tts-1\",\"voice\":\"nova\"}");
    assert(create_speech_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_speech_all_voices", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
}

void test_edge_speech_long_input(void) {
    /* Speech generation with a very long input text */
    LITERLLMAlefHandle create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.\",\"model\":\"tts-1\",\"voice\":\"echo\"}");
    assert(create_speech_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_speech_long_input", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
}

void test_error_speech_auth_401(void) {
    /* 401 Unauthorized for speech generation with invalid API key */
    LITERLLMAlefHandle create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello\",\"model\":\"tts-1\",\"voice\":\"alloy\"}");
    if (create_speech_request_handle == 0) { ALEF_TEST_PASS(); }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_speech_auth_401", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status != 0 && "expected call to fail");
    // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
    literllm_free_bytes(out_ptr, out_len, out_cap);
}

void test_error_speech_bad_model(void) {
    /* 400 Bad Request for speech with unsupported model */
    LITERLLMAlefHandle create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello\",\"model\":\"tts-nonexistent\",\"voice\":\"alloy\"}");
    if (create_speech_request_handle == 0) { ALEF_TEST_PASS(); }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_speech_bad_model", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status != 0 && "expected call to fail");
    // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
    literllm_free_bytes(out_ptr, out_len, out_cap);
}

void test_smoke_speech_basic(void) {
    /* Basic text-to-speech generation */
    LITERLLMAlefHandle create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello, world!\",\"model\":\"tts-1\",\"voice\":\"alloy\"}");
    assert(create_speech_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_speech_basic", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
}

void test_smoke_speech_mp3_format(void) {
    /* Text-to-speech with explicit MP3 response format */
    LITERLLMAlefHandle create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"The quick brown fox jumps over the lazy dog.\",\"model\":\"tts-1-hd\",\"response_format\":\"mp3\",\"speed\":1.0,\"voice\":\"nova\"}");
    assert(create_speech_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_speech_mp3_format", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
}
