/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Speech generation with a very long input text */
static void test_edge_speech_long_input(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"_binary\":true,\"_content_type\":\"audio/"
      "mpeg\",\"_description\":\"Mock binary audio data for long text\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/speech", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url,
        "{\"input\":\"This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. This is a long input text. This is a long input text. "
        "This is a long input text. This is a long input text. This is a long "
        "input text. End of input.\",\"model\":\"tts-1\",\"voice\":\"echo\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_edge_speech_long_input");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* 401 Unauthorized for speech generation with invalid API key */
static void test_error_speech_auth_401(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API "
      "key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/speech", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"Hello\",\"model\":\"tts-1\",\"voice\":\"alloy\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 401L, "test_error_speech_auth_401");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 400 Bad Request for speech with unsupported model */
static void test_error_speech_bad_model(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"model_not_found\",\"message\":\"The model "
      "'tts-nonexistent' does not "
      "exist.\",\"param\":\"model\",\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/speech", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"input\":\"Hello\",\"model\":\"tts-"
                                 "nonexistent\",\"voice\":\"alloy\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 400L, "test_error_speech_bad_model");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* Basic text-to-speech generation */
static void test_smoke_speech_basic(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body = "{\"_binary\":true,\"_content_type\":\"audio/"
                          "mpeg\",\"_description\":\"Mock binary audio data\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/speech", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"Hello, "
             "world!\",\"model\":\"tts-1\",\"voice\":\"alloy\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_speech_basic");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Text-to-speech with explicit MP3 response format */
static void test_smoke_speech_mp3_format(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body = "{\"_binary\":true,\"_content_type\":\"audio/"
                          "mpeg\",\"_description\":\"Mock MP3 audio data\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/speech", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"The quick brown fox jumps over the lazy "
             "dog.\",\"model\":\"tts-1-hd\",\"response_format\":\"mp3\","
             "\"speed\":1.0,\"voice\":\"nova\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_speech_mp3_format");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

int main(void) {
  test_edge_speech_long_input();
  printf("PASS: Speech generation with a very long input text\n");
  test_error_speech_auth_401();
  printf("PASS: 401 Unauthorized for speech generation with invalid API key\n");
  test_error_speech_bad_model();
  printf("PASS: 400 Bad Request for speech with unsupported model\n");
  test_smoke_speech_basic();
  printf("PASS: Basic text-to-speech generation\n");
  test_smoke_speech_mp3_format();
  printf("PASS: Text-to-speech with explicit MP3 response format\n");
  printf("All speech tests passed.\n");
  return 0;
}
