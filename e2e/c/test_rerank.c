/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Reranking with an empty query string */
static void test_edge_rerank_empty_query(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"rerank-empty001\",\"meta\":{\"api_version\":{\"version\":"
      "\"2\"},\"billed_units\":{\"search_units\":1}},\"results\":[{\"index\":0,"
      "\"relevance_score\":0.5},{\"index\":1,\"relevance_score\":0.48}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/rerank", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"documents\":[\"Some document\",\"Another "
             "document\"],\"model\":\"rerank-v3.5\",\"query\":\"\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_edge_rerank_empty_query");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Reranking with only a single document */
static void test_edge_rerank_single_doc(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"rerank-single001\",\"meta\":{\"api_version\":{\"version\":"
      "\"2\"},\"billed_units\":{\"search_units\":1}},\"results\":[{\"index\":0,"
      "\"relevance_score\":0.98}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/rerank", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url,
        "{\"documents\":[\"Artificial intelligence is the simulation of human "
        "intelligence.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is "
        "AI?\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_edge_rerank_single_doc");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* 401 Unauthorized for reranking with invalid API key */
static void test_error_rerank_auth_401(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API "
      "key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/rerank", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"documents\":[\"doc1\"],\"model\":\"rerank-"
                                 "v3.5\",\"query\":\"test\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 401L, "test_error_rerank_auth_401");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 400 Bad Request for reranking with invalid model */
static void test_error_rerank_bad_request(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"model_not_found\",\"message\":\"The model "
      "'nonexistent-rerank' does not "
      "exist.\",\"param\":\"model\",\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/rerank", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"documents\":[\"doc1\"],\"model\":"
                                 "\"nonexistent-rerank\",\"query\":\"test\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 400L, "test_error_rerank_bad_request");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* Basic reranking of documents against a query */
static void test_smoke_rerank_basic(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"rerank-abc123\",\"meta\":{\"api_version\":{\"version\":\"2\"},"
      "\"billed_units\":{\"search_units\":1}},\"results\":[{\"index\":0,"
      "\"relevance_score\":0.95},{\"index\":2,\"relevance_score\":0.82},{"
      "\"index\":1,\"relevance_score\":0.05}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/rerank", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"documents\":[\"Machine learning is a subset of AI.\",\"The "
             "weather is sunny today.\",\"Deep learning uses neural "
             "networks.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is "
             "machine learning?\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_rerank_basic");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Reranking with return_documents flag to include document text */
static void test_smoke_rerank_return_docs(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"rerank-ghi789\",\"meta\":{\"api_version\":{\"version\":\"2\"},"
      "\"billed_units\":{\"search_units\":1}},\"results\":[{\"document\":{"
      "\"text\":\"Rust is a systems programming "
      "language.\"},\"index\":0,\"relevance_score\":0.96},{\"document\":{"
      "\"text\":\"Iron rusts when exposed to "
      "water.\"},\"index\":1,\"relevance_score\":0.12}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/rerank", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"documents\":[\"Rust is a systems programming "
             "language.\",\"Iron rusts when exposed to "
             "water.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is "
             "Rust?\",\"return_documents\":true}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_rerank_return_docs");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Reranking with top_n parameter to limit results */
static void test_smoke_rerank_with_top_n(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"rerank-def456\",\"meta\":{\"api_version\":{\"version\":\"2\"},"
      "\"billed_units\":{\"search_units\":1}},\"results\":[{\"index\":0,"
      "\"relevance_score\":0.97},{\"index\":2,\"relevance_score\":0.91}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/rerank", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url,
        "{\"documents\":[\"Python is a programming language.\",\"Cats are cute "
        "animals.\",\"Python was created by Guido van Rossum.\",\"The sun is a "
        "star.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is "
        "Python?\",\"top_n\":2}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_rerank_with_top_n");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

int main(void) {
  test_edge_rerank_empty_query();
  printf("PASS: Reranking with an empty query string\n");
  test_edge_rerank_single_doc();
  printf("PASS: Reranking with only a single document\n");
  test_error_rerank_auth_401();
  printf("PASS: 401 Unauthorized for reranking with invalid API key\n");
  test_error_rerank_bad_request();
  printf("PASS: 400 Bad Request for reranking with invalid model\n");
  test_smoke_rerank_basic();
  printf("PASS: Basic reranking of documents against a query\n");
  test_smoke_rerank_return_docs();
  printf(
      "PASS: Reranking with return_documents flag to include document text\n");
  test_smoke_rerank_with_top_n();
  printf("PASS: Reranking with top_n parameter to limit results\n");
  printf("All rerank tests passed.\n");
  return 0;
}
