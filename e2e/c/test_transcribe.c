/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Transcription of a silent or empty audio file returns empty text */
static void test_edge_transcribe_empty_audio(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body = "{\"text\":\"\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/transcriptions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"file\":\"silence.mp3\",\"model\":\"whisper-1\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_edge_transcribe_empty_audio");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* 401 Unauthorized for transcription with invalid API key */
static void test_error_transcribe_auth_401(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API "
      "key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/transcriptions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 401L, "test_error_transcribe_auth_401");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 400 Bad Request when audio format is unsupported */
static void test_error_transcribe_bad_format(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":null,\"message\":\"Invalid file format. Supported "
      "formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, "
      "webm.\",\"param\":\"file\",\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/transcriptions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"file\":\"audio.xyz\",\"model\":\"whisper-1\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 400L, "test_error_transcribe_bad_format");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* Basic audio transcription */
static void test_smoke_transcribe_basic(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body = "{\"text\":\"Hello, this is a test transcription.\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/transcriptions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_transcribe_basic");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Audio transcription with explicit language hint */
static void test_smoke_transcribe_with_language(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"text\":\"Hallo, dies ist ein Testtranskription.\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/audio/transcriptions", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"file\":\"audio_de.mp3\",\"language\":"
                                 "\"de\",\"model\":\"whisper-1\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_transcribe_with_language");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

int main(void) {
  test_edge_transcribe_empty_audio();
  printf("PASS: Transcription of a silent or empty audio file returns empty "
         "text\n");
  test_error_transcribe_auth_401();
  printf("PASS: 401 Unauthorized for transcription with invalid API key\n");
  test_error_transcribe_bad_format();
  printf("PASS: 400 Bad Request when audio format is unsupported\n");
  test_smoke_transcribe_basic();
  printf("PASS: Basic audio transcription\n");
  test_smoke_transcribe_with_language();
  printf("PASS: Audio transcription with explicit language hint\n");
  printf("All transcribe tests passed.\n");
  return 0;
}
