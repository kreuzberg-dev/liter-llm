/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Moderation response with multiple categories flagged */
static void test_edge_moderate_all_categories(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"modr-multi001\",\"model\":\"omni-moderation-latest\","
      "\"results\":[{\"categories\":{\"harassment\":true,\"hate\":true,\"self-"
      "harm\":false,\"sexual\":false,\"violence\":true,\"violence/"
      "graphic\":true},\"category_scores\":{\"harassment\":0.9,\"hate\":0.85,"
      "\"self-harm\":0.02,\"sexual\":0.01,\"violence\":0.95,\"violence/"
      "graphic\":0.78},\"flagged\":true}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/moderations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"Extremely harmful content targeting multiple "
             "categories\",\"model\":\"omni-moderation-latest\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_edge_moderate_all_categories");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Moderation with empty string input */
static void test_edge_moderate_empty_input(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"modr-empty001\",\"model\":\"omni-moderation-latest\","
      "\"results\":[{\"categories\":{\"harassment\":false,\"hate\":false,"
      "\"sexual\":false,\"violence\":false},\"category_scores\":{"
      "\"harassment\":0.0,\"hate\":0.0,\"sexual\":0.0,\"violence\":0.0},"
      "\"flagged\":false}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/moderations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"\",\"model\":\"omni-moderation-latest\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_edge_moderate_empty_input");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* 401 Unauthorized for moderation with invalid API key */
static void test_error_moderate_auth_401(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API "
      "key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/moderations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"Hello\",\"model\":\"omni-moderation-latest\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 401L, "test_error_moderate_auth_401");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 400 Bad Request for moderation with invalid model */
static void test_error_moderate_bad_request(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"model_not_found\",\"message\":\"The model "
      "'nonexistent-moderation' does not "
      "exist.\",\"param\":\"model\",\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/moderations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"Hello\",\"model\":\"nonexistent-moderation\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 400L, "test_error_moderate_bad_request");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* Moderate multiple inputs in a single request */
static void test_smoke_moderate_batch(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"modr-def456\",\"model\":\"omni-moderation-latest\","
      "\"results\":[{\"categories\":{\"harassment\":false,\"hate\":false,"
      "\"sexual\":false,\"violence\":false},\"category_scores\":{"
      "\"harassment\":0.0001,\"hate\":0.0001,\"sexual\":0.0001,\"violence\":0."
      "0001},\"flagged\":false},{\"categories\":{\"harassment\":false,\"hate\":"
      "false,\"sexual\":false,\"violence\":false},\"category_scores\":{"
      "\"harassment\":0.0001,\"hate\":0.0001,\"sexual\":0.0001,\"violence\":0."
      "0001},\"flagged\":false}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/moderations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":[\"Hello world\",\"Nice weather "
             "today\"],\"model\":\"omni-moderation-latest\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_moderate_batch");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Moderation detects flagged content */
static void test_smoke_moderate_flagged(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"modr-ghi789\",\"model\":\"omni-moderation-latest\","
      "\"results\":[{\"categories\":{\"harassment\":false,\"hate\":false,"
      "\"sexual\":false,\"violence\":true},\"category_scores\":{\"harassment\":"
      "0.15,\"hate\":0.01,\"sexual\":0.0001,\"violence\":0.92},\"flagged\":"
      "true}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/moderations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"I want to hurt someone very "
             "badly\",\"model\":\"omni-moderation-latest\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_moderate_flagged");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Moderate a single non-flagged input */
static void test_smoke_moderate_single(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"id\":\"modr-abc123\",\"model\":\"omni-moderation-latest\","
      "\"results\":[{\"categories\":{\"harassment\":false,\"harassment/"
      "threatening\":false,\"hate\":false,\"hate/"
      "threatening\":false,\"self-harm\":false,\"self-harm/"
      "instructions\":false,\"self-harm/"
      "intent\":false,\"sexual\":false,\"sexual/"
      "minors\":false,\"violence\":false,\"violence/"
      "graphic\":false},\"category_scores\":{\"harassment\":0.0002,"
      "\"harassment/threatening\":0.0001,\"hate\":0.0001,\"hate/"
      "threatening\":0.0001,\"self-harm\":0.0001,\"self-harm/"
      "instructions\":0.0001,\"self-harm/"
      "intent\":0.0001,\"sexual\":0.0001,\"sexual/"
      "minors\":0.0001,\"violence\":0.0001,\"violence/"
      "graphic\":0.0001},\"flagged\":false}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/moderations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"input\":\"The weather is nice "
             "today.\",\"model\":\"omni-moderation-latest\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_moderate_single");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

int main(void) {
  test_edge_moderate_all_categories();
  printf("PASS: Moderation response with multiple categories flagged\n");
  test_edge_moderate_empty_input();
  printf("PASS: Moderation with empty string input\n");
  test_error_moderate_auth_401();
  printf("PASS: 401 Unauthorized for moderation with invalid API key\n");
  test_error_moderate_bad_request();
  printf("PASS: 400 Bad Request for moderation with invalid model\n");
  test_smoke_moderate_batch();
  printf("PASS: Moderate multiple inputs in a single request\n");
  test_smoke_moderate_flagged();
  printf("PASS: Moderation detects flagged content\n");
  test_smoke_moderate_single();
  printf("PASS: Moderate a single non-flagged input\n");
  printf("All moderate tests passed.\n");
  return 0;
}
