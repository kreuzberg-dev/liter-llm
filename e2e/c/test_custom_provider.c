/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:1b7959dc5e2bbe4993c6539ad03ce1f58880672e18776639631fc3a85f630999
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: custom_provider */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_provider_auth(void) {
    /* Tests custom provider with custom auth header */
    LITERLLMAlefHandle chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"my-auth-model-v1\"}");
    assert(chat_completion_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/provider_auth", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_chat(client, chat_completion_request_handle);
    assert(result != 0 && "expected call to succeed");
    literllm_chat_completion_response_free(result);
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
}

void test_register_provider(void) {
    /* Tests that a custom provider can be registered and routes requests */
    LITERLLMAlefHandle chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"my-model-v1\"}");
    assert(chat_completion_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/register_provider", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_chat(client, chat_completion_request_handle);
    assert(result != 0 && "expected call to succeed");
    literllm_chat_completion_response_free(result);
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
}
