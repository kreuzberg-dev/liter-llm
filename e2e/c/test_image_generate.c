/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Image generation returning base64-encoded data instead of URL */
static void test_edge_image_b64_response(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"created\":1711000000,\"data\":[{\"b64_json\":"
      "\"iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+"
      "M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==\",\"revised_prompt\":\"A perfect "
      "blue circle on a white background\"}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/images/generations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url,
        "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue "
        "circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_edge_image_b64_response");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Image generation with an empty prompt returns 400 */
static void test_edge_image_empty_prompt(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":null,\"message\":\"You must provide a "
      "prompt.\",\"param\":\"prompt\",\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/images/generations", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":"
                                 "\"\",\"size\":\"1024x1024\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 400L, "test_edge_image_empty_prompt");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 401 Unauthorized when generating images with invalid API key */
static void test_error_image_auth_401(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API "
      "key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/images/generations", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":"
                                 "\"A cat\",\"size\":\"1024x1024\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 401L, "test_error_image_auth_401");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 400 Bad Request when image generation parameters are invalid */
static void test_error_image_bad_request(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":null,\"message\":\"Invalid value for 'size': "
      "'9999x9999'. Supported values are: '1024x1024', '1792x1024', "
      "'1024x1792'.\",\"param\":\"size\",\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/images/generations", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":"
                                 "\"A cat\",\"size\":\"9999x9999\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 400L, "test_error_image_bad_request");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* 429 Rate limit exceeded for image generation */
static void test_error_image_rate_limit(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"rate_limit_exceeded\",\"message\":\"Rate limit "
      "reached for images per minute. Limit: 5, Used: 5, Requested: "
      "1.\",\"param\":null,\"type\":\"rate_limit_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/images/generations", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":"
                                 "\"A cat\",\"size\":\"1024x1024\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 429L, "test_error_image_rate_limit");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

/* Basic image generation with a text prompt */
static void test_smoke_image_basic(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"created\":1711000000,\"data\":[{\"revised_prompt\":\"A fluffy white "
      "cat sitting peacefully on a sunlit "
      "windowsill\",\"url\":\"https://example.com/images/img-abc123.png\"}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/images/generations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting "
             "on a windowsill\",\"size\":\"1024x1024\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_image_basic");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Image generation requesting multiple images */
static void test_smoke_image_multiple(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"created\":1711000000,\"data\":[{\"url\":\"https://example.com/images/"
      "img-001.png\"},{\"url\":\"https://example.com/images/"
      "img-002.png\"},{\"url\":\"https://example.com/images/img-003.png\"}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/images/generations", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":"
                                 "\"A red bicycle\",\"size\":\"256x256\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_image_multiple");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

/* Image generation with explicit size parameter */
static void test_smoke_image_with_size(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"created\":1711000000,\"data\":[{\"revised_prompt\":\"A breathtaking "
      "sunset over snow-capped "
      "mountains\",\"url\":\"https://example.com/images/img-def456.png\"}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/images/generations", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over "
             "mountains\",\"size\":\"1792x1024\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_smoke_image_with_size");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

int main(void) {
  test_edge_image_b64_response();
  printf(
      "PASS: Image generation returning base64-encoded data instead of URL\n");
  test_edge_image_empty_prompt();
  printf("PASS: Image generation with an empty prompt returns 400\n");
  test_error_image_auth_401();
  printf(
      "PASS: 401 Unauthorized when generating images with invalid API key\n");
  test_error_image_bad_request();
  printf(
      "PASS: 400 Bad Request when image generation parameters are invalid\n");
  test_error_image_rate_limit();
  printf("PASS: 429 Rate limit exceeded for image generation\n");
  test_smoke_image_basic();
  printf("PASS: Basic image generation with a text prompt\n");
  test_smoke_image_multiple();
  printf("PASS: Image generation requesting multiple images\n");
  test_smoke_image_with_size();
  printf("PASS: Image generation with explicit size parameter\n");
  printf("All image_generate tests passed.\n");
  return 0;
}
