// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f02816c8d2fa5de4d27b2f8555761a34cff602bff8a7ba57f426498be869e50c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { createClient } from "liter_llm";

describe("list-models", () => {
  it("empty_model_list: List models response returns an empty data array when no models are available", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/empty_model_list`,
    );
    const result = await client.listModels();
    expect(result.data.length).toBeGreaterThanOrEqual(0);
    expect(result.data.length).toBe(0);
  });

  it("list_models_error_401: 401 Unauthorized error on list models request when API key is invalid", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/list_models_error_401`,
    );
    await expect(async () => {
      await client.listModels();
    }).rejects.toThrow();
  });

  it("list_models_error_500: 500 Internal Server Error when listing models due to server failure", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/list_models_error_500`,
    );
    await expect(async () => {
      await client.listModels();
    }).rejects.toThrow();
  });

  it("list_models_filtered: List models response with multiple model objects", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/list_models_filtered`,
    );
    const result = await client.listModels();
    expect(result.data.length).toBeGreaterThanOrEqual(5);
    expect(result.data["0"].object.trim()).toBe("model");
  });
});
