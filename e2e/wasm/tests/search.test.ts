// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a62c0a0ef75435067db9165b9b7d54bf88b00078f6a3c4407c1e9148a8132ad1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import init, { createClient, type WasmChatCompletionRequest } from "liter_llm";
await init(
  (await import("node:fs")).readFileSync(
    (await import("node:module"))
      .createRequire(import.meta.url)
      .resolve("liter_llm")
      .replace(/\.js$/, "_bg.wasm"),
  ),
);

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("search", () => {
  it("search_basic: Basic web search request with a simple query", async () => {
    const client = createClient("test-key", `${process.env.MOCK_SERVER_URL}/fixtures/search_basic`);
    await client.search({
      model: "brave/web-search",
      query: "What is Rust programming language?",
    } as WasmChatCompletionRequest);
  });

  it("search_empty_results: Web search with a query that returns no results", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/search_empty_results`,
    );
    await client.search({
      model: "brave/web-search",
      query: "xyznonexistent12345xyz",
    } as WasmChatCompletionRequest);
  });

  it("search_error_400: 400 Bad Request error when search query is empty", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/search_error_400`,
    );
    await expect(async () => {
      await client.search({ model: "brave/web-search", query: "" } as WasmChatCompletionRequest);
    }).rejects.toThrow();
  });

  it("search_error_401: 401 Unauthorized error on web search due to invalid API credentials", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/search_error_401`,
    );
    await expect(async () => {
      await client.search({
        model: "brave/web-search",
        query: "test",
      } as WasmChatCompletionRequest);
    }).rejects.toThrow();
  });

  it("search_with_max_results: Search request with max_results parameter limiting response count", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/search_with_max_results`,
    );
    const result = await client.search({
      maxResults: 2,
      model: "brave/web-search",
      query: "Rust programming",
    } as WasmChatCompletionRequest);
    expect(result.results.length).toBe(2);
  });
});
