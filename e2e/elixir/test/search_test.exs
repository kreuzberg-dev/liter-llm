# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:03bc8b7593b5dbaaf3d4a79b4f7867cc5112a33e9c752c59648c6807d53ba848
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: search
defmodule E2e.SearchTest do
  use ExUnit.Case, async: false

  describe "search_basic" do
    test "search_basic" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/search_basic")
      {:ok, result} = LiterLlm.search_async(client, nil)
    end
  end

  describe "search_empty_results" do
    test "search_empty_results" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/search_empty_results")
      {:ok, result} = LiterLlm.search_async(client, nil)
    end
  end

  describe "search_error_400" do
    test "search_error_400" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/search_error_400")
      assert {:error, _} = LiterLlm.search_async(client, nil)
    end
  end

  describe "search_error_401" do
    test "search_error_401" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/search_error_401")
      assert {:error, _} = LiterLlm.search_async(client, nil)
    end
  end

  describe "search_with_max_results" do
    test "search_with_max_results" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/search_with_max_results")
      {:ok, result} = LiterLlm.search_async(client, nil)
      assert length(result.results) == 2
    end
  end
end
