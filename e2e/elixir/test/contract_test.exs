# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:0d650f94e7f51e8c9a6d694bf1beab0c59a6d8a86eae68faf194290a140a5266
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: contract
defmodule E2e.ContractTest do
  use ExUnit.Case, async: false

  describe "binding_api_parity" do
    test "binding_api_parity" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/binding_api_parity")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(result.choices["0"].message.content) == "OK"
      assert String.trim(result.choices["0"].finish_reason) == "stop"
      assert result.usage.total_tokens == 6
      assert String.trim(result.model) == "gpt-4o"
    end
  end

  describe "contract_ocr" do
    test "contract_ocr" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/contract_ocr")
      {:ok, result} = LiterLlm.ocr_async(client, nil)
    end
  end

  describe "contract_search" do
    test "contract_search" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/contract_search")
      {:ok, result} = LiterLlm.search_async(client, nil)
    end
  end
end
