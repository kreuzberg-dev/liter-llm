# Code generated by liter-llm-e2e-generator. DO NOT EDIT.
defmodule LiterLlmE2E.TranscribeTest do
  use ExUnit.Case, async: true

  alias LiterLlmE2E.MockServer
  alias LiterLlmE2E.Helpers

  test "Transcription of a silent or empty audio file returns empty text" do
    routes = [
      %{
        path: "/audio/transcriptions",
        method: "POST",
        status: 200,
        body: "{\"text\":\"\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/audio/transcriptions",
        body: "{\"file\":\"silence.mp3\",\"model\":\"whisper-1\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "401 Unauthorized for transcription with invalid API key" do
    routes = [
      %{
        path: "/audio/transcriptions",
        method: "POST",
        status: 401,
        body:
          "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/audio/transcriptions",
        body: "{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 401
  end

  test "400 Bad Request when audio format is unsupported" do
    routes = [
      %{
        path: "/audio/transcriptions",
        method: "POST",
        status: 400,
        body:
          "{\"error\":{\"code\":null,\"message\":\"Invalid file format. Supported formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, webm.\",\"param\":\"file\",\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/audio/transcriptions",
        body: "{\"file\":\"audio.xyz\",\"model\":\"whisper-1\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 400
  end

  test "Basic audio transcription" do
    routes = [
      %{
        path: "/audio/transcriptions",
        method: "POST",
        status: 200,
        body: "{\"text\":\"Hello, this is a test transcription.\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/audio/transcriptions",
        body: "{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Audio transcription with explicit language hint" do
    routes = [
      %{
        path: "/audio/transcriptions",
        method: "POST",
        status: 200,
        body: "{\"text\":\"Hallo, dies ist ein Testtranskription.\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/audio/transcriptions",
        body: "{\"file\":\"audio_de.mp3\",\"language\":\"de\",\"model\":\"whisper-1\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end
end
