# Code generated by liter-llm-e2e-generator. DO NOT EDIT.
defmodule LiterLlmE2E.BatchesTest do
  use ExUnit.Case, async: true

  alias LiterLlmE2E.MockServer
  alias LiterLlmE2E.Helpers

  test "Attempt to cancel an already-cancelled batch" do
    routes = [
      %{
        path: "/batches/batch-placeholder",
        method: "POST",
        status: 400,
        body:
          "{\"error\":{\"code\":null,\"message\":\"Batch batch-cancelled001 is already in terminal state 'cancelled' and cannot be cancelled.\",\"param\":null,\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/batches/batch-placeholder",
        body: "{\"batch_id\":\"batch-cancelled001\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 400
  end

  test "List batches when no batches exist" do
    routes = [
      %{
        path: "/batches",
        method: "GET",
        status: 200,
        body: "{\"data\":[],\"has_more\":false,\"object\":\"list\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/batches", decode_body: false)
    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "401 Unauthorized when creating a batch with invalid API key" do
    routes = [
      %{
        path: "/batches",
        method: "POST",
        status: 401,
        body:
          "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/batches",
        body:
          "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 401
  end

  test "400 Bad Request when creating a batch with invalid input file" do
    routes = [
      %{
        path: "/batches",
        method: "POST",
        status: 400,
        body:
          "{\"error\":{\"code\":null,\"message\":\"The file 'file-wrong-purpose' has purpose 'fine-tune', but batch requires 'batch'.\",\"param\":\"input_file_id\",\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/batches",
        body:
          "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-wrong-purpose\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 400
  end

  test "404 Not Found when retrieving a nonexistent batch" do
    routes = [
      %{
        path: "/batches/batch-placeholder",
        method: "GET",
        status: 404,
        body:
          "{\"error\":{\"code\":null,\"message\":\"No such Batch object: batch-nonexistent\",\"param\":null,\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/batches/batch-placeholder", decode_body: false)
    assert resp.status == 404
  end

  test "Retrieve a completed batch job with output file" do
    routes = [
      %{
        path: "/batches/batch-placeholder",
        method: "GET",
        status: 200,
        body:
          "{\"completed_at\":1711003600,\"completion_window\":\"24h\",\"created_at\":1711000000,\"endpoint\":\"/v1/chat/completions\",\"id\":\"batch-ghi789\",\"in_progress_at\":1711000100,\"input_file_id\":\"file-input001\",\"object\":\"batch\",\"output_file_id\":\"file-output001\",\"request_counts\":{\"completed\":98,\"failed\":2,\"total\":100},\"status\":\"completed\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/batches/batch-placeholder", decode_body: false)
    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Cancel a running batch job" do
    routes = [
      %{
        path: "/batches/batch-placeholder",
        method: "POST",
        status: 200,
        body:
          "{\"cancelling_at\":1711000400,\"created_at\":1711000200,\"endpoint\":\"/v1/chat/completions\",\"id\":\"batch-def456\",\"object\":\"batch\",\"request_counts\":{\"completed\":30,\"failed\":0,\"total\":100},\"status\":\"cancelling\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/batches/batch-placeholder",
        body: "{\"batch_id\":\"batch-def456\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Create a new batch processing job" do
    routes = [
      %{
        path: "/batches",
        method: "POST",
        status: 200,
        body:
          "{\"completion_window\":\"24h\",\"created_at\":1711000000,\"endpoint\":\"/v1/chat/completions\",\"id\":\"batch-abc123\",\"input_file_id\":\"file-abc123\",\"object\":\"batch\",\"request_counts\":{\"completed\":0,\"failed\":0,\"total\":0},\"status\":\"validating\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/batches",
        body:
          "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "List all batch jobs" do
    routes = [
      %{
        path: "/batches",
        method: "GET",
        status: 200,
        body:
          "{\"data\":[{\"created_at\":1711000000,\"endpoint\":\"/v1/chat/completions\",\"id\":\"batch-abc123\",\"object\":\"batch\",\"status\":\"completed\"},{\"created_at\":1711000200,\"endpoint\":\"/v1/embeddings\",\"id\":\"batch-def456\",\"object\":\"batch\",\"status\":\"in_progress\"}],\"has_more\":false,\"object\":\"list\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/batches", decode_body: false)
    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Retrieve the status of a batch job" do
    routes = [
      %{
        path: "/batches/batch-placeholder",
        method: "GET",
        status: 200,
        body:
          "{\"completion_window\":\"24h\",\"created_at\":1711000000,\"endpoint\":\"/v1/chat/completions\",\"id\":\"batch-abc123\",\"in_progress_at\":1711000100,\"input_file_id\":\"file-abc123\",\"object\":\"batch\",\"request_counts\":{\"completed\":45,\"failed\":2,\"total\":100},\"status\":\"in_progress\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/batches/batch-placeholder", decode_body: false)
    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end
end
