# Code generated by liter-llm-e2e-generator. DO NOT EDIT.
defmodule LiterLlmE2E.RerankTest do
  use ExUnit.Case, async: true

  alias LiterLlmE2E.MockServer
  alias LiterLlmE2E.Helpers

  test "Reranking with an empty query string" do
    routes = [
      %{
        path: "/rerank",
        method: "POST",
        status: 200,
        body:
          "{\"id\":\"rerank-empty001\",\"meta\":{\"api_version\":{\"version\":\"2\"},\"billed_units\":{\"search_units\":1}},\"results\":[{\"index\":0,\"relevance_score\":0.5},{\"index\":1,\"relevance_score\":0.48}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/rerank",
        body:
          "{\"documents\":[\"Some document\",\"Another document\"],\"model\":\"rerank-v3.5\",\"query\":\"\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Reranking with only a single document" do
    routes = [
      %{
        path: "/rerank",
        method: "POST",
        status: 200,
        body:
          "{\"id\":\"rerank-single001\",\"meta\":{\"api_version\":{\"version\":\"2\"},\"billed_units\":{\"search_units\":1}},\"results\":[{\"index\":0,\"relevance_score\":0.98}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/rerank",
        body:
          "{\"documents\":[\"Artificial intelligence is the simulation of human intelligence.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is AI?\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "401 Unauthorized for reranking with invalid API key" do
    routes = [
      %{
        path: "/rerank",
        method: "POST",
        status: 401,
        body:
          "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/rerank",
        body: "{\"documents\":[\"doc1\"],\"model\":\"rerank-v3.5\",\"query\":\"test\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 401
  end

  test "400 Bad Request for reranking with invalid model" do
    routes = [
      %{
        path: "/rerank",
        method: "POST",
        status: 400,
        body:
          "{\"error\":{\"code\":\"model_not_found\",\"message\":\"The model 'nonexistent-rerank' does not exist.\",\"param\":\"model\",\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/rerank",
        body: "{\"documents\":[\"doc1\"],\"model\":\"nonexistent-rerank\",\"query\":\"test\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 400
  end

  test "Basic reranking of documents against a query" do
    routes = [
      %{
        path: "/rerank",
        method: "POST",
        status: 200,
        body:
          "{\"id\":\"rerank-abc123\",\"meta\":{\"api_version\":{\"version\":\"2\"},\"billed_units\":{\"search_units\":1}},\"results\":[{\"index\":0,\"relevance_score\":0.95},{\"index\":2,\"relevance_score\":0.82},{\"index\":1,\"relevance_score\":0.05}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/rerank",
        body:
          "{\"documents\":[\"Machine learning is a subset of AI.\",\"The weather is sunny today.\",\"Deep learning uses neural networks.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is machine learning?\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Reranking with return_documents flag to include document text" do
    routes = [
      %{
        path: "/rerank",
        method: "POST",
        status: 200,
        body:
          "{\"id\":\"rerank-ghi789\",\"meta\":{\"api_version\":{\"version\":\"2\"},\"billed_units\":{\"search_units\":1}},\"results\":[{\"document\":{\"text\":\"Rust is a systems programming language.\"},\"index\":0,\"relevance_score\":0.96},{\"document\":{\"text\":\"Iron rusts when exposed to water.\"},\"index\":1,\"relevance_score\":0.12}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/rerank",
        body:
          "{\"documents\":[\"Rust is a systems programming language.\",\"Iron rusts when exposed to water.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Rust?\",\"return_documents\":true}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Reranking with top_n parameter to limit results" do
    routes = [
      %{
        path: "/rerank",
        method: "POST",
        status: 200,
        body:
          "{\"id\":\"rerank-def456\",\"meta\":{\"api_version\":{\"version\":\"2\"},\"billed_units\":{\"search_units\":1}},\"results\":[{\"index\":0,\"relevance_score\":0.97},{\"index\":2,\"relevance_score\":0.91}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/rerank",
        body:
          "{\"documents\":[\"Python is a programming language.\",\"Cats are cute animals.\",\"Python was created by Guido van Rossum.\",\"The sun is a star.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Python?\",\"top_n\":2}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end
end
