# Code generated by liter-llm-e2e-generator. DO NOT EDIT.
defmodule LiterLlmE2E.ImageGenerateTest do
  use ExUnit.Case, async: true

  alias LiterLlmE2E.MockServer
  alias LiterLlmE2E.Helpers

  test "Image generation returning base64-encoded data instead of URL" do
    routes = [
      %{
        path: "/images/generations",
        method: "POST",
        status: 200,
        body:
          "{\"created\":1711000000,\"data\":[{\"b64_json\":\"iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==\",\"revised_prompt\":\"A perfect blue circle on a white background\"}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/images/generations",
        body:
          "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Image generation with an empty prompt returns 400" do
    routes = [
      %{
        path: "/images/generations",
        method: "POST",
        status: 400,
        body:
          "{\"error\":{\"code\":null,\"message\":\"You must provide a prompt.\",\"param\":\"prompt\",\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/images/generations",
        body: "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 400
  end

  test "401 Unauthorized when generating images with invalid API key" do
    routes = [
      %{
        path: "/images/generations",
        method: "POST",
        status: 401,
        body:
          "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/images/generations",
        body: "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 401
  end

  test "400 Bad Request when image generation parameters are invalid" do
    routes = [
      %{
        path: "/images/generations",
        method: "POST",
        status: 400,
        body:
          "{\"error\":{\"code\":null,\"message\":\"Invalid value for 'size': '9999x9999'. Supported values are: '1024x1024', '1792x1024', '1024x1792'.\",\"param\":\"size\",\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/images/generations",
        body: "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 400
  end

  test "429 Rate limit exceeded for image generation" do
    routes = [
      %{
        path: "/images/generations",
        method: "POST",
        status: 429,
        body:
          "{\"error\":{\"code\":\"rate_limit_exceeded\",\"message\":\"Rate limit reached for images per minute. Limit: 5, Used: 5, Requested: 1.\",\"param\":null,\"type\":\"rate_limit_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/images/generations",
        body: "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 429
  end

  test "Basic image generation with a text prompt" do
    routes = [
      %{
        path: "/images/generations",
        method: "POST",
        status: 200,
        body:
          "{\"created\":1711000000,\"data\":[{\"revised_prompt\":\"A fluffy white cat sitting peacefully on a sunlit windowsill\",\"url\":\"https://example.com/images/img-abc123.png\"}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/images/generations",
        body:
          "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Image generation requesting multiple images" do
    routes = [
      %{
        path: "/images/generations",
        method: "POST",
        status: 200,
        body:
          "{\"created\":1711000000,\"data\":[{\"url\":\"https://example.com/images/img-001.png\"},{\"url\":\"https://example.com/images/img-002.png\"},{\"url\":\"https://example.com/images/img-003.png\"}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/images/generations",
        body:
          "{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Image generation with explicit size parameter" do
    routes = [
      %{
        path: "/images/generations",
        method: "POST",
        status: 200,
        body:
          "{\"created\":1711000000,\"data\":[{\"revised_prompt\":\"A breathtaking sunset over snow-capped mountains\",\"url\":\"https://example.com/images/img-def456.png\"}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/images/generations",
        body:
          "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end
end
