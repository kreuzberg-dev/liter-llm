# Code generated by liter-llm-e2e-generator. DO NOT EDIT.
defmodule LiterLlmE2E.FilesTest do
  use ExUnit.Case, async: true

  alias LiterLlmE2E.MockServer
  alias LiterLlmE2E.Helpers

  test "List files when no files have been uploaded" do
    routes = [
      %{
        path: "/files",
        method: "GET",
        status: 200,
        body: "{\"data\":[],\"object\":\"list\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/files", decode_body: false)
    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Upload a large file successfully" do
    routes = [
      %{
        path: "/files",
        method: "POST",
        status: 200,
        body:
          "{\"bytes\":536870912,\"created_at\":1711000000,\"filename\":\"large_training_data.jsonl\",\"id\":\"file-large001\",\"object\":\"file\",\"purpose\":\"fine-tune\",\"status\":\"processed\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/files",
        body: "{\"file\":\"large_training_data.jsonl\",\"purpose\":\"fine-tune\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "401 Unauthorized when listing files with invalid API key" do
    routes = [
      %{
        path: "/files",
        method: "GET",
        status: 401,
        body:
          "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/files", decode_body: false)
    assert resp.status == 401
  end

  test "400 Bad Request when uploading a file with invalid purpose" do
    routes = [
      %{
        path: "/files",
        method: "POST",
        status: 400,
        body:
          "{\"error\":{\"code\":null,\"message\":\"Invalid value for 'purpose': 'invalid-purpose'. Supported values are: 'fine-tune', 'assistants', 'batch'.\",\"param\":\"purpose\",\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/files",
        body: "{\"file\":\"data.jsonl\",\"purpose\":\"invalid-purpose\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 400
  end

  test "404 Not Found when retrieving a nonexistent file" do
    routes = [
      %{
        path: "/files/file-placeholder",
        method: "GET",
        status: 404,
        body:
          "{\"error\":{\"code\":null,\"message\":\"No such File object: file-nonexistent\",\"param\":null,\"type\":\"invalid_request_error\"}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/files/file-placeholder", decode_body: false)
    assert resp.status == 404
  end

  test "Upload a file for use with the API" do
    routes = [
      %{
        path: "/files",
        method: "POST",
        status: 200,
        body:
          "{\"bytes\":1024,\"created_at\":1711000000,\"filename\":\"training_data.jsonl\",\"id\":\"file-abc123\",\"object\":\"file\",\"purpose\":\"fine-tune\",\"status\":\"processed\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/files",
        body: "{\"file\":\"training_data.jsonl\",\"purpose\":\"fine-tune\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Delete an uploaded file" do
    routes = [
      %{
        path: "/files/file-placeholder",
        method: "POST",
        status: 200,
        body: "{\"deleted\":true,\"id\":\"file-abc123\",\"object\":\"file\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/files/file-placeholder",
        body: "{\"file_id\":\"file-abc123\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Retrieve the content of an uploaded file" do
    routes = [
      %{
        path: "/files/file-placeholder/content",
        method: "GET",
        status: 200,
        body:
          "{\"_binary\":true,\"_content_type\":\"application/octet-stream\",\"_description\":\"Mock file content bytes\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/files/file-placeholder/content", decode_body: false)
    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "List all uploaded files" do
    routes = [
      %{
        path: "/files",
        method: "GET",
        status: 200,
        body:
          "{\"data\":[{\"bytes\":1024,\"created_at\":1711000000,\"filename\":\"training_data.jsonl\",\"id\":\"file-abc123\",\"object\":\"file\",\"purpose\":\"fine-tune\",\"status\":\"processed\"},{\"bytes\":2048,\"created_at\":1711000100,\"filename\":\"batch_input.jsonl\",\"id\":\"file-def456\",\"object\":\"file\",\"purpose\":\"batch\",\"status\":\"processed\"}],\"object\":\"list\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/files", decode_body: false)
    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Retrieve metadata for an uploaded file" do
    routes = [
      %{
        path: "/files/file-placeholder",
        method: "GET",
        status: 200,
        body:
          "{\"bytes\":1024,\"created_at\":1711000000,\"filename\":\"training_data.jsonl\",\"id\":\"file-abc123\",\"object\":\"file\",\"purpose\":\"fine-tune\",\"status\":\"processed\"}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} = Req.get(base_url <> "/files/file-placeholder", decode_body: false)
    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end
end
