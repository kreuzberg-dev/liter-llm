# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:004c8465b2bdb35c76030e61e0094219e60aa30f8119f5b3d8eb65fe643c8c8c
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: moderate
defmodule E2e.ModerateTest do
  use ExUnit.Case, async: false

  describe "edge_moderate_all_categories" do
    test "edge_moderate_all_categories" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_moderate_all_categories")
      {:ok, result} = LiterLlm.moderate_async(client, "\"Extremely harmful content targeting multiple categories\"")
      assert length(result.results) == 1
      assert result.results["0"].flagged == true
    end
  end

  describe "edge_moderate_empty_input" do
    test "edge_moderate_empty_input" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_moderate_empty_input")
      {:ok, result} = LiterLlm.moderate_async(client, "\"\"")
      assert length(result.results) == 1
      assert result.results["0"].flagged == false
    end
  end

  describe "error_moderate_auth_401" do
    test "error_moderate_auth_401" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_moderate_auth_401")
      assert {:error, _} = LiterLlm.moderate_async(client, "\"Hello\"")
    end
  end

  describe "error_moderate_bad_request" do
    test "error_moderate_bad_request" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_moderate_bad_request")
      assert {:error, _} = LiterLlm.moderate_async(client, "\"Hello\"")
    end
  end

  describe "smoke_moderate_batch" do
    test "smoke_moderate_batch" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_moderate_batch")
      {:ok, result} = LiterLlm.moderate_async(client, "[\"Hello world\",\"Nice weather today\"]")
      assert length(result.results) == 2
      assert result.results["0"].flagged == false
    end
  end

  describe "smoke_moderate_flagged" do
    test "smoke_moderate_flagged" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_moderate_flagged")
      {:ok, result} = LiterLlm.moderate_async(client, "\"I want to hurt someone very badly\"")
      assert length(result.results) == 1
      assert result.results["0"].flagged == true
    end
  end

  describe "smoke_moderate_single" do
    test "smoke_moderate_single" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_moderate_single")
      {:ok, result} = LiterLlm.moderate_async(client, "\"The weather is nice today.\"")
      assert length(result.results) == 1
      assert result.results["0"].flagged == false
    end
  end
end
