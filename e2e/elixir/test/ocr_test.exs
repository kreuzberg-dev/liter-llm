# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f3b124536d2d00e51336edaee17c8a4f3a08d56dbccdbfc45c4c42673dec0398
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: ocr
defmodule E2e.OcrTest do
  use ExUnit.Case, async: false

  describe "ocr_error_400" do
    test "ocr_error_400" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/ocr_error_400")

      assert {:error, __reason} =
               LiterLlm.defaultclient_ocr_async(
                 client,
                 "{\"document\":{\"type\":\"document_url\",\"url\":\"invalid://url\"},\"model\":\"mistral/mistral-ocr-latest\"}"
               )

      # skipped: declared error variant 'BadRequest' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "ocr_error_401" do
    test "ocr_error_401" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/ocr_error_401")

      assert {:error, __reason} =
               LiterLlm.defaultclient_ocr_async(
                 client,
                 "{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/doc.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}"
               )

      # skipped: declared error variant 'Authentication' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "ocr_multi_page" do
    test "ocr_multi_page" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/ocr_multi_page")

      {:ok, result} =
        LiterLlm.defaultclient_ocr_async(
          client,
          "{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/multipage.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}"
        )

      assert length(result.pages) == 2
      assert Enum.at(result.pages, 0).index == 0
      assert Enum.at(result.pages, 1).index == 1
    end
  end

  describe "ocr_url_document" do
    test "ocr_url_document" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/ocr_url_document")

      {:ok, result} =
        LiterLlm.defaultclient_ocr_async(
          client,
          "{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/doc.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}"
        )

      refute is_nil(result)
    end
  end
end
