# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:589b293d2b612dfa4f666e8755a4019ce8d23787ef2ad68a2724e0f16ca06323
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: speech
defmodule E2e.SpeechTest do
  use ExUnit.Case, async: false

  describe "edge_speech_all_voices" do
    test "edge_speech_all_voices" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_speech_all_voices")

      {:ok, result} = LiterLlm.defaultclient_speech_async(client, "{\"input\":\"Hello world\",\"model\":\"tts-1\",\"voice\":\"nova\"}")
      assert result not in [nil, "", [], %{}]
    end
  end

  describe "edge_speech_long_input" do
    test "edge_speech_long_input" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_speech_long_input")

      {:ok, result} =
        LiterLlm.defaultclient_speech_async(
          client,
          "{\"input\":\"This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.\",\"model\":\"tts-1\",\"voice\":\"echo\"}"
        )

      assert result not in [nil, "", [], %{}]
    end
  end

  describe "error_speech_auth_401" do
    test "error_speech_auth_401" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_speech_auth_401")

      assert {:error, __reason} =
               LiterLlm.defaultclient_speech_async(client, "{\"input\":\"Hello\",\"model\":\"tts-1\",\"voice\":\"alloy\"}")

      # skipped: declared error variant 'Authentication' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "error_speech_bad_model" do
    test "error_speech_bad_model" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_speech_bad_model")

      assert {:error, __reason} =
               LiterLlm.defaultclient_speech_async(client, "{\"input\":\"Hello\",\"model\":\"tts-nonexistent\",\"voice\":\"alloy\"}")

      # skipped: declared error variant 'BadRequest' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "smoke_speech_basic" do
    test "smoke_speech_basic" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_speech_basic")

      {:ok, result} = LiterLlm.defaultclient_speech_async(client, "{\"input\":\"Hello, world!\",\"model\":\"tts-1\",\"voice\":\"alloy\"}")
      assert result not in [nil, "", [], %{}]
    end
  end

  describe "smoke_speech_mp3_format" do
    test "smoke_speech_mp3_format" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_speech_mp3_format")

      {:ok, result} =
        LiterLlm.defaultclient_speech_async(
          client,
          "{\"input\":\"The quick brown fox jumps over the lazy dog.\",\"model\":\"tts-1-hd\",\"response_format\":\"mp3\",\"speed\":1.0,\"voice\":\"nova\"}"
        )

      assert result not in [nil, "", [], %{}]
    end
  end
end
