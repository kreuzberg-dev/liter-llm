# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f8762525b7c86503b9cf934801e27793dc115616578946df1aff25182130b8d6
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: streaming
defmodule E2e.StreamingTest do
  use ExUnit.Case, async: false

  describe "anthropic_stream" do
    test "anthropic_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/anthropic_stream")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 3
      assert String.trim(result.stream_content) == "One Two Three"
      assert result.stream_complete == true
    end
  end

  describe "azure_stream" do
    test "azure_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/azure_stream")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 3
      assert String.trim(result.stream_content) == "1 2 3"
      assert result.stream_complete == true
    end
  end

  describe "basic_stream" do
    test "basic_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/basic_stream")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 3
      assert String.trim(result.stream_content) == "1 2 3"
    end
  end

  describe "bedrock_stream" do
    test "bedrock_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/bedrock_stream")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 2
      assert String.trim(result.stream_content) == "One Two Three"
      assert result.stream_complete == true
    end
  end

  describe "edge_stream_function_call" do
    test "edge_stream_function_call" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/edge_stream_function_call")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 2
    end
  end

  describe "empty_stream" do
    test "empty_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/empty_stream")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 0
      assert String.trim(result.stream_content) == ""
    end
  end

  describe "local_stream_ollama" do
    test "local_stream_ollama" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/local_stream_ollama")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 3
      assert String.trim(result.stream_content) == "1 2 3"
      assert result.stream_complete == true
    end
  end

  describe "stream_content_policy_error" do
    test "stream_content_policy_error" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/stream_content_policy_error")
      assert {:error, _} = LiterLlm.chat_stream_async(client, nil)
    end
  end

  describe "stream_done_signal" do
    test "stream_done_signal" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/stream_done_signal")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert result.stream_complete == true
      assert String.trim(result.stream_content) == "Done"
      assert result.no_chunks_after_done == true
    end
  end

  describe "stream_error_401" do
    test "stream_error_401" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/stream_error_401")
      assert {:error, _} = LiterLlm.chat_stream_async(client, nil)
    end
  end

  describe "stream_multiple_choices" do
    test "stream_multiple_choices" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/stream_multiple_choices")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
    end
  end

  describe "stream_with_tool_calls" do
    test "stream_with_tool_calls" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/stream_with_tool_calls")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 4
      assert String.trim(result.finish_reason) == "tool_calls"
      assert result.tool_calls != ""
      assert String.trim(result.tool_calls["0"].function.name) == "get_weather"
    end
  end

  describe "stream_with_usage" do
    test "stream_with_usage" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/stream_with_usage")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 3
      assert String.trim(result.stream_content) == "Hi there!"
      assert result.usage.total_tokens == 18
    end
  end

  describe "vertex_stream" do
    test "vertex_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", System.get_env("MOCK_SERVER_URL") <> "/fixtures/vertex_stream")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      assert length(result.chunks) >= 2
      assert String.trim(result.stream_content) == "One Two Three"
      assert result.stream_complete == true
    end
  end
end
