# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:7d269ea058c865f291bc27eb40aed64878c0f6ec5eaf0285ce920955d5537aeb
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: streaming
defmodule E2e.StreamingTest do
  use ExUnit.Case, async: false

  describe "anthropic_stream" do
    test "anthropic_stream" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/anthropic_stream")

      {:ok, result} =
        LiterLlm.chat_stream(
          client,
          "{\"max_tokens\":32,\"messages\":[{\"content\":\"Count to three, one word per response.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\",\"stream\":true}"
        )

      chunks = Enum.to_list(result)
      assert length(chunks) >= 3

      assert Enum.join(
               Enum.map(chunks, fn c -> Map.get(Map.get(Enum.at(c.choices, 0) || %{}, :delta, %{}) || %{}, :content, "") || "" end),
               ""
             ) == "One Two Three"

      assert (case List.last(chunks) do
                nil ->
                  false

                c ->
                  case List.first(Map.get(c, :choices, []) || []) do
                    nil -> false
                    choice -> Map.get(choice, :finish_reason) != nil
                  end
              end)
    end
  end

  describe "azure_stream" do
    test "azure_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/azure_stream")

      {:ok, result} =
        LiterLlm.chat_stream(
          client,
          "{\"messages\":[{\"content\":\"Count to 3\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\",\"stream\":true,\"temperature\":0}"
        )

      chunks = Enum.to_list(result)
      assert length(chunks) >= 3

      assert Enum.join(
               Enum.map(chunks, fn c -> Map.get(Map.get(Enum.at(c.choices, 0) || %{}, :delta, %{}) || %{}, :content, "") || "" end),
               ""
             ) == "1 2 3"

      assert (case List.last(chunks) do
                nil ->
                  false

                c ->
                  case List.first(Map.get(c, :choices, []) || []) do
                    nil -> false
                    choice -> Map.get(choice, :finish_reason) != nil
                  end
              end)
    end
  end

  describe "basic_stream" do
    test "basic_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/basic_stream")

      {:ok, result} =
        LiterLlm.chat_stream(client, "{\"messages\":[{\"content\":\"Count to 3\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true}")

      chunks = Enum.to_list(result)
      assert length(chunks) >= 3

      assert Enum.join(
               Enum.map(chunks, fn c -> Map.get(Map.get(Enum.at(c.choices, 0) || %{}, :delta, %{}) || %{}, :content, "") || "" end),
               ""
             ) == "1 2 3"
    end
  end

  describe "bedrock_stream" do
    test "bedrock_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/bedrock_stream")

      {:ok, result} =
        LiterLlm.chat_stream(
          client,
          "{\"max_tokens\":32,\"messages\":[{\"content\":\"Count to three, one word per response.\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\",\"stream\":true}"
        )

      chunks = Enum.to_list(result)
      assert length(chunks) >= 2

      assert Enum.join(
               Enum.map(chunks, fn c -> Map.get(Map.get(Enum.at(c.choices, 0) || %{}, :delta, %{}) || %{}, :content, "") || "" end),
               ""
             ) == "One Two Three"

      assert (case List.last(chunks) do
                nil ->
                  false

                c ->
                  case List.first(Map.get(c, :choices, []) || []) do
                    nil -> false
                    choice -> Map.get(choice, :finish_reason) != nil
                  end
              end)
    end
  end

  describe "edge_stream_function_call" do
    test "edge_stream_function_call" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_stream_function_call")

      {:ok, result} =
        LiterLlm.chat_stream(
          client,
          "{\"messages\":[{\"content\":\"What's the weather?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"tools\":[{\"function\":{\"name\":\"get_weather\",\"parameters\":{\"properties\":{\"city\":{\"type\":\"string\"}},\"type\":\"object\"}},\"type\":\"function\"}]}"
        )

      chunks = Enum.to_list(result)
      assert length(chunks) >= 2
    end
  end

  describe "empty_stream" do
    test "empty_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/empty_stream")

      {:ok, result} =
        LiterLlm.chat_stream(client, "{\"messages\":[{\"content\":\"Say nothing\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true}")

      chunks = Enum.to_list(result)
      assert length(chunks) >= 0

      assert Enum.join(
               Enum.map(chunks, fn c -> Map.get(Map.get(Enum.at(c.choices, 0) || %{}, :delta, %{}) || %{}, :content, "") || "" end),
               ""
             ) == ""
    end
  end

  describe "local_stream_ollama" do
    test "local_stream_ollama" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/local_stream_ollama")

      {:ok, result} =
        LiterLlm.chat_stream(
          client,
          "{\"messages\":[{\"content\":\"Count to 3\",\"role\":\"user\"}],\"model\":\"ollama/qwen2:0.5b\",\"stream\":true}"
        )

      chunks = Enum.to_list(result)
      assert length(chunks) >= 3

      assert Enum.join(
               Enum.map(chunks, fn c -> Map.get(Map.get(Enum.at(c.choices, 0) || %{}, :delta, %{}) || %{}, :content, "") || "" end),
               ""
             ) == "1 2 3"

      assert (case List.last(chunks) do
                nil ->
                  false

                c ->
                  case List.first(Map.get(c, :choices, []) || []) do
                    nil -> false
                    choice -> Map.get(choice, :finish_reason) != nil
                  end
              end)
    end
  end

  describe "stream_content_policy_error" do
    test "stream_content_policy_error" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/stream_content_policy_error")

      assert {:error, __reason} =
               LiterLlm.chat_stream(
                 client,
                 "{\"messages\":[{\"content\":\"Generate harmful content\",\"role\":\"user\"}],\"model\":\"gpt-4o\",\"stream\":true}"
               )

      # skipped: declared error variant 'ContentPolicy' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "stream_done_signal" do
    test "stream_done_signal" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/stream_done_signal")

      {:ok, result} =
        LiterLlm.chat_stream(client, "{\"messages\":[{\"content\":\"Say done\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true}")

      chunks = Enum.to_list(result)

      assert (case List.last(chunks) do
                nil ->
                  false

                c ->
                  case List.first(Map.get(c, :choices, []) || []) do
                    nil -> false
                    choice -> Map.get(choice, :finish_reason) != nil
                  end
              end)

      assert Enum.join(
               Enum.map(chunks, fn c -> Map.get(Map.get(Enum.at(c.choices, 0) || %{}, :delta, %{}) || %{}, :content, "") || "" end),
               ""
             ) == "Done"

      assert true
    end
  end

  describe "stream_error_401" do
    test "stream_error_401" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/stream_error_401")

      assert {:error, __reason} =
               LiterLlm.chat_stream(
                 client,
                 "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true}"
               )

      # skipped: declared error variant 'Authentication' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "stream_multiple_choices" do
    test "stream_multiple_choices" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/stream_multiple_choices")

      {:ok, result} =
        LiterLlm.chat_stream(
          client,
          "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4o\",\"n\":2,\"stream\":true}"
        )

      chunks = Enum.to_list(result)
      refute is_nil(chunks)
    end
  end

  describe "stream_with_tool_calls" do
    test "stream_with_tool_calls" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/stream_with_tool_calls")

      {:ok, result} =
        LiterLlm.chat_stream(
          client,
          "{\"messages\":[{\"content\":\"What is the weather in NYC?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true,\"tools\":[{\"function\":{\"description\":\"Get the current weather for a given location\",\"name\":\"get_weather\",\"parameters\":{\"properties\":{\"location\":{\"description\":\"The city and state, e.g. New York, NY\",\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"}},\"type\":\"function\"}]}"
        )

      chunks = Enum.to_list(result)
      assert length(chunks) >= 4
      assert Enum.at(List.last(chunks).choices, 0).finish_reason == "tool_calls"

      assert Enum.flat_map(chunks, fn c -> Map.get(Map.get(List.first(c.choices) || %{}, :delta, %{}) || %{}, :tool_calls, []) || [] end) not in [
               nil,
               "",
               [],
               %{}
             ]

      assert Enum.at(
               Enum.flat_map(chunks, fn c -> Map.get(Map.get(List.first(c.choices) || %{}, :delta, %{}) || %{}, :tool_calls, []) || [] end),
               0
             ).function.name == "get_weather"
    end
  end

  describe "vertex_stream" do
    test "vertex_stream" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/vertex_stream")

      {:ok, result} =
        LiterLlm.chat_stream(
          client,
          "{\"max_tokens\":32,\"messages\":[{\"content\":\"Count to three, one word per response.\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\",\"stream\":true}"
        )

      chunks = Enum.to_list(result)
      assert length(chunks) >= 2

      assert Enum.join(
               Enum.map(chunks, fn c -> Map.get(Map.get(Enum.at(c.choices, 0) || %{}, :delta, %{}) || %{}, :content, "") || "" end),
               ""
             ) == "One Two Three"

      assert (case List.last(chunks) do
                nil ->
                  false

                c ->
                  case List.first(Map.get(c, :choices, []) || []) do
                    nil -> false
                    choice -> Map.get(choice, :finish_reason) != nil
                  end
              end)
    end
  end
end
