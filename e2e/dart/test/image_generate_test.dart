// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:607373993772bfe2a1c7550590ecbcb341ee3ba4fdfae0d2a39284b43abe8bd8
// To regenerate: alef generate
// To verify freshness: alef verify
// ignore_for_file: unused_local_variable, unused_element

import 'package:test/test.dart';
import 'dart:io';
import 'package:liter_llm/liter_llm.dart';
import 'package:liter_llm/src/liter_llm_bridge_generated/frb_generated.dart' show RustLib;
import 'dart:async';
import 'dart:convert';
import 'e2e_helpers.dart';

// E2e tests for category: image-generate

Process? _sutProcess;
String? _spawnedSutUrl;
MockServerHandle? _mockServerHandle;
final Map<String, String> _fixtureUrls = <String, String>{};

String _sutUrl() => _spawnedSutUrl ?? Platform.environment['MOCK_SERVER_URL'] ?? Platform.environment['SUT_URL'] ?? 'http://localhost:8008';

String _fixtureUrl(String fixtureId) {
  final perFixture = _fixtureUrls[fixtureId];
  if (perFixture != null) return perFixture;
  final env = Platform.environment['MOCK_SERVERS'];
  if (env != null && env.isNotEmpty) {
    try {
      final decoded = jsonDecode(env);
      if (decoded is Map && decoded[fixtureId] is String) {
        return decoded[fixtureId] as String;
      }
    } catch (_) {}
  }
  return '${_sutUrl()}/fixtures/$fixtureId';
}

void main() {
  var _rustLibInitialized = false;

  setUpAll(() async {
    await RustLib.init();
    _rustLibInitialized = true;
    if (Platform.environment['MOCK_SERVER_URL'] == null && Platform.environment['SUT_URL'] == null) {
      final _harness = Directory.current.uri.resolve('app_harness.dart').toFilePath();
      if (File(_harness).existsSync()) {
        _sutProcess = await Process.start('dart', ['run', _harness], mode: ProcessStartMode.normal);
        final _ready = Completer<void>();
        _sutProcess!.stdout
            .transform(utf8.decoder)
            .transform(const LineSplitter())
            .listen((_line) {
          final _trimmed = _line.trim();
          if (_trimmed.startsWith('SUT_URL=')) {
            _spawnedSutUrl = _trimmed.substring('SUT_URL='.length);
            if (!_ready.isCompleted) _ready.complete();
          }
        }, onDone: () {
          if (!_ready.isCompleted) _ready.complete();
        });
        await _ready.future.timeout(const Duration(seconds: 15), onTimeout: () {});
      } else {
        // Standalone mock-server mode: delegate to the shared helper.
        final _handle = await startMockServer();
        _mockServerHandle = _handle;
        _spawnedSutUrl = _handle.url;
        _fixtureUrls.addAll(_handle.fixtureUrls);
      }
    }
  });

  tearDownAll(() async {
    if (_rustLibInitialized) {
      RustLib.dispose();
    }
    final proc = _sutProcess;
    if (proc != null) {
      proc.kill();
      await proc.exitCode;
    }
    await _mockServerHandle?.stop();
  });

  test('Image generation returning base64-encoded data instead of URL', () async {
    final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-3","n":1,"prompt":"A blue circle","response_format":"b64_json","size":"1024x1024"}');
    final mockUrl = _fixtureUrl("edge_image_b64_response");
        final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
    final result = await client.imageGenerate(req: req);
    expect(result.data.length, equals(1));
    expect(result.data[0].b64Json?.toString(), isNotEmpty);
  });

  test('Image generation with an empty prompt returns 400', () async {
    await expectLater(() async {
      final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-3","n":1,"prompt":"","size":"1024x1024"}');
      final mockUrl = _fixtureUrl("edge_image_empty_prompt");
          final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
      return client.imageGenerate(req: req);
      // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
    }(), throwsA(anything));
  });

  // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
  test('Image generation requesting multiple images with n=3', () async {
    final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-3","n":3,"prompt":"A cat"}');
    final mockUrl = _fixtureUrl("edge_image_multiple_n");
        final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
    final result = await client.imageGenerate(req: req);
    expect(result.data.length, equals(3));
    expect(result.data[0].url?.toString(), isNotEmpty);
    expect(result.data[1].url?.toString(), isNotEmpty);
    expect(result.data[2].url?.toString(), isNotEmpty);
  });

  test('401 Unauthorized when generating images with invalid API key', () async {
    await expectLater(() async {
      final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-3","n":1,"prompt":"A cat","size":"1024x1024"}');
      final mockUrl = _fixtureUrl("error_image_auth_401");
          final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
      return client.imageGenerate(req: req);
      // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
    }(), throwsA(anything));
  });

  // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
  test('400 Bad Request when image generation parameters are invalid', () async {
    await expectLater(() async {
      final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-3","n":1,"prompt":"A cat","size":"9999x9999"}');
      final mockUrl = _fixtureUrl("error_image_bad_request");
          final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
      return client.imageGenerate(req: req);
      // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
    }(), throwsA(anything));
  });

  // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
  test('429 Rate limit exceeded for image generation', () async {
    await expectLater(() async {
      final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-3","n":1,"prompt":"A cat","size":"1024x1024"}');
      final mockUrl = _fixtureUrl("error_image_rate_limit");
          final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
      return client.imageGenerate(req: req);
      // skipped: declared error variant 'RateLimited' not substantiated by this backend's generated error type
    }(), throwsA(anything));
  });

  // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
  test('Basic image generation with a text prompt', () async {
    final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-3","n":1,"prompt":"A white cat sitting on a windowsill","size":"1024x1024"}');
    final mockUrl = _fixtureUrl("smoke_image_basic");
        final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
    final result = await client.imageGenerate(req: req);
    expect(result.data.length, equals(1));
    expect(result.data[0].url?.toString(), isNotEmpty);
  });

  test('Image generation requesting multiple images', () async {
    final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-2","n":3,"prompt":"A red bicycle","size":"256x256"}');
    final mockUrl = _fixtureUrl("smoke_image_multiple");
        final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
    final result = await client.imageGenerate(req: req);
    expect(result.data.length, equals(3));
    expect(result.data[0].url?.toString(), isNotEmpty);
  });

  test('Image generation with explicit size parameter', () async {
    final req = await createCreateImageRequestFromJson(json: '{"model":"dall-e-3","n":1,"prompt":"A sunset over mountains","size":"1792x1024"}');
    final mockUrl = _fixtureUrl("smoke_image_with_size");
        final client = await LiterLlmBridge.createClient('test-key', baseUrl: mockUrl);
    final result = await client.imageGenerate(req: req);
    expect(result.data.length, equals(1));
    expect(result.data[0].url?.toString(), isNotEmpty);
  });

}
