// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:liter_llm/liter_llm.dart';

// E2e tests for category: proxy
void main() {
  test('401 Unauthorized when an invalid API key is provided through the proxy', () async {
    await expectLater(chat(null), throwsA(isA<Exception>()));
  });

  test('401 Unauthorized when no API key is provided through the proxy', () async {
    await expectLater(chat(null), throwsA(isA<Exception>()));
  });

  test('Basic chat completion request routed through the proxy', () async {
    final result = await chat(null);
    expect(result.choices.length, equals(1));
    expect(result.choices.get("0").message.content.trim(), equals('Hello!'));
    expect(result.choices.get("0").finish_reason.trim(), equals('stop'));
  });

  test('Streaming chat completion routed through the proxy', () async {
    final result = await chatStream(null);
    expect(result.chunks.length, greaterThanOrEqualTo(3));
    expect(result.stream_content.trim(), equals('1 2 3'));
    expect(result.stream_complete, isTrue);
  });

  test('Embedding request routed through the proxy', () async {
    final result = await embed('Hello world');
    expect(result.data.length, equals(1));
  });

  test('Health check verifying proxy connectivity via list models', () async {
    final result = await listModels();
    expect(result.data.length, greaterThanOrEqualTo(1));
  });

  test('Image generation request routed through the proxy', () async {
    final result = await imageGenerate(null);
    expect(result.data.length, equals(1));
    expect(result.data.get("0").url, isNotEmpty);
  });

  test('List models request routed through the proxy', () async {
    final result = await listModels();
    expect(result.data.length, greaterThanOrEqualTo(1));
  });

  test('Content moderation request routed through the proxy', () async {
    final result = await moderate('The weather is nice today.');
    expect(result.results.length, equals(1));
    expect(result.results.get("0").flagged, equals(false));
  });

  test('Document reranking request routed through the proxy', () async {
    final result = await rerank(null);
    expect(result.results.length, equals(2));
    expect(result.results.get("0").relevance_score, greaterThan(0.9));
  });

  test('429 Too Many Requests from upstream provider through the proxy', () async {
    await expectLater(chat(null), throwsA(isA<Exception>()));
  });

  test('500 Internal Server Error from upstream provider through the proxy', () async {
    await expectLater(chat(null), throwsA(isA<Exception>()));
  });

}
