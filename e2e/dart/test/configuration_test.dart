// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:liter_llm/liter_llm.dart';

// E2e tests for category: configuration
void main() {
  test('Client configured with a custom base URL routes all requests to that endpoint', () async {
    final result = await chat(null);
    expect(result.choices.length, equals(1));
    expect(result.choices.get("0").message.content.trim(), equals('Hi there!'));
    expect(result.choices.get("0").finish_reason.trim(), equals('stop'));
    expect(result.model.trim(), equals('local-model'));
  });

  test('Client configured with extra custom headers successfully completes a chat request', () async {
    final result = await chat(null);
    expect(result.choices.length, equals(1));
    expect(result.choices.get("0").finish_reason.trim(), equals('stop'));
  });

  test('llamacpp local provider routes requests via llamacpp/ model prefix with no auth', () async {
    final result = await chat(null);
    expect(result.choices.length, equals(1));
    expect(result.choices.get("0").message.content.trim(), equals('Hi there! I\'m running locally.'));
    expect(result.choices.get("0").finish_reason.trim(), equals('stop'));
    expect(result.model.trim(), equals('my-model'));
  });

  test('Ollama local provider routes requests via ollama/ model prefix with no auth', () async {
    final result = await chat(null);
    expect(result.choices.length, equals(1));
    expect(result.choices.get("0").message.content.trim(), equals('Hello! How can I help you today?'));
    expect(result.choices.get("0").finish_reason.trim(), equals('stop'));
    expect(result.model.trim(), equals('qwen2:0.5b'));
  });

  test('vLLM local provider routes requests via vllm/ model prefix with no auth', () async {
    final result = await chat(null);
    expect(result.choices.length, equals(1));
    expect(result.choices.get("0").message.content.trim(), equals('Hello! How may I assist you?'));
    expect(result.choices.get("0").finish_reason.trim(), equals('stop'));
    expect(result.model.trim(), equals('meta-llama/Llama-3.2-1B'));
  });

}
