# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:fae1dc0e8c3b8db1379e417695c7dd951e3ec36c234198aec40a8f3494b939ab
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: rerank."""

import os

import pytest  # noqa: F401
from liter_llm import create_client


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = (
        " ".join(str(value) for value in raw_items)
        if isinstance(raw_items, list)
        else ""
    )
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio
async def test_edge_rerank_empty_query() -> None:
    """Reranking with an empty query string."""
    request = {
        "documents": ["Some document", "Another document"],
        "model": "rerank-v3.5",
        "query": "",
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_rerank_empty_query",
    )
    result = await client.rerank(request=request)
    assert len(result.results) == 2  # noqa: S101


@pytest.mark.asyncio
async def test_edge_rerank_single_doc() -> None:
    """Reranking with only a single document."""
    request = {
        "documents": [
            "Artificial intelligence is the simulation of human intelligence."
        ],
        "model": "rerank-v3.5",
        "query": "What is AI?",
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_rerank_single_doc",
    )
    result = await client.rerank(request=request)
    assert len(result.results) == 1  # noqa: S101
    assert result.results.get("0").relevance_score > 0.9  # noqa: S101


@pytest.mark.asyncio
async def test_error_rerank_auth_401() -> None:
    """401 Unauthorized for reranking with invalid API key."""
    request = {"documents": ["doc1"], "model": "rerank-v3.5", "query": "test"}
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_rerank_auth_401",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.rerank(request=request)
    assert "Authentication" in str(exc_info.value)  # noqa: S101


@pytest.mark.asyncio
async def test_error_rerank_bad_request() -> None:
    """400 Bad Request for reranking with invalid model."""
    request = {"documents": ["doc1"], "model": "nonexistent-rerank", "query": "test"}
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_rerank_bad_request",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.rerank(request=request)
    assert "BadRequest" in str(exc_info.value)  # noqa: S101


@pytest.mark.asyncio
async def test_smoke_rerank_basic() -> None:
    """Basic reranking of documents against a query."""
    request = {
        "documents": [
            "Machine learning is a subset of AI.",
            "The weather is sunny today.",
            "Deep learning uses neural networks.",
        ],
        "model": "rerank-v3.5",
        "query": "What is machine learning?",
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_rerank_basic",
    )
    result = await client.rerank(request=request)
    assert len(result.results) == 3  # noqa: S101
    assert result.results.get("0").relevance_score > 0.9  # noqa: S101


@pytest.mark.asyncio
async def test_smoke_rerank_return_docs() -> None:
    """Reranking with return_documents flag to include document text."""
    request = {
        "documents": [
            "Rust is a systems programming language.",
            "Iron rusts when exposed to water.",
        ],
        "model": "rerank-v3.5",
        "query": "What is Rust?",
        "return_documents": True,
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_rerank_return_docs",
    )
    result = await client.rerank(request=request)
    assert len(result.results) == 2  # noqa: S101
    assert result.results.get("0").document  # noqa: S101
    assert result.results.get("0").relevance_score > 0.9  # noqa: S101


@pytest.mark.asyncio
async def test_smoke_rerank_with_top_n() -> None:
    """Reranking with top_n parameter to limit results."""
    request = {
        "documents": [
            "Python is a programming language.",
            "Cats are cute animals.",
            "Python was created by Guido van Rossum.",
            "The sun is a star.",
        ],
        "model": "rerank-v3.5",
        "query": "What is Python?",
        "top_n": 2,
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_rerank_with_top_n",
    )
    result = await client.rerank(request=request)
    assert len(result.results) == 2  # noqa: S101
    assert result.results.get("0").relevance_score > 0.9  # noqa: S101
