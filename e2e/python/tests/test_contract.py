# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c9438f67b19f9c407b1366bc1937d441463161c54ffad83e56f7f57346e471d7
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: contract."""

import os

import pytest
from liter_llm import create_client
from liter_llm._internal_bindings import ChatCompletionRequest
from liter_llm._internal_bindings import OcrRequest
from liter_llm._internal_bindings import SearchRequest


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


@pytest.mark.asyncio

async def test_binding_api_parity() -> None:
    """Verify all bindings expose the full API surface — constructor accepts all config options and every method exists."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/binding_api_parity")
    req = ChatCompletionRequest.from_json("{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")

    result = await client.chat(req)
    assert len(result.choices) == 1
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "OK".lower()
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()
    assert (result.usage.total_tokens if result.usage else None) == 6
    assert result.model == "gpt-4o"


@pytest.mark.asyncio

async def test_contract_ocr() -> None:
    """Verify ocr() method exists in all bindings."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/contract_ocr")
    req = OcrRequest.from_json("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/contract-test.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}")

    result = await client.ocr(req)
    assert result is not None


@pytest.mark.asyncio

async def test_contract_search() -> None:
    """Verify search() method exists in all bindings."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/contract_search")
    req = SearchRequest.from_json("{\"model\":\"brave/web-search\",\"query\":\"contract test query\"}")

    result = await client.search(req)
    assert result is not None
