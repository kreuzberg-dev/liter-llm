# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:90cbc5f6f4f41c243044b153a9f003c249bfffcc508694f33ef282ac69ac0bdb
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: search."""

import os

import pytest
from liter_llm import create_client
from liter_llm._internal_bindings import SearchRequest


@pytest.mark.asyncio

async def test_search_basic() -> None:
    """Basic web search request with a simple query."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/search_basic")
    req = SearchRequest.from_json("{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}")

    result = await client.search(req)
    assert result is not None


@pytest.mark.asyncio

async def test_search_empty_results() -> None:
    """Web search with a query that returns no results."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/search_empty_results")
    req = SearchRequest.from_json("{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}")

    result = await client.search(req)
    assert result is not None


@pytest.mark.asyncio

async def test_search_error_400() -> None:
    """400 Bad Request error when search query is empty."""
    # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/search_error_400")
        req = SearchRequest.from_json("{\"model\":\"brave/web-search\",\"query\":\"\"}")
        await client.search(req)
    assert "BadRequest" in str(exc_info.value) or "BadRequest" in type(exc_info.value).__name__


@pytest.mark.asyncio

async def test_search_error_401() -> None:
    """401 Unauthorized error on web search due to invalid API credentials."""
    # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/search_error_401")
        req = SearchRequest.from_json("{\"model\":\"brave/web-search\",\"query\":\"test\"}")
        await client.search(req)
    assert "Authentication" in str(exc_info.value) or "Authentication" in type(exc_info.value).__name__


@pytest.mark.asyncio

async def test_search_with_max_results() -> None:
    """Search request with max_results parameter limiting response count."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/search_with_max_results")
    req = SearchRequest.from_json("{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}")

    result = await client.search(req)
    assert len(result.results) == 2
