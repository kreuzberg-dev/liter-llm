# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:ece84dd647296b4302b336b6b68602d786c783a6bdade7563ca76bca61302411
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: proxy."""

import os

import pytest  # noqa: F401
from liter_llm import create_client


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = (
        " ".join(str(value) for value in raw_items)
        if isinstance(raw_items, list)
        else ""
    )
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio
async def test_proxy_auth_invalid() -> None:
    """401 Unauthorized when an invalid API key is provided through the proxy."""
    request = None
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_auth_invalid",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.chat(request=request)
    assert "Authentication" in str(exc_info.value)  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_auth_missing() -> None:
    """401 Unauthorized when no API key is provided through the proxy."""
    request = None
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_auth_missing",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.chat(request=request)
    assert "Authentication" in str(exc_info.value)  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_chat_basic() -> None:
    """Basic chat completion request routed through the proxy."""
    request = None
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_chat_basic",
    )
    result = await client.chat(request=request)
    assert len(result.choices) == 1  # noqa: S101
    assert result.choices.get("0").message.content.strip() == "Hello!"  # noqa: S101
    assert result.choices.get("0").finish_reason.strip() == "stop"  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_chat_streaming() -> None:
    """Streaming chat completion routed through the proxy."""
    request = None
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_chat_streaming",
    )
    result = await client.chat_stream(request=request)
    assert len(result.chunks) >= 3  # noqa: S101
    assert result.stream_content.strip() == "1 2 3"  # noqa: S101
    assert result.stream_complete is True  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_embeddings() -> None:
    """Embedding request routed through the proxy."""
    request = "Hello world"
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_embeddings",
    )
    result = await client.embed(request=request)
    assert len(result.data) == 1  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_health() -> None:
    """Health check verifying proxy connectivity via list models."""
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_health",
    )
    result = await client.list_models()
    assert len(result.data) >= 1  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_image_generate() -> None:
    """Image generation request routed through the proxy."""
    request = None
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_image_generate",
    )
    result = await client.image_generate(request=request)
    assert len(result.data) == 1  # noqa: S101
    assert result.data.get("0").url  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_models_list() -> None:
    """List models request routed through the proxy."""
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_models_list",
    )
    result = await client.list_models()
    assert len(result.data) >= 1  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_moderation() -> None:
    """Content moderation request routed through the proxy."""
    request = "The weather is nice today."
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_moderation",
    )
    result = await client.moderate(request=request)
    assert len(result.results) == 1  # noqa: S101
    assert result.results.get("0").flagged is False  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_rerank() -> None:
    """Document reranking request routed through the proxy."""
    request = None
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_rerank",
    )
    result = await client.rerank(request=request)
    assert len(result.results) == 2  # noqa: S101
    assert result.results.get("0").relevance_score > 0.9  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_upstream_429() -> None:
    """429 Too Many Requests from upstream provider through the proxy."""
    request = None
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_upstream_429",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.chat(request=request)
    assert "RateLimited" in str(exc_info.value)  # noqa: S101


@pytest.mark.asyncio
async def test_proxy_upstream_500() -> None:
    """500 Internal Server Error from upstream provider through the proxy."""
    request = None
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/proxy_upstream_500",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.chat(request=request)
    assert "ServerError" in str(exc_info.value)  # noqa: S101
