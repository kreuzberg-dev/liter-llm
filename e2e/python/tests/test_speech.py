# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:60b255b9ef063e00988460b48357d646a7c26317eab6a2bc6bd919300b922f5e
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: speech."""

import os

import pytest
from liter_llm import create_client
from liter_llm._internal_bindings import CreateSpeechRequest


@pytest.mark.asyncio

async def test_edge_speech_all_voices() -> None:
    """Text-to-speech with specific voice selection."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_speech_all_voices")
    req = CreateSpeechRequest.from_json("{\"input\":\"Hello world\",\"model\":\"tts-1\",\"voice\":\"nova\"}")

    result = await client.speech(req)
    assert result is not None and (not hasattr(result, "__len__") or len(result) > 0)


@pytest.mark.asyncio

async def test_edge_speech_long_input() -> None:
    """Speech generation with a very long input text."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_speech_long_input")
    req = CreateSpeechRequest.from_json("{\"input\":\"This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.\",\"model\":\"tts-1\",\"voice\":\"echo\"}")

    result = await client.speech(req)
    assert result is not None and (not hasattr(result, "__len__") or len(result) > 0)


@pytest.mark.asyncio

async def test_error_speech_auth_401() -> None:
    """401 Unauthorized for speech generation with invalid API key."""
    # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_speech_auth_401")
        req = CreateSpeechRequest.from_json("{\"input\":\"Hello\",\"model\":\"tts-1\",\"voice\":\"alloy\"}")
        await client.speech(req)
    assert "Authentication" in str(exc_info.value) or "Authentication" in type(exc_info.value).__name__


@pytest.mark.asyncio

async def test_error_speech_bad_model() -> None:
    """400 Bad Request for speech with unsupported model."""
    # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_speech_bad_model")
        req = CreateSpeechRequest.from_json("{\"input\":\"Hello\",\"model\":\"tts-nonexistent\",\"voice\":\"alloy\"}")
        await client.speech(req)
    assert "BadRequest" in str(exc_info.value) or "BadRequest" in type(exc_info.value).__name__


@pytest.mark.asyncio

async def test_smoke_speech_basic() -> None:
    """Basic text-to-speech generation."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_speech_basic")
    req = CreateSpeechRequest.from_json("{\"input\":\"Hello, world!\",\"model\":\"tts-1\",\"voice\":\"alloy\"}")

    result = await client.speech(req)
    assert result is not None and (not hasattr(result, "__len__") or len(result) > 0)


@pytest.mark.asyncio

async def test_smoke_speech_mp3_format() -> None:
    """Text-to-speech with explicit MP3 response format."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_speech_mp3_format")
    req = CreateSpeechRequest.from_json("{\"input\":\"The quick brown fox jumps over the lazy dog.\",\"model\":\"tts-1-hd\",\"response_format\":\"mp3\",\"speed\":1.0,\"voice\":\"nova\"}")

    result = await client.speech(req)
    assert result is not None and (not hasattr(result, "__len__") or len(result) > 0)
