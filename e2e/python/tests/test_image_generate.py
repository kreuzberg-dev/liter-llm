# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:4cb67743be0cd8dd00bdd25994c25f67f9b2d9e6841ae2f22e59cfdc70fba82b
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: image-generate."""

import os

import pytest  # noqa: F401
from liter_llm import create_client
from liter_llm._internal_bindings import CreateImageRequest


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = (
        " ".join(str(value) for value in raw_items)
        if isinstance(raw_items, list)
        else ""
    )
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio
async def test_edge_image_b64_response() -> None:
    """Image generation returning base64-encoded data instead of URL."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-3","n":1,"prompt":"A blue circle","response_format":"b64_json","size":"1024x1024"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_image_b64_response",
    )
    result = await client.image_generate(request)
    assert len(result.data) == 1  # noqa: S101
    assert result.data[0].b64_json  # noqa: S101


@pytest.mark.asyncio
async def test_edge_image_empty_prompt() -> None:
    """Image generation with an empty prompt returns 400."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-3","n":1,"prompt":"","size":"1024x1024"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_image_empty_prompt",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.image_generate(request)
    assert "BadRequest" in type(exc_info.value).__name__  # noqa: S101


@pytest.mark.asyncio
async def test_edge_image_multiple_n() -> None:
    """Image generation requesting multiple images with n=3."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-3","n":3,"prompt":"A cat"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_image_multiple_n",
    )
    result = await client.image_generate(request)
    assert len(result.data) == 3  # noqa: S101
    assert result.data[0].url  # noqa: S101
    assert result.data[1].url  # noqa: S101
    assert result.data[2].url  # noqa: S101


@pytest.mark.asyncio
async def test_error_image_auth_401() -> None:
    """401 Unauthorized when generating images with invalid API key."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-3","n":1,"prompt":"A cat","size":"1024x1024"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_image_auth_401",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.image_generate(request)
    assert "Authentication" in type(exc_info.value).__name__  # noqa: S101


@pytest.mark.asyncio
async def test_error_image_bad_request() -> None:
    """400 Bad Request when image generation parameters are invalid."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-3","n":1,"prompt":"A cat","size":"9999x9999"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_image_bad_request",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.image_generate(request)
    assert "BadRequest" in type(exc_info.value).__name__  # noqa: S101


@pytest.mark.asyncio
async def test_error_image_rate_limit() -> None:
    """429 Rate limit exceeded for image generation."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-3","n":1,"prompt":"A cat","size":"1024x1024"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_image_rate_limit",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.image_generate(request)
    assert "RateLimited" in type(exc_info.value).__name__  # noqa: S101


@pytest.mark.asyncio
async def test_smoke_image_basic() -> None:
    """Basic image generation with a text prompt."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-3","n":1,"prompt":"A white cat sitting on a windowsill","size":"1024x1024"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_image_basic",
    )
    result = await client.image_generate(request)
    assert len(result.data) == 1  # noqa: S101
    assert result.data[0].url  # noqa: S101


@pytest.mark.asyncio
async def test_smoke_image_multiple() -> None:
    """Image generation requesting multiple images."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-2","n":3,"prompt":"A red bicycle","size":"256x256"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_image_multiple",
    )
    result = await client.image_generate(request)
    assert len(result.data) == 3  # noqa: S101
    assert result.data[0].url  # noqa: S101


@pytest.mark.asyncio
async def test_smoke_image_with_size() -> None:
    """Image generation with explicit size parameter."""
    request = CreateImageRequest.from_json(
        '{"model":"dall-e-3","n":1,"prompt":"A sunset over mountains","size":"1792x1024"}'
    )
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_image_with_size",
    )
    result = await client.image_generate(request)
    assert len(result.data) == 1  # noqa: S101
    assert result.data[0].url  # noqa: S101
