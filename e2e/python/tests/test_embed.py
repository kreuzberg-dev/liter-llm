# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:26f2b09745cd0e29cf85253fc5e870059478f8eb06b6ab12b22dd4ebcd9cf728
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: embed."""

import os

import pytest  # noqa: F401
from liter_llm import create_client


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = (
        " ".join(str(value) for value in raw_items)
        if isinstance(raw_items, list)
        else ""
    )
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio
async def test_batch_embed() -> None:
    """Embedding request with multiple input strings returns one embedding object per input."""
    request = {"input": ["Hello", "World"], "model": "text-embedding-3-small"}
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/batch_embed",
    )
    result = await client.embed(request=request)
    assert len(result.data) == 2  # noqa: S101
    assert len(result.data.get("0").embedding) == 5  # noqa: S101


@pytest.mark.asyncio
async def test_edge_embed_batch_input() -> None:
    """Embedding request with multiple inputs returns multiple embedding objects."""
    request = {
        "input": ["Hello world", "Goodbye world"],
        "model": "text-embedding-3-small",
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_embed_batch_input",
    )
    result = await client.embed(request=request)
    assert len(result.data) == 2  # noqa: S101
    assert result.data.get("0").index == 0  # noqa: S101
    assert result.data.get("1").index == 1  # noqa: S101


@pytest.mark.asyncio
async def test_edge_embed_empty_input() -> None:
    """Embedding request with empty string input returns empty data array."""
    request = {"input": "", "model": "text-embedding-3-small"}
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_embed_empty_input",
    )
    result = await client.embed(request=request)
    assert len(result.data) == 0  # noqa: S101


@pytest.mark.asyncio
async def test_embed_encoding_format() -> None:
    """Embedding request with explicit encoding_format of float returns float array embeddings."""
    request = {
        "encoding_format": "float",
        "input": "Test input",
        "model": "text-embedding-3-small",
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/embed_encoding_format",
    )
    result = await client.embed(request=request)
    assert len(result.data) == 1  # noqa: S101
    assert len(result.data.get("0").embedding) == 5  # noqa: S101


@pytest.mark.asyncio
async def test_embed_error_401() -> None:
    """401 Unauthorized error on embedding request when API key is invalid."""
    request = {"input": "Hello world", "model": "text-embedding-3-small"}
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/embed_error_401",
    )
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        await client.embed(request=request)
    assert "Authentication" in str(exc_info.value)  # noqa: S101


@pytest.mark.asyncio
async def test_embed_with_dimensions() -> None:
    """Embedding request with explicit dimensions parameter returns embeddings of the requested size."""
    request = {
        "dimensions": 256,
        "input": "Hello world",
        "model": "text-embedding-3-small",
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/embed_with_dimensions",
    )
    result = await client.embed(request=request)
    assert len(result.data) == 1  # noqa: S101
    assert len(result.data.get("0").embedding) == 8  # noqa: S101


@pytest.mark.asyncio
async def test_local_embed_ollama() -> None:
    """Embedding request via Ollama local provider with all-minilm model."""
    request = {
        "input": "The quick brown fox jumps over the lazy dog",
        "model": "ollama/all-minilm",
    }
    client = create_client(
        api_key="test-key",
        base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/local_embed_ollama",
    )
    result = await client.embed(request=request)
    assert len(result.data) == 1  # noqa: S101
    assert len(result.data.get("0").embedding) == 32  # noqa: S101
