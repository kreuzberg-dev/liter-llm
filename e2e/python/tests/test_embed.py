# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:02fecbe27c62649abf37ed8caa2d9d7be7f2ccea318c05271897e1affb70e2b7
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: embed."""

import os

import pytest
from liter_llm import create_client, AuthenticationError
from liter_llm._internal_bindings import EmbeddingRequest


@pytest.mark.asyncio

async def test_batch_embed() -> None:
    """Embedding request with multiple input strings returns one embedding object per input."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/batch_embed")
    req = EmbeddingRequest.from_json("{\"input\":[\"Hello\",\"World\"],\"model\":\"text-embedding-3-small\"}")

    result = await client.embed(req)
    assert len(result.data) == 2
    assert len(result.data[0].embedding) == 5


@pytest.mark.asyncio

async def test_edge_embed_batch_input() -> None:
    """Embedding request with multiple inputs returns multiple embedding objects."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_embed_batch_input")
    req = EmbeddingRequest.from_json("{\"input\":[\"Hello world\",\"Goodbye world\"],\"model\":\"text-embedding-3-small\"}")

    result = await client.embed(req)
    assert len(result.data) == 2
    assert result.data[0].index == 0
    assert result.data[1].index == 1


@pytest.mark.asyncio

async def test_edge_embed_empty_input() -> None:
    """Embedding request with empty string input returns empty data array."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_embed_empty_input")
    req = EmbeddingRequest.from_json("{\"input\":\"\",\"model\":\"text-embedding-3-small\"}")

    result = await client.embed(req)
    assert len(result.data) == 0


@pytest.mark.asyncio

async def test_embed_base64() -> None:
    """Embedding request with encoding_format base64 decodes to a float array embedding."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/embed_base64")
    req = EmbeddingRequest.from_json("{\"encoding_format\":\"base64\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}")

    result = await client.embed(req)
    assert len(result.data) == 1
    assert len(result.data[0].embedding) == 4


@pytest.mark.asyncio

async def test_embed_encoding_format() -> None:
    """Embedding request with explicit encoding_format of float returns float array embeddings."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/embed_encoding_format")
    req = EmbeddingRequest.from_json("{\"encoding_format\":\"float\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}")

    result = await client.embed(req)
    assert len(result.data) == 1
    assert len(result.data[0].embedding) == 5


@pytest.mark.asyncio

async def test_embed_error_401() -> None:
    """401 Unauthorized error on embedding request when API key is invalid."""
    with pytest.raises(AuthenticationError) as exc_info:
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/embed_error_401")
        req = EmbeddingRequest.from_json("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}")
        await client.embed(req)
    import liter_llm._internal_bindings  # noqa: PLC0415
    assert liter_llm._internal_bindings.liter_llm_error_info(exc_info.value).status_code == 401


@pytest.mark.asyncio

async def test_embed_with_dimensions() -> None:
    """Embedding request with explicit dimensions parameter returns embeddings of the requested size."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/embed_with_dimensions")
    req = EmbeddingRequest.from_json("{\"dimensions\":256,\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}")

    result = await client.embed(req)
    assert len(result.data) == 1
    assert len(result.data[0].embedding) == 8


@pytest.mark.asyncio

async def test_local_embed_ollama() -> None:
    """Embedding request via Ollama local provider with all-minilm model."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/local_embed_ollama")
    req = EmbeddingRequest.from_json("{\"input\":\"The quick brown fox jumps over the lazy dog\",\"model\":\"ollama/all-minilm\"}")

    result = await client.embed(req)
    assert len(result.data) == 1
    assert len(result.data[0].embedding) == 32
