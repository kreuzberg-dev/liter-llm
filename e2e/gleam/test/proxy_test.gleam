// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm

import gleam/int
import gleam/list

// 401 Unauthorized when an invalid API key is provided through the proxy
pub fn proxy_auth_invalid_test() {
  liter_llm.chat(Nil) |> should.be_error()
}

// 401 Unauthorized when no API key is provided through the proxy
pub fn proxy_auth_missing_test() {
  liter_llm.chat(Nil) |> should.be_error()
}

// Basic chat completion request routed through the proxy
pub fn proxy_chat_basic_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.length |> should.equal(1)
  result.choices.get("0").message.content |> should.equal("Hello!")
  result.choices.get("0").finish_reason |> should.equal("stop")
}

// Streaming chat completion routed through the proxy
pub fn proxy_chat_streaming_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(3) |> should.equal(True)
  result.stream_content |> should.equal("1 2 3")
  result.stream_complete |> should.equal(True)
}

// Embedding request routed through the proxy
pub fn proxy_embeddings_test() {
  let result = liter_llm.embed("Hello world")
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
}

// Health check verifying proxy connectivity via list models
pub fn proxy_health_test() {
  let result = liter_llm.list_models()
  result |> should.be_ok()
  result.data |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Image generation request routed through the proxy
pub fn proxy_image_generate_test() {
  let result = liter_llm.image_generate(Nil)
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").url |> list.is_empty |> should.equal(False)
}

// List models request routed through the proxy
pub fn proxy_models_list_test() {
  let result = liter_llm.list_models()
  result |> should.be_ok()
  result.data |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Content moderation request routed through the proxy
pub fn proxy_moderation_test() {
  let result = liter_llm.moderate("The weather is nice today.")
  result |> should.be_ok()
  result.results |> list.length |> should.equal(1)
  result.results.get("0").flagged |> should.equal(False)
}

// Document reranking request routed through the proxy
pub fn proxy_rerank_test() {
  let result = liter_llm.rerank(Nil)
  result |> should.be_ok()
  result.results |> list.length |> should.equal(2)
  result.results.get("0").relevance_score |> int.is_strictly_greater_than(0.9) |> should.equal(True)
}

// 429 Too Many Requests from upstream provider through the proxy
pub fn proxy_upstream_429_test() {
  liter_llm.chat(Nil) |> should.be_error()
}

// 500 Internal Server Error from upstream provider through the proxy
pub fn proxy_upstream_500_test() {
  liter_llm.chat(Nil) |> should.be_error()
}

