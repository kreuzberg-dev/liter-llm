// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm

import gleam/list

// Streaming chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) yielding multiple SSE chunks
pub fn anthropic_stream_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(3) |> should.equal(True)
  result.stream_content |> should.equal("One Two Three")
  result.stream_complete |> should.equal(True)
}

// Streaming chat completion via Azure OpenAI — verifies the azure/ prefix routes correctly and SSE chunks are delivered in the standard OpenAI chat.completion.chunk shape
pub fn azure_stream_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(3) |> should.equal(True)
  result.stream_content |> should.equal("1 2 3")
  result.stream_complete |> should.equal(True)
}

// Streaming chat completion that produces content across multiple SSE chunks
pub fn basic_stream_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(3) |> should.equal(True)
  result.stream_content |> should.equal("1 2 3")
}

// Streaming chat completion via the AWS Bedrock provider using the bedrock/ prefix — verifies SSE chunks are yielded and assembled correctly from the Converse streaming API
pub fn bedrock_stream_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(2) |> should.equal(True)
  result.stream_content |> should.equal("One Two Three")
  result.stream_complete |> should.equal(True)
}

// Streaming chat completion with tool/function call chunks
pub fn edge_stream_function_call_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(2) |> should.equal(True)
}

// Streaming chat completion that produces no content chunks before the DONE signal
pub fn empty_stream_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(0) |> should.equal(True)
  result.stream_content |> should.equal("")
}

// Streaming chat completion via Ollama local provider with SSE chunks
pub fn local_stream_ollama_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(3) |> should.equal(True)
  result.stream_content |> should.equal("1 2 3")
  result.stream_complete |> should.equal(True)
}

// 400 Bad Request error on stream due to content policy violation
pub fn stream_content_policy_error_test() {
  liter_llm.chat_stream(Nil) |> should.be_error()
}

// Verify that the [DONE] sentinel signal properly terminates the stream
pub fn stream_done_signal_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.stream_complete |> should.equal(True)
  result.stream_content |> should.equal("Done")
  result.no_chunks_after_done |> should.equal(True)
}

// 401 Unauthorized error on stream initiation before any chunks are received
pub fn stream_error_401_test() {
  liter_llm.chat_stream(Nil) |> should.be_error()
}

// Streaming chat completion with multiple choice outputs (n > 1)
pub fn stream_multiple_choices_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
}

// Streaming chat completion where the assistant responds with a tool call across multiple chunks
pub fn stream_with_tool_calls_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(4) |> should.equal(True)
  result.finish_reason |> should.equal("tool_calls")
  result.tool_calls |> list.is_empty |> should.equal(False)
  result.tool_calls.get("0").function.name |> should.equal("get_weather")
}

// Streaming chat completion that includes a usage summary in the final chunk
pub fn stream_with_usage_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(3) |> should.equal(True)
  result.stream_content |> should.equal("Hi there!")
  result.usage.total_tokens |> should.equal(18)
}

// Streaming chat completion via the Google Vertex AI provider using the vertex_ai/ prefix — verifies SSE chunks from the Gemini streaming endpoint are yielded and assembled correctly
pub fn vertex_stream_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(2) |> should.equal(True)
  result.stream_content |> should.equal("One Two Three")
  result.stream_complete |> should.equal(True)
}

