// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm

import gleam/list

// Embedding request with multiple input strings returns one embedding object per input
pub fn batch_embed_test() {
  let result = liter_llm.embed(["Hello", "World"])
  result |> should.be_ok()
  result.data |> list.length |> should.equal(2)
  result.data.get("0").embedding |> list.length |> should.equal(5)
}

// Embedding request with multiple inputs returns multiple embedding objects
pub fn edge_embed_batch_input_test() {
  let result = liter_llm.embed(["Hello world", "Goodbye world"])
  result |> should.be_ok()
  result.data |> list.length |> should.equal(2)
  result.data.get("0").index |> should.equal(0)
  result.data.get("1").index |> should.equal(1)
}

// Embedding request with empty string input returns empty data array
pub fn edge_embed_empty_input_test() {
  let result = liter_llm.embed("")
  result |> should.be_ok()
  result.data |> list.length |> should.equal(0)
}

// Embedding request with explicit encoding_format of float returns float array embeddings
pub fn embed_encoding_format_test() {
  let result = liter_llm.embed("Test input")
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").embedding |> list.length |> should.equal(5)
}

// 401 Unauthorized error on embedding request when API key is invalid
pub fn embed_error_401_test() {
  liter_llm.embed("Hello world") |> should.be_error()
}

// Embedding request with explicit dimensions parameter returns embeddings of the requested size
pub fn embed_with_dimensions_test() {
  let result = liter_llm.embed("Hello world")
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").embedding |> list.length |> should.equal(8)
}

// Embedding request via Ollama local provider with all-minilm model
pub fn local_embed_ollama_test() {
  let result = liter_llm.embed("The quick brown fox jumps over the lazy dog")
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").embedding |> list.length |> should.equal(32)
}

