// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm

import gleam/list

// Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages
pub fn anthropic_chat_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.length |> should.equal(1)
  result.choices.get("0").message.content |> should.equal("Hello!")
  result.choices.get("0").finish_reason |> should.equal("stop")
  result.model |> should.equal("claude-3-5-sonnet-20241022")
  result.usage.total_tokens |> should.equal(23)
}

// Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape
pub fn azure_chat_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.length |> should.equal(1)
  result.choices.get("0").message.content |> should.equal("Hello!")
  result.choices.get("0").finish_reason |> should.equal("stop")
  result.model |> should.equal("gpt-4")
  result.usage.total_tokens |> should.equal(13)
}

// Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged
pub fn azure_embed_test() {
  let result = liter_llm.embed("Hello world")
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").embedding |> list.length |> should.equal(1536)
}

// Basic chat completion with a single user message
pub fn basic_chat_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.length |> should.equal(1)
  result.choices.get("0").message.content |> should.equal("Hello!")
  result.choices.get("0").finish_reason |> should.equal("stop")
  result.usage.total_tokens |> should.equal(15)
  result.model |> should.equal("gpt-4")
}

// Basic embedding request for a single input string
pub fn basic_embed_test() {
  let result = liter_llm.embed("Hello world")
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").embedding |> list.length |> should.equal(5)
}

// List available models from the API
pub fn basic_list_models_test() {
  let result = liter_llm.list_models()
  result |> should.be_ok()
  result.data |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape
pub fn bedrock_chat_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.length |> should.equal(1)
  result.choices.get("0").message.content |> should.equal("Hello!")
  result.choices.get("0").finish_reason |> should.equal("stop")
  result.model |> should.equal("anthropic.claude-3-sonnet-20240229-v1:0")
  result.usage.total_tokens |> should.equal(15)
}

// Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message
pub fn github_copilot_chat_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.length |> should.equal(1)
  result.choices.get("0").message.content |> should.equal("Hello!")
  result.choices.get("0").finish_reason |> should.equal("stop")
  result.model |> should.equal("gpt-4o")
  result.usage.total_tokens |> should.equal(14)
}

// Chat completion against local Ollama with qwen2:0.5b model
pub fn local_chat_ollama_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.is_empty |> should.equal(False)
}

// List models from local Ollama instance
pub fn local_list_models_ollama_test() {
  let result = liter_llm.list_models()
  result |> should.be_ok()
  result.data |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Test in-memory caching by sending identical requests twice
pub fn smoke_cache_memory_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.is_empty |> should.equal(False)
}

// Basic chat completion against real Anthropic API
pub fn smoke_chat_anthropic_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.is_empty |> should.equal(False)
  result.usage |> list.is_empty |> should.equal(False)
}

// Basic chat completion against real Google Gemini API
pub fn smoke_chat_gemini_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.is_empty |> should.equal(False)
  result.usage |> list.is_empty |> should.equal(False)
}

// Basic chat completion against real OpenAI API
pub fn smoke_chat_openai_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.is_empty |> should.equal(False)
  result.usage |> list.is_empty |> should.equal(False)
}

// Embeddings request against real OpenAI API
pub fn smoke_embed_openai_test() {
  let result = liter_llm.embed(["Hello world"])
  result |> should.be_ok()
  result.data |> list.is_empty |> should.equal(False)
}

// List models against real OpenAI API
pub fn smoke_list_models_openai_test() {
  let result = liter_llm.list_models()
  result |> should.be_ok()
  result.data |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Test provider routing by sending requests to OpenAI and Anthropic
pub fn smoke_provider_routing_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.is_empty |> should.equal(False)
}

// Chat streaming against real OpenAI API, verifies chunks received
pub fn smoke_streaming_openai_test() {
  let result = liter_llm.chat_stream(Nil)
  result |> should.be_ok()
  result.chunks |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape
pub fn vertex_chat_test() {
  let result = liter_llm.chat(Nil)
  result |> should.be_ok()
  result.choices |> list.length |> should.equal(1)
  result.choices.get("0").message.content |> should.equal("Hello!")
  result.choices.get("0").finish_reason |> should.equal("stop")
  result.model |> should.equal("gemini-2.0-flash")
  result.usage.total_tokens |> should.equal(13)
}

// Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape
pub fn vertex_embed_test() {
  let result = liter_llm.embed("Hello")
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").embedding |> list.length |> should.equal(160)
}

