// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm

import gleam/list
import gleam/string

// Transcription of a silent or empty audio file returns empty text
pub fn edge_transcribe_empty_audio_test() {
  let result = liter_llm.transcribe(Nil)
  result |> should.be_ok()
  result.text |> string.contains("") |> should.equal(True)
}

// Transcription with verbose JSON response format including timestamp segments
pub fn edge_transcribe_with_timestamps_test() {
  let result = liter_llm.transcribe(Nil)
  result |> should.be_ok()
  result.text |> list.is_empty |> should.equal(False)
  result.segments |> list.length |> should.equal(3)
  result.segments.get("0").id |> should.equal(0)
}

// 401 Unauthorized for transcription with invalid API key
pub fn error_transcribe_auth_401_test() {
  liter_llm.transcribe(Nil) |> should.be_error()
}

// 400 Bad Request when audio format is unsupported
pub fn error_transcribe_bad_format_test() {
  liter_llm.transcribe(Nil) |> should.be_error()
}

// Basic audio transcription
pub fn smoke_transcribe_basic_test() {
  let result = liter_llm.transcribe(Nil)
  result |> should.be_ok()
  result.text |> string.contains("Hello, this is a test transcription.") |> should.equal(True)
}

// Audio transcription with explicit language hint
pub fn smoke_transcribe_with_language_test() {
  let result = liter_llm.transcribe(Nil)
  result |> should.be_ok()
  result.text |> string.contains("Hallo, dies ist ein Testtranskription.") |> should.equal(True)
}

