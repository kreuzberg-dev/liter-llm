// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm

import gleam/list

// Moderation response with multiple categories flagged
pub fn edge_moderate_all_categories_test() {
  let result = liter_llm.moderate("Extremely harmful content targeting multiple categories")
  result |> should.be_ok()
  result.results |> list.length |> should.equal(1)
  result.results.get("0").flagged |> should.equal(True)
}

// Moderation with empty string input
pub fn edge_moderate_empty_input_test() {
  let result = liter_llm.moderate("")
  result |> should.be_ok()
  result.results |> list.length |> should.equal(1)
  result.results.get("0").flagged |> should.equal(False)
}

// 401 Unauthorized for moderation with invalid API key
pub fn error_moderate_auth_401_test() {
  liter_llm.moderate("Hello") |> should.be_error()
}

// 400 Bad Request for moderation with invalid model
pub fn error_moderate_bad_request_test() {
  liter_llm.moderate("Hello") |> should.be_error()
}

// Moderate multiple inputs in a single request
pub fn smoke_moderate_batch_test() {
  let result = liter_llm.moderate(["Hello world", "Nice weather today"])
  result |> should.be_ok()
  result.results |> list.length |> should.equal(2)
  result.results.get("0").flagged |> should.equal(False)
}

// Moderation detects flagged content
pub fn smoke_moderate_flagged_test() {
  let result = liter_llm.moderate("I want to hurt someone very badly")
  result |> should.be_ok()
  result.results |> list.length |> should.equal(1)
  result.results.get("0").flagged |> should.equal(True)
}

// Moderate a single non-flagged input
pub fn smoke_moderate_single_test() {
  let result = liter_llm.moderate("The weather is nice today.")
  result |> should.be_ok()
  result.results |> list.length |> should.equal(1)
  result.results.get("0").flagged |> should.equal(False)
}

