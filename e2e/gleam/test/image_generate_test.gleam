// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3bd2519d40f376be867de515d5c1273618b62735a89533beb24bd00bb73d6881
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm

import gleam/list

// Image generation returning base64-encoded data instead of URL
pub fn edge_image_b64_response_test() {
  let result = liter_llm.image_generate(Nil)
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").b64_json |> list.is_empty |> should.equal(False)
}

// Image generation with an empty prompt returns 400
pub fn edge_image_empty_prompt_test() {
  liter_llm.image_generate(Nil) |> should.be_error()
}

// Image generation requesting multiple images with n=3
pub fn edge_image_multiple_n_test() {
  let result = liter_llm.image_generate(Nil)
  result |> should.be_ok()
  result.data |> list.length |> should.equal(3)
  result.data.get("0").url |> list.is_empty |> should.equal(False)
  result.data.get("1").url |> list.is_empty |> should.equal(False)
  result.data.get("2").url |> list.is_empty |> should.equal(False)
}

// 401 Unauthorized when generating images with invalid API key
pub fn error_image_auth_401_test() {
  liter_llm.image_generate(Nil) |> should.be_error()
}

// 400 Bad Request when image generation parameters are invalid
pub fn error_image_bad_request_test() {
  liter_llm.image_generate(Nil) |> should.be_error()
}

// 429 Rate limit exceeded for image generation
pub fn error_image_rate_limit_test() {
  liter_llm.image_generate(Nil) |> should.be_error()
}

// Basic image generation with a text prompt
pub fn smoke_image_basic_test() {
  let result = liter_llm.image_generate(Nil)
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").url |> list.is_empty |> should.equal(False)
}

// Image generation requesting multiple images
pub fn smoke_image_multiple_test() {
  let result = liter_llm.image_generate(Nil)
  result |> should.be_ok()
  result.data |> list.length |> should.equal(3)
  result.data.get("0").url |> list.is_empty |> should.equal(False)
}

// Image generation with explicit size parameter
pub fn smoke_image_with_size_test() {
  let result = liter_llm.image_generate(Nil)
  result |> should.be_ok()
  result.data |> list.length |> should.equal(1)
  result.data.get("0").url |> list.is_empty |> should.equal(False)
}

