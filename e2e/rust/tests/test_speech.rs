// Code generated by liter-llm-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

#[path = "mock_server.rs"]
mod mock_server;

use liter_llm::{ClientConfigBuilder, DefaultClient, LlmClient};

/// Speech generation with a very long input text
#[tokio::test]
async fn edge_speech_long_input() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock binary audio data for long text"}"#
            .to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "speech"

    server.shutdown();
}

/// 401 Unauthorized for speech generation with invalid API key
#[tokio::test]
async fn error_speech_auth_401() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/audio/speech",
            method: "POST",
            status: 401,
            body: r#"{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "speech"

    server.shutdown();
}

/// 400 Bad Request for speech with unsupported model
#[tokio::test]
async fn error_speech_bad_model() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/audio/speech",
            method: "POST",
            status: 400,
            body: r#"{"error":{"code":"model_not_found","message":"The model 'tts-nonexistent' does not exist.","param":"model","type":"invalid_request_error"}}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "speech"

    server.shutdown();
}

/// Basic text-to-speech generation
#[tokio::test]
async fn smoke_speech_basic() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock binary audio data"}"#.to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "speech"

    server.shutdown();
}

/// Text-to-speech with explicit MP3 response format
#[tokio::test]
async fn smoke_speech_mp3_format() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock MP3 audio data"}"#.to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "speech"

    server.shutdown();
}
