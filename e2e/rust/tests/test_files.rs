// Code generated by liter-llm-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

#[path = "mock_server.rs"]
mod mock_server;

use liter_llm::{ClientConfigBuilder, DefaultClient, LlmClient};

/// List files when no files have been uploaded
#[tokio::test]
async fn edge_file_empty_list() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/files",
        method: "GET",
        status: 200,
        body: r#"{"data":[],"object":"list"}"#.to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "list_files"

    server.shutdown();
}

/// Upload a large file successfully
#[tokio::test]
async fn edge_file_large_upload() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/files",
            method: "POST",
            status: 200,
            body: r#"{"bytes":536870912,"created_at":1711000000,"filename":"large_training_data.jsonl","id":"file-large001","object":"file","purpose":"fine-tune","status":"processed"}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "create_file"

    server.shutdown();
}

/// 401 Unauthorized when listing files with invalid API key
#[tokio::test]
async fn error_file_auth_401() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/files",
            method: "GET",
            status: 401,
            body: r#"{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "list_files"

    server.shutdown();
}

/// 400 Bad Request when uploading a file with invalid purpose
#[tokio::test]
async fn error_file_bad_purpose() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/files",
            method: "POST",
            status: 400,
            body: r#"{"error":{"code":null,"message":"Invalid value for 'purpose': 'invalid-purpose'. Supported values are: 'fine-tune', 'assistants', 'batch'.","param":"purpose","type":"invalid_request_error"}}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "create_file"

    server.shutdown();
}

/// 404 Not Found when retrieving a nonexistent file
#[tokio::test]
async fn error_file_not_found() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/files/file-placeholder",
            method: "GET",
            status: 404,
            body: r#"{"error":{"code":null,"message":"No such File object: file-nonexistent","param":null,"type":"invalid_request_error"}}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "retrieve_file"

    server.shutdown();
}

/// Upload a file for use with the API
#[tokio::test]
async fn smoke_create_file() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/files",
            method: "POST",
            status: 200,
            body: r#"{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "create_file"

    server.shutdown();
}

/// Delete an uploaded file
#[tokio::test]
async fn smoke_delete_file() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/files/file-placeholder",
        method: "POST",
        status: 200,
        body: r#"{"deleted":true,"id":"file-abc123","object":"file"}"#.to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "delete_file"

    server.shutdown();
}

/// Retrieve the content of an uploaded file
#[tokio::test]
async fn smoke_file_content() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/files/file-placeholder/content",
        method: "GET",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"application/octet-stream","_description":"Mock file content bytes"}"#
            .to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "file_content"

    server.shutdown();
}

/// List all uploaded files
#[tokio::test]
async fn smoke_list_files() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/files",
            method: "GET",
            status: 200,
            body: r#"{"data":[{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"},{"bytes":2048,"created_at":1711000100,"filename":"batch_input.jsonl","id":"file-def456","object":"file","purpose":"batch","status":"processed"}],"object":"list"}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "list_files"

    server.shutdown();
}

/// Retrieve metadata for an uploaded file
#[tokio::test]
async fn smoke_retrieve_file() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/files/file-placeholder",
            method: "GET",
            status: 200,
            body: r#"{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "retrieve_file"

    server.shutdown();
}
