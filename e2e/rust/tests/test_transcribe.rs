// Code generated by liter-llm-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

#[path = "mock_server.rs"]
mod mock_server;

use liter_llm::{ClientConfigBuilder, DefaultClient, LlmClient};

/// Transcription of a silent or empty audio file returns empty text
#[tokio::test]
async fn edge_transcribe_empty_audio() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/audio/transcriptions",
        method: "POST",
        status: 200,
        body: r#"{"text":""}"#.to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "transcribe"

    server.shutdown();
}

/// 401 Unauthorized for transcription with invalid API key
#[tokio::test]
async fn error_transcribe_auth_401() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/audio/transcriptions",
            method: "POST",
            status: 401,
            body: r#"{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "transcribe"

    server.shutdown();
}

/// 400 Bad Request when audio format is unsupported
#[tokio::test]
async fn error_transcribe_bad_format() {
    let server = mock_server::MockServer::start(vec![
        mock_server::MockRoute {
            path: "/audio/transcriptions",
            method: "POST",
            status: 400,
            body: r#"{"error":{"code":null,"message":"Invalid file format. Supported formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, webm.","param":"file","type":"invalid_request_error"}}"#.to_string(),
            stream_chunks: vec![],
        },
    ]).await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "transcribe"

    server.shutdown();
}

/// Basic audio transcription
#[tokio::test]
async fn smoke_transcribe_basic() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/audio/transcriptions",
        method: "POST",
        status: 200,
        body: r#"{"text":"Hello, this is a test transcription."}"#.to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "transcribe"

    server.shutdown();
}

/// Audio transcription with explicit language hint
#[tokio::test]
async fn smoke_transcribe_with_language() {
    let server = mock_server::MockServer::start(vec![mock_server::MockRoute {
        path: "/audio/transcriptions",
        method: "POST",
        status: 200,
        body: r#"{"text":"Hallo, dies ist ein Testtranskription."}"#.to_string(),
        stream_chunks: vec![],
    }])
    .await;

    let config = ClientConfigBuilder::new("test-key")
        .base_url(&server.url)
        .max_retries(0)
        .build();
    let client = DefaultClient::new(config, None).unwrap();

    // TODO: unknown method "transcribe"

    server.shutdown();
}
