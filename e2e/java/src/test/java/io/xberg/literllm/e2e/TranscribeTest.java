// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:814e4a06b4555bbe6da3c1521d396d0009bdb15b126bbc6a7586105d4dbb4494
// To regenerate: alef generate
// To verify freshness: alef verify

package io.xberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.literllm.LiterLlm;
import io.xberg.literllm.CreateTranscriptionRequest;
import java.util.Optional;
import io.xberg.literllm.JsonUtil;

/** E2e tests for category: transcribe. */
public class TranscribeTest {


    @Test
    void testEdgeTranscribeEmptyAudio() throws Exception {
        // Transcription of a silent or empty audio file returns empty text
        var req = JsonUtil.fromJson("{\"file\":\"silence.mp3\",\"model\":\"whisper-1\"}", CreateTranscriptionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_transcribe_empty_audio";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.transcribe(req);
        assertTrue(result.text().contains(""), "expected to contain: " + "");


    }


    @Test
    void testEdgeTranscribeWithTimestamps() throws Exception {
        // Transcription with verbose JSON response format including timestamp segments
        var req = JsonUtil.fromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\",\"response_format\":\"verbose_json\"}", CreateTranscriptionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_transcribe_with_timestamps";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.transcribe(req);
        assertFalse(result.text().isEmpty(), "expected non-empty value");
        assertEquals(3, java.util.Optional.ofNullable(result.segments()).orElse(java.util.List.of()).size(), "expected exactly 3 elements");
        assertEquals(0, result.segments().get(0).id());


    }


    @Test
    void testErrorTranscribeAuth401() throws Exception {
        // 401 Unauthorized for transcription with invalid API key
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
        var req = JsonUtil.fromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}", CreateTranscriptionRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_transcribe_auth_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.transcribe(req);
        });
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testErrorTranscribeBadFormat() throws Exception {
        // 400 Bad Request when audio format is unsupported
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
        var req = JsonUtil.fromJson("{\"file\":\"audio.xyz\",\"model\":\"whisper-1\"}", CreateTranscriptionRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_transcribe_bad_format";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.transcribe(req);
        });
        // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testSmokeTranscribeBasic() throws Exception {
        // Basic audio transcription
        var req = JsonUtil.fromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}", CreateTranscriptionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_transcribe_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.transcribe(req);
        assertTrue(result.text().contains("Hello, this is a test transcription."), "expected to contain: " + "Hello, this is a test transcription.");


    }


    @Test
    void testSmokeTranscribeWithLanguage() throws Exception {
        // Audio transcription with explicit language hint
        var req = JsonUtil.fromJson("{\"file\":\"audio_de.mp3\",\"language\":\"de\",\"model\":\"whisper-1\"}", CreateTranscriptionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_transcribe_with_language";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.transcribe(req);
        assertTrue(result.text().contains("Hallo, dies ist ein Testtranskription."), "expected to contain: " + "Hallo, dies ist ein Testtranskription.");


    }

}
