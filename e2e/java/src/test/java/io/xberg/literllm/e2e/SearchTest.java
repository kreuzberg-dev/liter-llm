// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9fa6febca03af12f5a3503c4b7ecab9974a41ab34250a50e1f03163176b39a23
// To regenerate: alef generate
// To verify freshness: alef verify

package io.xberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.literllm.LiterLlm;
import io.xberg.literllm.SearchRequest;
import java.util.Optional;
import io.xberg.literllm.JsonUtil;

/** E2e tests for category: search. */
public class SearchTest {


    @Test
    void testSearchBasic() throws Exception {
        // Basic web search request with a simple query
        var req = JsonUtil.fromJson("{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}", SearchRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/search_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.search(req);
        assertNotNull(result, "expected non-null response");


    }


    @Test
    void testSearchEmptyResults() throws Exception {
        // Web search with a query that returns no results
        var req = JsonUtil.fromJson("{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}", SearchRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/search_empty_results";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.search(req);
        assertNotNull(result, "expected non-null response");


    }


    @Test
    void testSearchError400() throws Exception {
        // 400 Bad Request error when search query is empty
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
        var req = JsonUtil.fromJson("{\"model\":\"brave/web-search\",\"query\":\"\"}", SearchRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/search_error_400";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.search(req);
        });
        // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testSearchError401() throws Exception {
        // 401 Unauthorized error on web search due to invalid API credentials
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
        var req = JsonUtil.fromJson("{\"model\":\"brave/web-search\",\"query\":\"test\"}", SearchRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/search_error_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.search(req);
        });
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testSearchWithMaxResults() throws Exception {
        // Search request with max_results parameter limiting response count
        var req = JsonUtil.fromJson("{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}", SearchRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/search_with_max_results";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.search(req);
        assertEquals(2, result.results().size(), "expected exactly 2 elements");


    }

}
