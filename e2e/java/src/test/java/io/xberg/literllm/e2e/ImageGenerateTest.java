// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a35e1f36c051d67229595c46fe0f98ea79dc9462cef2f2396eae300d92bcc38c
// To regenerate: alef generate
// To verify freshness: alef verify

package io.xberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.literllm.LiterLlm;
import io.xberg.literllm.CreateImageRequest;
import java.util.Optional;
import io.xberg.literllm.JsonUtil;

/** E2e tests for category: image-generate. */
public class ImageGenerateTest {


    @Test
    void testEdgeImageB64Response() throws Exception {
        // Image generation returning base64-encoded data instead of URL
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}", CreateImageRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_image_b64_response";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(req);
        assertEquals(1, result.data().size(), "expected exactly 1 elements");
        assertTrue(java.util.Optional.ofNullable(result.data().get(0).b64Json()).filter(value -> switch ((Object) value) {
            case CharSequence text -> !text.isEmpty();
            case java.util.Collection<?> items -> !items.isEmpty();
            case java.util.Map<?, ?> entries -> !entries.isEmpty();
            default -> true;
        }).isPresent(), "expected non-empty value");


    }


    @Test
    void testEdgeImageEmptyPrompt() throws Exception {
        // Image generation with an empty prompt returns 400
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}", CreateImageRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_image_empty_prompt";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.imageGenerate(req);
        });
        // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testEdgeImageMultipleN() throws Exception {
        // Image generation requesting multiple images with n=3
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-3\",\"n\":3,\"prompt\":\"A cat\"}", CreateImageRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_image_multiple_n";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(req);
        assertEquals(3, result.data().size(), "expected exactly 3 elements");
        assertTrue(java.util.Optional.ofNullable(result.data().get(0).url()).filter(value -> switch ((Object) value) {
            case CharSequence text -> !text.isEmpty();
            case java.util.Collection<?> items -> !items.isEmpty();
            case java.util.Map<?, ?> entries -> !entries.isEmpty();
            default -> true;
        }).isPresent(), "expected non-empty value");
        assertTrue(java.util.Optional.ofNullable(result.data().get(1).url()).filter(value -> switch ((Object) value) {
            case CharSequence text -> !text.isEmpty();
            case java.util.Collection<?> items -> !items.isEmpty();
            case java.util.Map<?, ?> entries -> !entries.isEmpty();
            default -> true;
        }).isPresent(), "expected non-empty value");
        assertTrue(java.util.Optional.ofNullable(result.data().get(2).url()).filter(value -> switch ((Object) value) {
            case CharSequence text -> !text.isEmpty();
            case java.util.Collection<?> items -> !items.isEmpty();
            case java.util.Map<?, ?> entries -> !entries.isEmpty();
            default -> true;
        }).isPresent(), "expected non-empty value");


    }


    @Test
    void testErrorImageAuth401() throws Exception {
        // 401 Unauthorized when generating images with invalid API key
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}", CreateImageRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_image_auth_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.imageGenerate(req);
        });
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testErrorImageBadRequest() throws Exception {
        // 400 Bad Request when image generation parameters are invalid
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}", CreateImageRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_image_bad_request";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.imageGenerate(req);
        });
        // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testErrorImageRateLimit() throws Exception {
        // 429 Rate limit exceeded for image generation
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}", CreateImageRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_image_rate_limit";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.imageGenerate(req);
        });
        // skipped: declared error variant 'RateLimited' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testSmokeImageBasic() throws Exception {
        // Basic image generation with a text prompt
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}", CreateImageRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_image_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(req);
        assertEquals(1, result.data().size(), "expected exactly 1 elements");
        assertTrue(java.util.Optional.ofNullable(result.data().get(0).url()).filter(value -> switch ((Object) value) {
            case CharSequence text -> !text.isEmpty();
            case java.util.Collection<?> items -> !items.isEmpty();
            case java.util.Map<?, ?> entries -> !entries.isEmpty();
            default -> true;
        }).isPresent(), "expected non-empty value");


    }


    @Test
    void testSmokeImageMultiple() throws Exception {
        // Image generation requesting multiple images
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}", CreateImageRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_image_multiple";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(req);
        assertEquals(3, result.data().size(), "expected exactly 3 elements");
        assertTrue(java.util.Optional.ofNullable(result.data().get(0).url()).filter(value -> switch ((Object) value) {
            case CharSequence text -> !text.isEmpty();
            case java.util.Collection<?> items -> !items.isEmpty();
            case java.util.Map<?, ?> entries -> !entries.isEmpty();
            default -> true;
        }).isPresent(), "expected non-empty value");


    }


    @Test
    void testSmokeImageWithSize() throws Exception {
        // Image generation with explicit size parameter
        var req = JsonUtil.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}", CreateImageRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_image_with_size";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(req);
        assertEquals(1, result.data().size(), "expected exactly 1 elements");
        assertTrue(java.util.Optional.ofNullable(result.data().get(0).url()).filter(value -> switch ((Object) value) {
            case CharSequence text -> !text.isEmpty();
            case java.util.Collection<?> items -> !items.isEmpty();
            case java.util.Map<?, ?> entries -> !entries.isEmpty();
            default -> true;
        }).isPresent(), "expected non-empty value");


    }

}
