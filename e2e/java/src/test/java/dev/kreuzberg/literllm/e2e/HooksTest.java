// Code generated by liter-llm-e2e-generator. DO NOT EDIT.
package dev.kreuzberg.literllm.e2e;

import static org.junit.jupiter.api.Assertions.*;

import java.util.List;
import org.junit.jupiter.api.Test;

/** E2E tests for category: hooks. */
class HooksTest {

  /** Tests that on_request hook can reject a request */
  @Test
  void hookGuardrail() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(new Helpers.MockRoute("/chat/completions", "POST", 200, "{}")))) {
      String mockUrl = server.url;

      // TDD: Hook tests — will fail until hooks feature is implemented.
      var config = new LlmClientConfig.Builder().apiKey("test-key").baseUrl(mockUrl).build();
      var client = new LlmClient(config);

      client.addHook(
          "on_request",
          (req) -> {
            throw new HookRejectedException("Blocked by guardrail");
          });

      var request =
          "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}";
      assertThrows(
          HookRejectedException.class,
          () -> client.chat(request),
          "Expected HookRejectedException from guardrail hook");
    }
  }

  /** Tests that on_error hook is called on failure */
  @Test
  void hookOnError() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/chat/completions",
                    "POST",
                    500,
                    "{\"error\":{\"code\":\"internal_error\",\"message\":\"Internal server"
                        + " error\",\"type\":\"server_error\"}}")))) {
      String mockUrl = server.url;

      // TDD: Hook tests — will fail until hooks feature is implemented.
      var config = new LlmClientConfig.Builder().apiKey("test-key").baseUrl(mockUrl).build();
      var client = new LlmClient(config);

      var hookCalled = new java.util.concurrent.atomic.AtomicBoolean(false);
      client.addHook("on_error", (err) -> hookCalled.set(true));

      var request =
          "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}";
      try {
        client.chat(request);
      } catch (Exception ignored) {
      }
      assertTrue(hookCalled.get(), "Expected on_error hook to be called");
    }
  }

  /** Tests that on_request hook is called before the request */
  @Test
  void hookOnRequest() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/chat/completions",
                    "POST",
                    200,
                    "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"Hello!"
                        + " How can I help you"
                        + " today?\",\"role\":\"assistant\"}}],\"created\":1711000000,\"id\":\"chatcmpl-hook-req-001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":9,\"prompt_tokens\":8,\"total_tokens\":17}}")))) {
      String mockUrl = server.url;

      // TDD: Hook tests — will fail until hooks feature is implemented.
      var config = new LlmClientConfig.Builder().apiKey("test-key").baseUrl(mockUrl).build();
      var client = new LlmClient(config);

      var hookCalled = new java.util.concurrent.atomic.AtomicBoolean(false);
      client.addHook("on_request", (req) -> hookCalled.set(true));

      var request =
          "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}";
      client.chat(request);
      assertTrue(hookCalled.get(), "Expected on_request hook to be called");
    }
  }

  /** Tests that on_response hook is called with response data */
  @Test
  void hookOnResponse() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/chat/completions",
                    "POST",
                    200,
                    "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"Hello!"
                        + " How can I help you"
                        + " today?\",\"role\":\"assistant\"}}],\"created\":1711000000,\"id\":\"chatcmpl-hook-resp-001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":9,\"prompt_tokens\":8,\"total_tokens\":17}}")))) {
      String mockUrl = server.url;

      // TDD: Hook tests — will fail until hooks feature is implemented.
      var config = new LlmClientConfig.Builder().apiKey("test-key").baseUrl(mockUrl).build();
      var client = new LlmClient(config);

      var hookCalled = new java.util.concurrent.atomic.AtomicBoolean(false);
      client.addHook("on_response", (resp) -> hookCalled.set(true));

      var request =
          "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}";
      client.chat(request);
      assertTrue(hookCalled.get(), "Expected on_response hook to be called");
    }
  }
}
