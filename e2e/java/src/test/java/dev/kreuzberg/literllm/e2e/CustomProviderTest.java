// Code generated by liter-llm-e2e-generator. DO NOT EDIT.
package dev.kreuzberg.literllm.e2e;

import static org.junit.jupiter.api.Assertions.*;

import java.util.List;
import org.junit.jupiter.api.Test;

/** E2E tests for category: custom_provider. */
class CustomProviderTest {

  /** Tests custom provider with custom auth header */
  @Test
  void providerAuth() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/chat/completions",
                    "POST",
                    200,
                    "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"Hello"
                        + " with custom"
                        + " auth!\",\"role\":\"assistant\"}}],\"created\":1711000000,\"id\":\"chatcmpl-auth-001\",\"model\":\"my-auth-model-v1\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":6,\"prompt_tokens\":8,\"total_tokens\":14}}")))) {
      String mockUrl = server.url;

      // TDD: Custom provider tests — will fail until custom provider registration is implemented.
      var config = new LlmClientConfig.Builder().apiKey("test-key").build();
      var client = new LlmClient(config);

      client.registerProvider(new ProviderConfig("my-auth-provider", mockUrl, List.of("my-auth-")));

      var request =
          "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"my-auth-model-v1\"}";
      var resp = client.chat(request);
      assertNotNull(resp, "Expected response from custom provider");
      assertEquals("Hello with custom auth!", resp.firstChoiceContent(), "response content");
    }
  }

  /** Tests that a custom provider can be registered and routes requests */
  @Test
  void registerProvider() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/chat/completions",
                    "POST",
                    200,
                    "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"Hello"
                        + " from custom"
                        + " provider!\",\"role\":\"assistant\"}}],\"created\":1711000000,\"id\":\"chatcmpl-custom-001\",\"model\":\"my-model-v1\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":7,\"prompt_tokens\":8,\"total_tokens\":15}}")))) {
      String mockUrl = server.url;

      // TDD: Custom provider tests — will fail until custom provider registration is implemented.
      var config = new LlmClientConfig.Builder().apiKey("test-key").build();
      var client = new LlmClient(config);

      client.registerProvider(new ProviderConfig("my-provider", mockUrl, List.of("my-")));

      var request =
          "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"my-model-v1\"}";
      var resp = client.chat(request);
      assertNotNull(resp, "Expected response from custom provider");
      assertEquals("Hello from custom provider!", resp.firstChoiceContent(), "response content");
    }
  }
}
