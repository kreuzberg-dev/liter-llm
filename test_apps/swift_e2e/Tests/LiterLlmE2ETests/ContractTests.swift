// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b8ae0137845283f9ef5da27f124f18137171bc6207208410ada661de56ba216f
// To regenerate: alef generate
// To verify freshness: alef verify
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import LiterLlm
import RustBridge

/// E2e tests for category: contract.
final class ContractTests: XCTestCase {
    override class func setUp() {
        super.setUp()
    }

    func testBindingApiParity() async throws {
        // Verify all bindings expose the full API surface — constructor accepts all config options and every method exists
        let _req = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/binding_api_parity")
        let result = try await _client.chat(_req)
        XCTAssertNotNil(result)
        XCTAssertEqual(result.choices().count, 1)
        // skipped: enum field 'choices[0].message.content' is a payload-carrying union with no scalar wire accessor in this binding
        let _vec_choices_equals_f15f117a_1 = result.choices()
        XCTAssertEqual((_vec_choices_equals_f15f117a_1[0].finishReason()?.toString() ?? ""), "stop")
        XCTAssertEqual(result.usage()?.totalTokens(), 6)
        XCTAssertEqual(result.model().toString(), "gpt-4o")
    }

    func testContractOcr() async throws {
        // Verify ocr() method exists in all bindings
        let _req = try LiterLlm.ocrRequestFromJson("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/contract-test.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/contract_ocr")
        let result = try await _client.ocr(_req)
        XCTAssertNotNil(result)
    }

    func testContractSearch() async throws {
        // Verify search() method exists in all bindings
        let _req = try LiterLlm.searchRequestFromJson("{\"model\":\"brave/web-search\",\"query\":\"contract test query\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/contract_search")
        let result = try await _client.search(_req)
        XCTAssertNotNil(result)
    }

}
