// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1b3963e66c65b3ed945e5aa2ab4b9e71e26183ac62b02abbc1548523eb0f3fa2
// To regenerate: alef generate
// To verify freshness: alef verify
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import LiterLlm
import RustBridge

/// E2e tests for category: image-generate.
final class ImageGenerateTests: XCTestCase {
    override class func setUp() {
        super.setUp()
    }

    func testEdgeImageB64Response() async throws {
        // Image generation returning base64-encoded data instead of URL
        let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/edge_image_b64_response")
        let result = try await _client.imageGenerate(_req)
        XCTAssertNotNil(result)
        XCTAssertEqual(result.data.count, 1)
        XCTAssertTrue(result.data[0].b64Json != nil, "expected non-empty value")
    }

    func testEdgeImageEmptyPrompt() async throws {
        // Image generation with an empty prompt returns 400
        do {
            let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}")
            let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/edge_image_empty_prompt")
            _ = try await _client.imageGenerate(_req)
            XCTFail("expected to throw")
        } catch {
            // skipped: declared error variant 'BadRequest' not yet preserved as a distinct identity by this backend's generator
        }
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    }

    func testEdgeImageMultipleN() async throws {
        // Image generation requesting multiple images with n=3
        let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":3,\"prompt\":\"A cat\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/edge_image_multiple_n")
        let result = try await _client.imageGenerate(_req)
        XCTAssertNotNil(result)
        XCTAssertEqual(result.data.count, 3)
        XCTAssertTrue(result.data[0].url != nil, "expected non-empty value")
        XCTAssertTrue(result.data[1].url != nil, "expected non-empty value")
        XCTAssertTrue(result.data[2].url != nil, "expected non-empty value")
    }

    func testErrorImageAuth401() async throws {
        // 401 Unauthorized when generating images with invalid API key
        do {
            let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}")
            let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/error_image_auth_401")
            _ = try await _client.imageGenerate(_req)
            XCTFail("expected to throw")
        } catch {
            // skipped: declared error variant 'Authentication' not yet preserved as a distinct identity by this backend's generator
        }
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    }

    func testErrorImageBadRequest() async throws {
        // 400 Bad Request when image generation parameters are invalid
        do {
            let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}")
            let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/error_image_bad_request")
            _ = try await _client.imageGenerate(_req)
            XCTFail("expected to throw")
        } catch {
            // skipped: declared error variant 'BadRequest' not yet preserved as a distinct identity by this backend's generator
        }
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    }

    func testErrorImageRateLimit() async throws {
        // 429 Rate limit exceeded for image generation
        do {
            let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}")
            let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/error_image_rate_limit")
            _ = try await _client.imageGenerate(_req)
            XCTFail("expected to throw")
        } catch {
            // skipped: declared error variant 'RateLimited' not yet preserved as a distinct identity by this backend's generator
        }
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    }

    func testSmokeImageBasic() async throws {
        // Basic image generation with a text prompt
        let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/smoke_image_basic")
        let result = try await _client.imageGenerate(_req)
        XCTAssertNotNil(result)
        XCTAssertEqual(result.data.count, 1)
        XCTAssertTrue(result.data[0].url != nil, "expected non-empty value")
    }

    func testSmokeImageMultiple() async throws {
        // Image generation requesting multiple images
        let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/smoke_image_multiple")
        let result = try await _client.imageGenerate(_req)
        XCTAssertNotNil(result)
        XCTAssertEqual(result.data.count, 3)
        XCTAssertTrue(result.data[0].url != nil, "expected non-empty value")
    }

    func testSmokeImageWithSize() async throws {
        // Image generation with explicit size parameter
        let _req = try LiterLlm.createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/smoke_image_with_size")
        let result = try await _client.imageGenerate(_req)
        XCTAssertNotNil(result)
        XCTAssertEqual(result.data.count, 1)
        XCTAssertTrue(result.data[0].url != nil, "expected non-empty value")
    }

}
