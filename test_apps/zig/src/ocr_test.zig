// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cd04de55cb41534fcfd73013541e02baa0d2b64b587d8c63739358134408f32d
// To regenerate: alef generate
// To verify freshness: alef verify
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: ocr

test "ocr_error_400" {
    // 400 Bad Request error when OCR input has an invalid image format
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/ocr_error_400", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.ocr("{\"document\":{\"type\":\"document_url\",\"url\":\"invalid://url\"},\"model\":\"mistral/mistral-ocr-latest\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "ocr_error_401" {
    // 401 Unauthorized error on OCR request due to invalid API credentials
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/ocr_error_401", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    if (_client.ocr("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/doc.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}")) |_| {
        return error.TestUnexpectedResult;
    } else |_err| {
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        const _err_name = @errorName(_err);
        const _err_message: []const u8 = liter_llm._last_error() orelse "";
        try testing.expect(_err_message.len > 0 or !std.mem.eql(u8, _err_name, "UnknownFfiError"));
    }
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

test "ocr_multi_page" {
    // OCR request returning multiple pages of document content
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/ocr_multi_page", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.ocr("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/multipage.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(usize, 2), (result.object.get("pages") orelse std.json.Value{ .null = {} }).array.items.len);
    try testing.expectEqual(@as(i64, 0), (result.object.get("pages") orelse std.json.Value{ .null = {} }).array.items[0].object.get("index").?.integer);
    try testing.expectEqual(@as(i64, 1), (result.object.get("pages") orelse std.json.Value{ .null = {} }).array.items[1].object.get("index").?.integer);
}

test "ocr_url_document" {
    // OCR request with a document URL input
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/ocr_url_document", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    const _result_json = try _client.ocr("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/doc.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}");
    defer std.heap.c_allocator.free(_result_json);
    std.debug.print("{s}\n", .{_result_json});
}
