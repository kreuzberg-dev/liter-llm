// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:54b3c540be94d43438a6a1fda1de120a77e4f5275eb369588e2ccf021e629144
// To regenerate: alef generate
// To verify freshness: alef verify

package io.xberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.literllm.LiterLlm;

/** E2e tests for category: list-models. */
public class ListModelsTest {


    @Test
    void testEmptyModelList() throws Exception {
        // List models response returns an empty data array when no models are available
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/empty_model_list";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listModels();
        assertTrue(result.data().size() >= 0, "expected at least 0 elements");
        assertEquals(0, result.data().size(), "expected exactly 0 elements");


    }


    @Test
    void testListModelsError401() throws Exception {
        // 401 Unauthorized error on list models request when API key is invalid
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/list_models_error_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.listModels();
        });
        // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testListModelsError500() throws Exception {
        // 500 Internal Server Error when listing models due to server failure
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        Exception thrown = assertThrows(Exception.class, () -> {
            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/list_models_error_500";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.listModels();
        });
        // skipped: declared error variant 'ServerError' not substantiated by this backend's generated error type
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

    }


    @Test
    void testListModelsFiltered() throws Exception {
        // List models response with multiple model objects
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/list_models_filtered";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listModels();
        assertTrue(result.data().size() >= 5, "expected at least 5 elements");
        assertEquals("model", result.data().get(0).object());


    }

}
