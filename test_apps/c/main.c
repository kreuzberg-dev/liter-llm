/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:1725cd8a72a8f8bdc554b4294b8a3bc707e4a29d7920fc15af7af988f4cc15ed
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
#include <stdio.h>
#include "test_runner.h"

enum alef_test_status alef_current_test_status = ALEF_TEST_PASSED;

int main(void) {
    int passed = 0;
    int failed = 0;
    int skipped = 0;

    /* Category: batches */
    printf("  Running test_edge_batch_already_cancelled...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_batch_already_cancelled();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_batch_empty_list...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_batch_empty_list();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_batch_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_batch_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_batch_invalid_file...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_batch_invalid_file();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_batch_not_found...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_batch_not_found();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_batch_completed...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_batch_completed();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_cancel_batch...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_cancel_batch();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_create_batch...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_create_batch();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_list_batches...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_list_batches();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_retrieve_batch...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_retrieve_batch();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: chat */
    printf("  Running test_developer_message...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_developer_message();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_chat_max_tokens...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_chat_max_tokens();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_chat_system_only...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_chat_system_only();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_chat_temperature_zero...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_chat_temperature_zero();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_finish_reason_content_filter...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_finish_reason_content_filter();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_finish_reason_length...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_finish_reason_length();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_multi_turn_conversation...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_multi_turn_conversation();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_parallel_tool_calls...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_parallel_tool_calls();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_response_format_json_object...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_response_format_json_object();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_response_format_json_schema...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_response_format_json_schema();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_seed_parameter...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_seed_parameter();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_stop_sequences...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_stop_sequences();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_tool_choice_required...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_tool_choice_required();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_tool_choice_specific...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_tool_choice_specific();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: configuration */
    printf("  Running test_custom_base_url...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_custom_base_url();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_extra_headers...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_extra_headers();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_local_provider_llamacpp...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_local_provider_llamacpp();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_local_provider_ollama...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_local_provider_ollama();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_local_provider_vllm...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_local_provider_vllm();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: contract */
    printf("  Running test_binding_api_parity...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_binding_api_parity();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_contract_ocr...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_contract_ocr();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_contract_search...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_contract_search();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: custom_provider */
    printf("  Running test_provider_auth...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_provider_auth();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_register_provider...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_register_provider();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: embed */
    printf("  Running test_batch_embed...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_batch_embed();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_embed_batch_input...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_embed_batch_input();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_embed_empty_input...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_embed_empty_input();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_embed_base64...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_embed_base64();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_embed_encoding_format...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_embed_encoding_format();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_embed_error_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_embed_error_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_embed_with_dimensions...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_embed_with_dimensions();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_local_embed_ollama...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_local_embed_ollama();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: error-handling */
    printf("  Running test_anthropic_error_auth...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_anthropic_error_auth();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_azure_error_auth...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_azure_error_auth();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_bad_request_400...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_bad_request_400();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_bedrock_error_auth...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_bedrock_error_auth();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_content_policy_violation...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_content_policy_violation();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_context_window_exceeded...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_context_window_exceeded();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_empty_response_body...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_empty_response_body();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_forbidden_403...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_forbidden_403();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_gateway_timeout_504...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_gateway_timeout_504();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_github_copilot_error_auth...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_github_copilot_error_auth();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_not_found_404...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_not_found_404();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_rate_limit_429...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_rate_limit_429();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_server_error_500...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_server_error_500();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_service_unavailable_502...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_service_unavailable_502();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_timeout_error...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_timeout_error();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_vertex_error_auth...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_vertex_error_auth();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: files */
    printf("  Running test_edge_file_empty_list...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_file_empty_list();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_file_large_upload...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_file_large_upload();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_file_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_file_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_file_bad_purpose...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_file_bad_purpose();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_file_not_found...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_file_not_found();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_create_file...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_create_file();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_delete_file...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_delete_file();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_file_content...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_file_content();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_list_files...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_list_files();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_retrieve_file...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_retrieve_file();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: image-generate */
    printf("  Running test_edge_image_b64_response...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_image_b64_response();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_image_empty_prompt...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_image_empty_prompt();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_image_multiple_n...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_image_multiple_n();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_image_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_image_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_image_bad_request...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_image_bad_request();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_image_rate_limit...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_image_rate_limit();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_image_basic...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_image_basic();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_image_multiple...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_image_multiple();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_image_with_size...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_image_with_size();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: list-models */
    printf("  Running test_empty_model_list...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_empty_model_list();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_list_models_error_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_list_models_error_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_list_models_error_500...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_list_models_error_500();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_list_models_filtered...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_list_models_filtered();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: moderate */
    printf("  Running test_edge_moderate_all_categories...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_moderate_all_categories();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_moderate_empty_input...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_moderate_empty_input();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_moderate_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_moderate_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_moderate_bad_request...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_moderate_bad_request();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_moderate_batch...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_moderate_batch();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_moderate_flagged...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_moderate_flagged();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_moderate_single...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_moderate_single();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: ocr */
    printf("  Running test_ocr_error_400...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_ocr_error_400();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_ocr_error_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_ocr_error_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_ocr_multi_page...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_ocr_multi_page();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_ocr_url_document...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_ocr_url_document();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: parity */
    printf("  Running test_api_surface_parity...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_api_surface_parity();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: rerank */
    printf("  Running test_edge_rerank_empty_query...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_rerank_empty_query();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_rerank_single_doc...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_rerank_single_doc();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_rerank_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_rerank_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_rerank_bad_request...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_rerank_bad_request();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_rerank_basic...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_rerank_basic();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_rerank_return_docs...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_rerank_return_docs();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_rerank_with_top_n...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_rerank_with_top_n();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: responses */
    printf("  Running test_edge_response_empty_output...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_response_empty_output();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_response_large_input...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_response_large_input();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_response_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_response_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_response_bad_request...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_response_bad_request();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_response_not_found...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_response_not_found();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_cancel_response...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_cancel_response();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_create_response...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_create_response();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_response_with_tools...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_response_with_tools();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_retrieve_response...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_retrieve_response();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: search */
    printf("  Running test_search_basic...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_search_basic();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_search_empty_results...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_search_empty_results();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_search_error_400...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_search_error_400();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_search_error_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_search_error_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_search_with_max_results...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_search_with_max_results();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: smoke */
    printf("  Running test_anthropic_chat...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_anthropic_chat();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_azure_chat...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_azure_chat();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_azure_embed...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_azure_embed();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_basic_chat...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_basic_chat();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_basic_embed...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_basic_embed();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_basic_list_models...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_basic_list_models();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_bedrock_chat...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_bedrock_chat();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_github_copilot_chat...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_github_copilot_chat();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_cache_memory...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_cache_memory();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_chat_anthropic...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_chat_anthropic();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_chat_gemini...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_chat_gemini();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_chat_openai...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_chat_openai();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_embed_openai...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_embed_openai();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_list_models_openai...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_list_models_openai();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_provider_routing...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_provider_routing();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_streaming_openai...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_streaming_openai();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_vertex_chat...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_vertex_chat();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_vertex_embed...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_vertex_embed();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: speech */
    printf("  Running test_edge_speech_all_voices...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_speech_all_voices();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_speech_long_input...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_speech_long_input();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_speech_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_speech_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_speech_bad_model...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_speech_bad_model();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_speech_basic...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_speech_basic();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_speech_mp3_format...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_speech_mp3_format();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: streaming */
    printf("  Running test_anthropic_stream...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_anthropic_stream();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_azure_stream...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_azure_stream();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_basic_stream...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_basic_stream();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_bedrock_stream...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_bedrock_stream();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_stream_function_call...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_stream_function_call();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_empty_stream...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_empty_stream();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_local_stream_ollama...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_local_stream_ollama();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_stream_content_policy_error...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_stream_content_policy_error();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_stream_done_signal...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_stream_done_signal();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_stream_error_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_stream_error_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_stream_multiple_choices...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_stream_multiple_choices();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_stream_with_tool_calls...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_stream_with_tool_calls();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_vertex_stream...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_vertex_stream();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: tool-calling */
    printf("  Running test_anthropic_tool_calling...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_anthropic_tool_calling();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_single_tool_call...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_single_tool_call();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: transcribe */
    printf("  Running test_edge_transcribe_empty_audio...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_transcribe_empty_audio();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_edge_transcribe_with_timestamps...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_edge_transcribe_with_timestamps();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_transcribe_auth_401...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_transcribe_auth_401();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_error_transcribe_bad_format...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_error_transcribe_bad_format();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_transcribe_basic...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_transcribe_basic();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_smoke_transcribe_with_language...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_smoke_transcribe_with_language();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    /* Category: types */
    printf("  Running test_all_message_types...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_all_message_types();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }
    printf("  Running test_multimodal_content...");
    alef_current_test_status = ALEF_TEST_PASSED;
    test_multimodal_content();
    if (alef_current_test_status == ALEF_TEST_FAILED) {
        printf(" FAILED\n");
        failed++;
    } else if (alef_current_test_status == ALEF_TEST_SKIPPED) {
        printf(" SKIPPED\n");
        skipped++;
    } else {
        printf(" PASSED\n");
        passed++;
    }

    printf("\nResults: %d passed, %d failed, %d skipped\n", passed, failed, skipped);
    return failed == 0 ? EXIT_SUCCESS : EXIT_FAILURE;
}
