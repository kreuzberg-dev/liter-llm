/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:081cdeb27d3d038d083cbfae06dc80089b05cd477a65a1d4d50a3165fc56d02c
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: embed */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_batch_embed(void) {
    /* Embedding request with multiple input strings returns one embedding object per input */
    LITERLLMAlefHandle embedding_request_handle = literllm_embedding_request_from_json("{\"input\":[\"Hello\",\"World\"],\"model\":\"text-embedding-3-small\"}");
    assert(embedding_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/batch_embed", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_embed(client, embedding_request_handle);
    assert(result != 0 && "expected call to succeed");
    char* data = literllm_embedding_response_data(result);
    char* data_json = literllm_embedding_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_embedding = alef_json_get_string(data_0_json, "embedding");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 2 && "expected 2 elements");
    }
    {
        /* count_equals: count elements in array */
        assert(data_0_embedding != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_0_embedding);
        assert(elem_count == 5 && "expected 5 elements");
    }
    literllm_free_string(data);
    free(data_0_embedding);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_embedding_response_free(result);
    literllm_embedding_request_free(embedding_request_handle);
    literllm_default_client_free(client);
}

void test_edge_embed_batch_input(void) {
    /* Embedding request with multiple inputs returns multiple embedding objects */
    LITERLLMAlefHandle embedding_request_handle = literllm_embedding_request_from_json("{\"input\":[\"Hello world\",\"Goodbye world\"],\"model\":\"text-embedding-3-small\"}");
    assert(embedding_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_embed_batch_input", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_embed(client, embedding_request_handle);
    assert(result != 0 && "expected call to succeed");
    char* data = literllm_embedding_response_data(result);
    char* data_json = literllm_embedding_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_index = alef_json_get_string(data_0_json, "index");
    char* data_1_json = alef_json_array_get_index(data_json, 1);
    char* data_1_index = alef_json_get_string(data_1_json, "index");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 2 && "expected 2 elements");
    }
    assert(data_0_index != NULL && atoll(data_0_index) == 0 && "equals assertion failed");
    assert(data_1_index != NULL && atoll(data_1_index) == 1 && "equals assertion failed");
    literllm_free_string(data);
    free(data_0_index);
    free(data_1_index);
    free(data_1_json);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_embedding_response_free(result);
    literllm_embedding_request_free(embedding_request_handle);
    literllm_default_client_free(client);
}

void test_edge_embed_empty_input(void) {
    /* Embedding request with empty string input returns empty data array */
    LITERLLMAlefHandle embedding_request_handle = literllm_embedding_request_from_json("{\"input\":\"\",\"model\":\"text-embedding-3-small\"}");
    assert(embedding_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_embed_empty_input", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_embed(client, embedding_request_handle);
    assert(result != 0 && "expected call to succeed");
    char* data = literllm_embedding_response_data(result);
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 0 && "expected 0 elements");
    }
    literllm_free_string(data);
    literllm_embedding_response_free(result);
    literllm_embedding_request_free(embedding_request_handle);
    literllm_default_client_free(client);
}

void test_embed_base64(void) {
    /* Embedding request with encoding_format base64 decodes to a float array embedding */
    LITERLLMAlefHandle embedding_request_handle = literllm_embedding_request_from_json("{\"encoding_format\":\"base64\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}");
    assert(embedding_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/embed_base64", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_embed(client, embedding_request_handle);
    assert(result != 0 && "expected call to succeed");
    char* data = literllm_embedding_response_data(result);
    char* data_json = literllm_embedding_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_embedding = alef_json_get_string(data_0_json, "embedding");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 1 && "expected 1 elements");
    }
    {
        /* count_equals: count elements in array */
        assert(data_0_embedding != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_0_embedding);
        assert(elem_count == 4 && "expected 4 elements");
    }
    literllm_free_string(data);
    free(data_0_embedding);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_embedding_response_free(result);
    literllm_embedding_request_free(embedding_request_handle);
    literllm_default_client_free(client);
}

void test_embed_encoding_format(void) {
    /* Embedding request with explicit encoding_format of float returns float array embeddings */
    LITERLLMAlefHandle embedding_request_handle = literllm_embedding_request_from_json("{\"encoding_format\":\"float\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}");
    assert(embedding_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/embed_encoding_format", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_embed(client, embedding_request_handle);
    assert(result != 0 && "expected call to succeed");
    char* data = literllm_embedding_response_data(result);
    char* data_json = literllm_embedding_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_embedding = alef_json_get_string(data_0_json, "embedding");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 1 && "expected 1 elements");
    }
    {
        /* count_equals: count elements in array */
        assert(data_0_embedding != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_0_embedding);
        assert(elem_count == 5 && "expected 5 elements");
    }
    literllm_free_string(data);
    free(data_0_embedding);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_embedding_response_free(result);
    literllm_embedding_request_free(embedding_request_handle);
    literllm_default_client_free(client);
}

void test_embed_error_401(void) {
    /* 401 Unauthorized error on embedding request when API key is invalid */
    LITERLLMAlefHandle embedding_request_handle = literllm_embedding_request_from_json("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}");
    if (embedding_request_handle == 0) { ALEF_TEST_PASS(); }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/embed_error_401", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_embed(client, embedding_request_handle);
    assert(result == 0 && "expected call to fail");
    // skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
    // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    literllm_embedding_request_free(embedding_request_handle);
    literllm_default_client_free(client);
}

void test_embed_with_dimensions(void) {
    /* Embedding request with explicit dimensions parameter returns embeddings of the requested size */
    LITERLLMAlefHandle embedding_request_handle = literllm_embedding_request_from_json("{\"dimensions\":256,\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}");
    assert(embedding_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/embed_with_dimensions", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_embed(client, embedding_request_handle);
    assert(result != 0 && "expected call to succeed");
    char* data = literllm_embedding_response_data(result);
    char* data_json = literllm_embedding_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_embedding = alef_json_get_string(data_0_json, "embedding");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 1 && "expected 1 elements");
    }
    {
        /* count_equals: count elements in array */
        assert(data_0_embedding != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_0_embedding);
        assert(elem_count == 8 && "expected 8 elements");
    }
    literllm_free_string(data);
    free(data_0_embedding);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_embedding_response_free(result);
    literllm_embedding_request_free(embedding_request_handle);
    literllm_default_client_free(client);
}

void test_local_embed_ollama(void) {
    /* Embedding request via Ollama local provider with all-minilm model */
    LITERLLMAlefHandle embedding_request_handle = literllm_embedding_request_from_json("{\"input\":\"The quick brown fox jumps over the lazy dog\",\"model\":\"ollama/all-minilm\"}");
    assert(embedding_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/local_embed_ollama", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_embed(client, embedding_request_handle);
    assert(result != 0 && "expected call to succeed");
    char* data = literllm_embedding_response_data(result);
    char* data_json = literllm_embedding_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_embedding = alef_json_get_string(data_0_json, "embedding");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 1 && "expected 1 elements");
    }
    {
        /* count_equals: count elements in array */
        assert(data_0_embedding != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_0_embedding);
        assert(elem_count == 32 && "expected 32 elements");
    }
    literllm_free_string(data);
    free(data_0_embedding);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_embedding_response_free(result);
    literllm_embedding_request_free(embedding_request_handle);
    literllm_default_client_free(client);
}
