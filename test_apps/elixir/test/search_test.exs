# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:fe67d5bc5294e35dc9e0e2d5976b941243a240cdc8dde2047fd7be52adcd9f4f
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: search
defmodule E2e.SearchTest do
  use ExUnit.Case, async: false

  describe "search_basic" do
    test "search_basic" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_basic")

      {:ok, result} =
        LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}")

      refute is_nil(result)
    end
  end

  describe "search_empty_results" do
    test "search_empty_results" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_empty_results")

      {:ok, result} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}")
      refute is_nil(result)
    end
  end

  describe "search_error_400" do
    test "search_error_400" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_error_400")

      assert {:error, __reason} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"\"}")
      # skipped: declared error variant 'BadRequest' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "search_error_401" do
    test "search_error_401" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_error_401")

      assert {:error, __reason} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"test\"}")
      # skipped: declared error variant 'Authentication' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "search_with_max_results" do
    test "search_with_max_results" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_with_max_results")

      {:ok, result} =
        LiterLlm.defaultclient_search_async(client, "{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}")

      assert length(result.results) == 2
    end
  end
end
