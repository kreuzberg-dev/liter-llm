# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:43c68a911ee6e98e8812f8449d89053d901569f759ff59a1e96be2d8e68b9bb6
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: image-generate
defmodule E2e.ImageGenerateTest do
  use ExUnit.Case, async: false

  describe "edge_image_b64_response" do
    test "edge_image_b64_response" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_image_b64_response")

      {:ok, result} =
        LiterLlm.defaultclient_image_generate_async(
          client,
          "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}"
        )

      assert length(result.data) == 1
      assert Enum.at(result.data, 0).b64_json not in [nil, "", [], %{}]
    end
  end

  describe "edge_image_empty_prompt" do
    test "edge_image_empty_prompt" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_image_empty_prompt")

      assert {:error, __reason} =
               LiterLlm.defaultclient_image_generate_async(
                 client,
                 "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}"
               )

      # skipped: declared error variant 'BadRequest' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "edge_image_multiple_n" do
    test "edge_image_multiple_n" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_image_multiple_n")

      {:ok, result} = LiterLlm.defaultclient_image_generate_async(client, "{\"model\":\"dall-e-3\",\"n\":3,\"prompt\":\"A cat\"}")
      assert length(result.data) == 3
      assert Enum.at(result.data, 0).url not in [nil, "", [], %{}]
      assert Enum.at(result.data, 1).url not in [nil, "", [], %{}]
      assert Enum.at(result.data, 2).url not in [nil, "", [], %{}]
    end
  end

  describe "error_image_auth_401" do
    test "error_image_auth_401" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_image_auth_401")

      assert {:error, __reason} =
               LiterLlm.defaultclient_image_generate_async(
                 client,
                 "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}"
               )

      # skipped: declared error variant 'Authentication' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "error_image_bad_request" do
    test "error_image_bad_request" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_image_bad_request")

      assert {:error, __reason} =
               LiterLlm.defaultclient_image_generate_async(
                 client,
                 "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}"
               )

      # skipped: declared error variant 'BadRequest' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "error_image_rate_limit" do
    test "error_image_rate_limit" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_image_rate_limit")

      assert {:error, __reason} =
               LiterLlm.defaultclient_image_generate_async(
                 client,
                 "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}"
               )

      # skipped: declared error variant 'RateLimited' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "smoke_image_basic" do
    test "smoke_image_basic" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_image_basic")

      {:ok, result} =
        LiterLlm.defaultclient_image_generate_async(
          client,
          "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}"
        )

      assert length(result.data) == 1
      assert Enum.at(result.data, 0).url not in [nil, "", [], %{}]
    end
  end

  describe "smoke_image_multiple" do
    test "smoke_image_multiple" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_image_multiple")

      {:ok, result} =
        LiterLlm.defaultclient_image_generate_async(
          client,
          "{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}"
        )

      assert length(result.data) == 3
      assert Enum.at(result.data, 0).url not in [nil, "", [], %{}]
    end
  end

  describe "smoke_image_with_size" do
    test "smoke_image_with_size" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_image_with_size")

      {:ok, result} =
        LiterLlm.defaultclient_image_generate_async(
          client,
          "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}"
        )

      assert length(result.data) == 1
      assert Enum.at(result.data, 0).url not in [nil, "", [], %{}]
    end
  end
end
