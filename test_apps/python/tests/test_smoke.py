# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:6352b7e677dee43860f22a05d79ee6f80c54c8544c53e6270807be09f66c649b
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: smoke."""

import os
import sys

import pytest
from liter_llm import create_client
from liter_llm._internal_bindings import ChatCompletionRequest
from liter_llm._internal_bindings import EmbeddingRequest


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


@pytest.mark.asyncio

async def test_anthropic_chat() -> None:
    """Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/anthropic_chat")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"You are a helpful assistant.\",\"role\":\"system\"},{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\",\"temperature\":0}")

    result = await client.chat(req)
    assert len(result.choices) == 1
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()
    assert result.model == "claude-3-5-sonnet-20241022"
    assert (result.usage.total_tokens if result.usage else None) == 23


@pytest.mark.asyncio

async def test_azure_chat() -> None:
    """Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/azure_chat")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\",\"temperature\":0}")

    result = await client.chat(req)
    assert len(result.choices) == 1
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()
    assert result.model == "gpt-4"
    assert (result.usage.total_tokens if result.usage else None) == 13


@pytest.mark.asyncio

async def test_azure_embed() -> None:
    """Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/azure_embed")
    req = EmbeddingRequest.from_json("{\"input\":\"Hello world\",\"model\":\"azure/text-embedding-ada-002\"}")

    result = await client.embed(req)
    assert len(result.data) == 1
    assert len(result.data[0].embedding) == 1480


@pytest.mark.asyncio

async def test_basic_chat() -> None:
    """Basic chat completion with a single user message."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/basic_chat")
    req = ChatCompletionRequest.from_json("{\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":0}")

    result = await client.chat(req)
    assert len(result.choices) == 1
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()
    assert (result.usage.total_tokens if result.usage else None) == 15
    assert result.model == "gpt-4"


@pytest.mark.asyncio

async def test_basic_embed() -> None:
    """Basic embedding request for a single input string."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/basic_embed")
    req = EmbeddingRequest.from_json("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}")

    result = await client.embed(req)
    assert len(result.data) == 1
    assert len(result.data[0].embedding) == 5


@pytest.mark.asyncio

async def test_basic_list_models() -> None:
    """List available models from the API."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/basic_list_models")

    result = await client.list_models()
    assert len(result.data) >= 1


@pytest.mark.asyncio

async def test_bedrock_chat() -> None:
    """Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/bedrock_chat")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\",\"temperature\":0}")

    result = await client.chat(req)
    assert len(result.choices) == 1
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()
    assert result.model == "anthropic.claude-3-sonnet-20240229-v1:0"
    assert (result.usage.total_tokens if result.usage else None) == 15


@pytest.mark.asyncio

async def test_github_copilot_chat() -> None:
    """Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/github_copilot_chat")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\",\"temperature\":0}")

    result = await client.chat(req)
    assert len(result.choices) == 1
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()
    assert result.model == "gpt-4o"
    assert (result.usage.total_tokens if result.usage else None) == 14


@pytest.mark.asyncio

async def test_smoke_cache_memory() -> None:
    """Test in-memory caching by sending identical requests twice."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        sys.stdout.write("smoke_cache_memory: using real API (OPENAI_API_KEY is set)\n")
        sys.stdout.flush()
        client = create_client(api_key=api_key)
    else:
        sys.stdout.write("smoke_cache_memory: using mock server (OPENAI_API_KEY not set)\n")
        sys.stdout.flush()
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_cache_memory")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":5,\"messages\":[{\"content\":\"What is 2+2? Answer with just the number.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")

    result = await client.chat(req)
    assert result.choices is not None and (not hasattr(result.choices, "__len__") or len(result.choices) > 0)


@pytest.mark.asyncio

async def test_smoke_chat_anthropic() -> None:
    """Basic chat completion against real Anthropic API."""
    api_key = os.environ.get("ANTHROPIC_API_KEY")
    if api_key:
        sys.stdout.write("smoke_chat_anthropic: using real API (ANTHROPIC_API_KEY is set)\n")
        sys.stdout.flush()
        client = create_client(api_key=api_key)
    else:
        sys.stdout.write("smoke_chat_anthropic: using mock server (ANTHROPIC_API_KEY not set)\n")
        sys.stdout.flush()
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_chat_anthropic")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-sonnet-4-20250514\"}")

    result = await client.chat(req)
    assert result.choices is not None and (not hasattr(result.choices, "__len__") or len(result.choices) > 0)
    assert result.usage is not None and (not hasattr(result.usage, "__len__") or len(result.usage) > 0)


@pytest.mark.asyncio

async def test_smoke_chat_gemini() -> None:
    """Basic chat completion against real Google Gemini API."""
    api_key = os.environ.get("GEMINI_API_KEY")
    if api_key:
        sys.stdout.write("smoke_chat_gemini: using real API (GEMINI_API_KEY is set)\n")
        sys.stdout.flush()
        client = create_client(api_key=api_key)
    else:
        sys.stdout.write("smoke_chat_gemini: using mock server (GEMINI_API_KEY not set)\n")
        sys.stdout.flush()
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_chat_gemini")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"gemini/gemini-2.5-flash-lite\"}")

    result = await client.chat(req)
    assert result.choices is not None and (not hasattr(result.choices, "__len__") or len(result.choices) > 0)
    assert result.usage is not None and (not hasattr(result.usage, "__len__") or len(result.usage) > 0)


@pytest.mark.asyncio

async def test_smoke_chat_openai() -> None:
    """Basic chat completion against real OpenAI API."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        sys.stdout.write("smoke_chat_openai: using real API (OPENAI_API_KEY is set)\n")
        sys.stdout.flush()
        client = create_client(api_key=api_key)
    else:
        sys.stdout.write("smoke_chat_openai: using mock server (OPENAI_API_KEY not set)\n")
        sys.stdout.flush()
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_chat_openai")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")

    result = await client.chat(req)
    assert result.choices is not None and (not hasattr(result.choices, "__len__") or len(result.choices) > 0)
    assert result.usage is not None and (not hasattr(result.usage, "__len__") or len(result.usage) > 0)


@pytest.mark.asyncio

async def test_smoke_embed_openai() -> None:
    """Embeddings request against real OpenAI API."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        sys.stdout.write("smoke_embed_openai: using real API (OPENAI_API_KEY is set)\n")
        sys.stdout.flush()
        client = create_client(api_key=api_key)
    else:
        sys.stdout.write("smoke_embed_openai: using mock server (OPENAI_API_KEY not set)\n")
        sys.stdout.flush()
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_embed_openai")
    req = EmbeddingRequest.from_json("{\"input\":[\"Hello world\"],\"model\":\"openai/text-embedding-3-small\"}")

    result = await client.embed(req)
    assert result.data is not None and (not hasattr(result.data, "__len__") or len(result.data) > 0)


@pytest.mark.asyncio

async def test_smoke_list_models_openai() -> None:
    """List models against real OpenAI API."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        sys.stdout.write("smoke_list_models_openai: using real API (OPENAI_API_KEY is set)\n")
        sys.stdout.flush()
        client = create_client(api_key=api_key)
    else:
        sys.stdout.write("smoke_list_models_openai: using mock server (OPENAI_API_KEY not set)\n")
        sys.stdout.flush()
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_list_models_openai")

    result = await client.list_models()
    assert len(result.data) >= 1


@pytest.mark.asyncio

async def test_smoke_provider_routing() -> None:
    """Test provider routing by sending requests to OpenAI and Anthropic."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        sys.stdout.write("smoke_provider_routing: using real API (OPENAI_API_KEY is set)\n")
        sys.stdout.flush()
        client = create_client(api_key=api_key)
    else:
        sys.stdout.write("smoke_provider_routing: using mock server (OPENAI_API_KEY not set)\n")
        sys.stdout.flush()
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_provider_routing")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":5,\"messages\":[{\"content\":\"Say hi.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")

    result = await client.chat(req)
    assert result.choices is not None and (not hasattr(result.choices, "__len__") or len(result.choices) > 0)


@pytest.mark.asyncio

async def test_smoke_streaming_openai() -> None:
    """Chat streaming against real OpenAI API, verifies chunks received."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        sys.stdout.write("smoke_streaming_openai: using real API (OPENAI_API_KEY is set)\n")
        sys.stdout.flush()
        client = create_client(api_key=api_key)
    else:
        sys.stdout.write("smoke_streaming_openai: using mock server (OPENAI_API_KEY not set)\n")
        sys.stdout.flush()
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_streaming_openai")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":50,\"messages\":[{\"content\":\"Count from 1 to 5.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")

    result = client.chat_stream(req)
    chunks = []
    async for chunk in result:
        chunks.append(chunk)
    assert len(chunks) >= 1


@pytest.mark.asyncio

async def test_vertex_chat() -> None:
    """Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/vertex_chat")
    req = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\",\"temperature\":0}")

    result = await client.chat(req)
    assert len(result.choices) == 1
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()
    assert result.model == "gemini-2.0-flash"
    assert (result.usage.total_tokens if result.usage else None) == 13


@pytest.mark.asyncio

async def test_vertex_embed() -> None:
    """Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/vertex_embed")
    req = EmbeddingRequest.from_json("{\"input\":\"Hello\",\"model\":\"vertex_ai/text-embedding-005\"}")

    result = await client.embed(req)
    assert len(result.data) == 1
    assert len(result.data[0].embedding) == 160
