# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:37b231915935169cdf1c91ccee3ea09dcbb74f0d5a004b13c4a2c5a824e96e12
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: image-generate."""

import os

import pytest
from liter_llm import create_client
from liter_llm._internal_bindings import CreateImageRequest


@pytest.mark.asyncio

async def test_edge_image_b64_response() -> None:
    """Image generation returning base64-encoded data instead of URL."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_image_b64_response")
    req = CreateImageRequest.from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}")

    result = await client.image_generate(req)
    assert len(result.data) == 1
    assert result.data[0].b64_json is not None and (not hasattr(result.data[0].b64_json, "__len__") or len(result.data[0].b64_json) > 0)


@pytest.mark.asyncio

async def test_edge_image_empty_prompt() -> None:
    """Image generation with an empty prompt returns 400."""
    # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_image_empty_prompt")
        req = CreateImageRequest.from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}")
        await client.image_generate(req)
    assert "BadRequest" in str(exc_info.value) or "BadRequest" in type(exc_info.value).__name__


@pytest.mark.asyncio

async def test_edge_image_multiple_n() -> None:
    """Image generation requesting multiple images with n=3."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_image_multiple_n")
    req = CreateImageRequest.from_json("{\"model\":\"dall-e-3\",\"n\":3,\"prompt\":\"A cat\"}")

    result = await client.image_generate(req)
    assert len(result.data) == 3
    assert result.data[0].url is not None and (not hasattr(result.data[0].url, "__len__") or len(result.data[0].url) > 0)
    assert result.data[1].url is not None and (not hasattr(result.data[1].url, "__len__") or len(result.data[1].url) > 0)
    assert result.data[2].url is not None and (not hasattr(result.data[2].url, "__len__") or len(result.data[2].url) > 0)


@pytest.mark.asyncio

async def test_error_image_auth_401() -> None:
    """401 Unauthorized when generating images with invalid API key."""
    # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_image_auth_401")
        req = CreateImageRequest.from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}")
        await client.image_generate(req)
    assert "Authentication" in str(exc_info.value) or "Authentication" in type(exc_info.value).__name__


@pytest.mark.asyncio

async def test_error_image_bad_request() -> None:
    """400 Bad Request when image generation parameters are invalid."""
    # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_image_bad_request")
        req = CreateImageRequest.from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}")
        await client.image_generate(req)
    assert "BadRequest" in str(exc_info.value) or "BadRequest" in type(exc_info.value).__name__


@pytest.mark.asyncio

async def test_error_image_rate_limit() -> None:
    """429 Rate limit exceeded for image generation."""
    # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_image_rate_limit")
        req = CreateImageRequest.from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}")
        await client.image_generate(req)
    assert "RateLimited" in str(exc_info.value) or "RateLimited" in type(exc_info.value).__name__


@pytest.mark.asyncio

async def test_smoke_image_basic() -> None:
    """Basic image generation with a text prompt."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_image_basic")
    req = CreateImageRequest.from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}")

    result = await client.image_generate(req)
    assert len(result.data) == 1
    assert result.data[0].url is not None and (not hasattr(result.data[0].url, "__len__") or len(result.data[0].url) > 0)


@pytest.mark.asyncio

async def test_smoke_image_multiple() -> None:
    """Image generation requesting multiple images."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_image_multiple")
    req = CreateImageRequest.from_json("{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}")

    result = await client.image_generate(req)
    assert len(result.data) == 3
    assert result.data[0].url is not None and (not hasattr(result.data[0].url, "__len__") or len(result.data[0].url) > 0)


@pytest.mark.asyncio

async def test_smoke_image_with_size() -> None:
    """Image generation with explicit size parameter."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_image_with_size")
    req = CreateImageRequest.from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}")

    result = await client.image_generate(req)
    assert len(result.data) == 1
    assert result.data[0].url is not None and (not hasattr(result.data[0].url, "__len__") or len(result.data[0].url) > 0)
