#!/usr/bin/env bash
# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:2e27314db88af20145352c5a7b7db041ec2ec5b51a732ff4f1a2deb75dcfa3ad
# To regenerate: alef generate
# To verify freshness: alef verify
# Tests the Homebrew CLI formula.
set -euo pipefail

VERSION="1.18.4"
TAP="xberg-io/homebrew-tap"
CLI_FORMULA="liter-llm"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

PASS=0
FAIL=0

pass() {
  echo "PASS: $1"
  PASS=$((PASS + 1))
}
fail() {
  echo "FAIL: $1 — $2" >&2
  FAIL=$((FAIL + 1))
}

# Trust and install formulae.
brew trust "$TAP" || true
brew bundle install --file="$SCRIPT_DIR/Brewfile"

# Test: version.
_output_version=$(eval "$CLI_FORMULA --version" 2>&1 || true)
if [[ "$_output_version" == *"$VERSION"* ]]; then
  pass "version"
else
  fail "version" "expected '$VERSION' in output, got: $_output_version"
fi

# Test: help.
_output_help=$(eval "$CLI_FORMULA --help" 2>&1 || true)
if [[ "$_output_help" == *"Usage"* ]]; then
  pass "help"
else
  fail "help" "expected 'Usage' in output, got: $_output_help"
fi

echo ""
echo "Results: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ]
